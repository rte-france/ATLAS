from abc import ABC
from typing import Generic

from atlas.abstract_class.abstract_parameters import P


class AbstractOrchestrator(ABC, Generic[P]):
    """Placeholder abstract class for orchestrator."""

    # TODO this function should contains common part from Workflow and ActionPlan classes
    pass
