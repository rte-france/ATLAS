{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Atlas","text":"<p>Atlas is a powerful simulation and optimization tool developed by Artelys for RTE (R\u00e9seau de Transport d'\u00c9lectricit\u00e9) to model and analyze electricity markets. It enables comprehensive simulation of various market mechanisms including day-ahead markets, intraday trading, and reserve markets, providing essential insights for power system operations and planning.</p> <p>Atlas uses a workflow-based architecture to simulate realistic market dynamics over extended periods.</p>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li> <p> Getting started</p> <p> Installation</p> <p> Quick Start (WIP)</p> </li> <li> <p> Overview</p> <p>Let's dig into the market concepts and the altas feature.</p> <p> Features (WIP)</p> </li> <li> <p> Release Notes</p> <p>Check out the latest features, bug fixes and improvements in the release notes.</p> <p> What's new</p> </li> <li> <p> Open Source, MPL 2.0</p> <p>Atlas is licensed under MPL 2.0 and available on GitHub.</p> <p> License</p> </li> </ul>"},{"location":"authors/","title":"\ud83e\uddfe Authors &amp; Contributors","text":""},{"location":"authors/#project-ownership","title":"\ud83c\udfe2 Project Ownership","text":"<p>This project is the property of RTE (R\u00e9seau de Transport d'\u00c9lectricit\u00e9).</p>"},{"location":"authors/#developed-by","title":"\ud83d\udee0\ufe0f Developed by","text":"<p>Artelys \u2013 https://www.artelys.com</p> <p>Development, maintenance, and support are carried out by Artelys on behalf of RTE.</p>"},{"location":"authors/#individual-contributors","title":"\ud83d\udc65 Individual Contributors","text":"<p>The following individuals have contributed to this project (in alphabetical order):</p> <ul> <li>Your Name \u2013 Core development</li> <li>Other Names \u2013 [Role or contribution]</li> </ul> <p>If you contributed and are not listed here, feel free to open a pull request to add yourself!</p>"},{"location":"changelog/","title":"\ud83d\udccb Changelog","text":"<p>All notable changes to this project will be documented in this file.</p>"},{"location":"changelog/#versioning","title":"\ud83d\udccc Versioning","text":"<ul> <li><code>MAJOR</code> version when you make incompatible API changes,</li> <li><code>MINOR</code> version when you add functionality in a backwards compatible manner,</li> <li><code>PATCH</code> version when you make backwards compatible bug fixes.</li> </ul>"},{"location":"changelog/#release-history-legend","title":"\ud83d\udd58 Release History Legend","text":"<ul> <li>\u2728 Feature</li> <li>\ud83d\udc1b Fix</li> <li>\ud83d\udd04 Change</li> <li>\ud83e\uddf9 Refactor</li> <li>\ud83d\udcda Docs</li> <li>\ud83d\udd12 Security</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>Thanks for taking the time to contribute! We welcome bug reports, feature suggestions, and pull requests.</p>"},{"location":"contributing/#project-setup","title":"\ud83d\udee0\ufe0f Project Setup","text":"<p>This project uses <code>uv</code> for dependency management and Python environment isolation.</p>"},{"location":"contributing/#1-clone-the-repository","title":"1. Clone the Repository","text":"<pre><code>git clone https://github.com/rte-france/ATLAS.git\n</code></pre>"},{"location":"contributing/#set-up-the-environment","title":"Set up the Environment","text":"<pre><code>uv sync --all-groups\n</code></pre> <p>Install pre-commit hooks</p> <pre><code>uv run pre-commit install\n</code></pre> <p>If you're contributing new dependencies, install them with:</p> <pre><code>uv add package-name\n</code></pre>"},{"location":"contributing/#running-tests","title":"\ud83e\uddea Running Tests","text":"<p>We use pytest for testing. To run the tests:</p> <pre><code>uv run pytest .\n</code></pre> <p>Make sure all tests pass before submitting a PR.</p>"},{"location":"contributing/#code-style","title":"\ud83e\uddf9 Code Style","text":"<p>We use <code>ruff</code> to format and lint the code:</p> <pre><code>uv run ruff format atlas\nuv run ruff check atlas\n</code></pre> <p>We also use <code>mypy</code> for typing analysis :</p> <pre><code>uv run mypy atlas\n</code></pre>"},{"location":"contributing/#making-a-pull-request","title":"\ud83e\udde9 Making a Pull Request","text":"<p>Before contributing code, make sure you've:</p> <ul> <li>Synced with the latest version of the <code>develop</code> branch.</li> <li>Created a descriptive branch name using one of the following conventions:</li> </ul>"},{"location":"contributing/#branch-naming-conventions","title":"\ud83d\udd16 Branch Naming Conventions","text":"<ul> <li> <p><code>feat/your-feature-name</code> or <code>feature/your-feature-name</code> \u2013 for new features</p> </li> <li> <p><code>fix/your-fix-description</code> \u2013 for bug fixes</p> </li> <li> <p><code>chore/your-task</code> \u2013 for non-functional tasks like config or dependency updates</p> </li> <li> <p><code>docs/your-doc-change</code> \u2013 for documentation-only changes</p> </li> <li> <p><code>test/your-test-task</code> \u2013 for adding or improving tests</p> </li> </ul>"},{"location":"getting_started/","title":"Getting Started","text":""},{"location":"getting_started/#quick-start","title":"Quick start","text":"<p>This project is under development. There is no public release. To use it :</p> <pre><code>git clone https://github.com/rte-france/ATLAS.git\n</code></pre> <p>Then, install uv and set up your environment :</p> <pre><code>uv sync\n</code></pre> <p>The aim is to have a basic installation :</p> <pre><code>pip install atlas-rte\n\nuv add atlas-rte # using uv\n</code></pre>"},{"location":"getting_started/#available-solver","title":"Available solver","text":"<p>Atlas project uses <code>or-tools</code> as modeller, you can use the solver of your choice. We provided a special support for the commercial Xpress solver. Here is a quick install guide :</p>"},{"location":"getting_started/#xpress-for-windows","title":"Xpress for windows","text":"<ul> <li>Download file (You will probably need to create an account) and install the file</li> <li>Move your license  <code>xpauth.xpr</code> in <code>xpressmp/bin/</code></li> </ul>"},{"location":"getting_started/#xpress-for-linux","title":"Xpress for linux","text":"<p>Find below a quick installation guide, more information can be find directly on FICO website : here</p> <ul> <li>Download file (You will probably need to create an account)</li> <li>Move the archive here : <code>/opt/xpressmp</code></li> <li>Enter the uncompressed archive : <code>cd &lt;archive_uncompressed&gt;</code></li> <li>Run the installer, select static licensing :</li> </ul> <pre><code>sudo chmod +x install.sh\nsudo ./install.sh\n</code></pre> <ul> <li> <p>Copy your license <code>xpauth.xpr</code> in <code>/opt/xpressmp/</code></p> </li> <li> <p>In your <code>~/.bashrc</code> or <code>~/.zshrc</code> :</p> </li> </ul> <pre><code>export XPRESS_DIR=\"/opt/xpressmp/y\" # if the installer has created a subfolder y/\nexport XPAUTH_PATH=\"/opt/xpressmp/xpauth.xpr\"\n</code></pre> <ul> <li>Now make sure everything is working running in python:</li> </ul> <pre><code>from atlas import OptimisationModel\n\nmodel = OptimisationModel('xpress')\n</code></pre>"},{"location":"release_process/","title":"Release Process","text":"<p>This document outlines the process for creating releases and publishing documentation for the Atlas project.</p>"},{"location":"release_process/#overview","title":"Overview","text":"<p>The Atlas project uses an automated release workflow that:</p> <ul> <li>Publishes packages to PyPI when version tags are pushed</li> <li>Automatically deploys versioned documentation using MkDocs and Mike</li> <li>Maintains both development and stable documentation versions</li> </ul>"},{"location":"release_process/#release-workflow","title":"Release Workflow","text":""},{"location":"release_process/#1-prepare-for-release","title":"1. Prepare for Release","text":"<ol> <li>Update Version: Update the version using <code>uv</code>:</li> </ol> <pre><code>uv version --bump major\n# possible values: major, minor, patch, stable, alpha, beta, rc, post, dev]\n</code></pre> <ol> <li>Update Changelog: Add release notes to <code>docs/changelog.md</code></li> </ol>"},{"location":"release_process/#2-create-release","title":"2. Create Release","text":"<ol> <li> <p>Create a pull-request from develop to main</p> </li> <li> <p>Create and Push Tag</p> </li> </ol>"},{"location":"release_process/#3-post-release","title":"3. Post-Release","text":"<ol> <li>Verify PyPI Publication: Check https://pypi.org/project/rte-atlas/</li> <li>Verify Documentation: Check the documentation site for the new version</li> <li>Create GitHub Release: Optionally create a GitHub release with release notes</li> </ol>"},{"location":"release_process/#documentation-versioning","title":"Documentation Versioning","text":""},{"location":"release_process/#how-it-works","title":"How It Works","text":"<p>The documentation system uses Mike for version management:</p> <ul> <li>Development docs (<code>dev</code>): Auto-deployed from <code>dev</code> branch</li> <li>Version docs (e.g., <code>0.1.0</code>, <code>0.2.0</code>): Auto-deployed from version tags</li> <li>Latest alias: Points to the most recent stable version</li> </ul>"},{"location":"release_process/#version-deployment-process","title":"Version Deployment Process","text":"<ol> <li>Main Branch Pushes:</li> <li>Deploys documentation as <code>dev</code> version</li> <li> <p>Updates <code>latest</code> alias to <code>dev</code></p> </li> <li> <p>Version Tag Pushes (e.g., <code>v0.2.0</code>):</p> </li> <li>Extracts version from tag (removes <code>v</code> prefix)</li> <li>Deploys documentation with version number</li> <li>Updates <code>latest</code> alias to new version</li> </ol>"},{"location":"release_process/#manual-documentation-operations","title":"Manual Documentation Operations","text":"<pre><code># List all deployed versions\nuv run mike list\n\n# Deploy a specific version manually\nuv run mike deploy --push --update-aliases 0.2.0 latest\n\n# Set default version\nuv run mike set-default --push latest\n\n# Delete a version\nuv run mike delete --push old-version\n</code></pre>"},{"location":"api/enum/","title":"Enums classes","text":""},{"location":"api/enum/#atlas.enums.ThermalStrategy","title":"atlas.enums.ThermalStrategy","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Thermal generation strategy classification.</p> <p>Attributes:</p> Name Type Description <code>BASE</code> <p>Base-load thermal strategy.</p> <code>INTERMEDIATE</code> <p>Intermediate thermal strategy.</p> <code>PEAK</code> <p>Peak-load thermal strategy.</p>"},{"location":"api/enum/#atlas.enums.ThermalStrategy.BASE","title":"BASE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BASE = 'Base'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ThermalStrategy.INTERMEDIATE","title":"INTERMEDIATE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INTERMEDIATE = 'Intermediate'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ThermalStrategy.PEAK","title":"PEAK  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PEAK = 'Peak'\n</code></pre>"},{"location":"api/enum/#atlas.enums.LoadType","title":"atlas.enums.LoadType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Represents different types of electrical loads.</p> <p>Attributes:</p> Name Type Description <code>BASE_LOAD</code> <p>Base load consumption.</p> <code>POWER_TO_GAS</code> <p>Power converted to gas.</p> <code>OTHER_NON_DISPATCHABLE_LOAD</code> <p>Other loads that cannot be dispatched.</p>"},{"location":"api/enum/#atlas.enums.LoadType.BASE_LOAD","title":"BASE_LOAD  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BASE_LOAD = 'BaseLoad'\n</code></pre>"},{"location":"api/enum/#atlas.enums.LoadType.OTHER_NON_DISPATCHABLE_LOAD","title":"OTHER_NON_DISPATCHABLE_LOAD  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>OTHER_NON_DISPATCHABLE_LOAD = 'OtherNonDispatchableLoad'\n</code></pre>"},{"location":"api/enum/#atlas.enums.LoadType.POWER_TO_GAS","title":"POWER_TO_GAS  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>POWER_TO_GAS = 'PowerToGas'\n</code></pre>"},{"location":"api/enum/#atlas.enums.StorageType","title":"atlas.enums.StorageType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Represents different energy storage technologies.</p> <p>Attributes:</p> Name Type Description <code>BATTERY</code> <p>Battery storage.</p> <code>PUMPED_HYDRAULIC_STORAGE</code> <p>Pumped hydroelectric storage.</p> <code>ELECTRIC_VEHICLE</code> <p>Electric vehicle used as storage.</p>"},{"location":"api/enum/#atlas.enums.StorageType.BATTERY","title":"BATTERY  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BATTERY = 'Battery'\n</code></pre>"},{"location":"api/enum/#atlas.enums.StorageType.ELECTRIC_VEHICLE","title":"ELECTRIC_VEHICLE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ELECTRIC_VEHICLE = 'ElectricVehicle'\n</code></pre>"},{"location":"api/enum/#atlas.enums.StorageType.PUMPED_HYDRAULIC_STORAGE","title":"PUMPED_HYDRAULIC_STORAGE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PUMPED_HYDRAULIC_STORAGE = 'PumpedHydraulicStorage'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ReservesTypes","title":"atlas.enums.ReservesTypes","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Types of reserves used in grid balancing.</p> <p>Attributes:</p> Name Type Description <code>FrBM</code> <p>Frequency Balancing Mechanism.</p> <code>mFRR</code> <p>Manual Frequency Restoration Reserve.</p> <code>aFRR</code> <p>Automatic Frequency Restoration Reserve.</p>"},{"location":"api/enum/#atlas.enums.ReservesTypes.FrBM","title":"FrBM  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FrBM = 'FrBM'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ReservesTypes.aFRR","title":"aFRR  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>aFRR = 'aFRR'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ReservesTypes.mFRR","title":"mFRR  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>mFRR = 'mFRR'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ComplementDirection","title":"atlas.enums.ComplementDirection","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Direction used in complementarity constraints.</p> <p>Attributes:</p> Name Type Description <code>EqualTo</code> <p>Equality constraint.</p> <code>GreaterThan</code> <p>Greater-than constraint.</p> <code>LesserThan</code> <p>Less-than constraint.</p>"},{"location":"api/enum/#atlas.enums.ComplementDirection.EqualTo","title":"EqualTo  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>EqualTo = 'EqualTo'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ComplementDirection.GreaterThan","title":"GreaterThan  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>GreaterThan = 'GreaterThan'\n</code></pre>"},{"location":"api/enum/#atlas.enums.ComplementDirection.LesserThan","title":"LesserThan  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LesserThan = 'LesserThan'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType","title":"atlas.enums.CouplingType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Type of coupling constraints between energy assets.</p> <p>Attributes:</p> Name Type Description <code>EXCLUSION</code> <p>Assets cannot be active together.</p> <code>COMPLEMENT</code> <p>Assets complement each other.</p> <code>IDENTICAL_VOLUME</code> <p>Assets must have equal energy volumes.</p> <code>PARENT_CHILDREN</code> <p>Hierarchical relationship.</p> <code>IDENTICAL_RATIO</code> <p>Coupling via fixed ratio.</p>"},{"location":"api/enum/#atlas.enums.CouplingType.ATC","title":"ATC  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ATC = 'ATC'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType.COMPLEMENT","title":"COMPLEMENT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>COMPLEMENT = 'COMPLEMENT'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType.EXCLUSION","title":"EXCLUSION  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>EXCLUSION = 'EXCLUSION'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType.IDENTICAL_RATIO","title":"IDENTICAL_RATIO  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>IDENTICAL_RATIO = 'IDENTICAL_RATIO'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType.IDENTICAL_VOLUME","title":"IDENTICAL_VOLUME  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>IDENTICAL_VOLUME = 'IDENTICAL_VOLUME'\n</code></pre>"},{"location":"api/enum/#atlas.enums.CouplingType.PARENT_CHILDREN","title":"PARENT_CHILDREN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>PARENT_CHILDREN = 'PARENT_CHILDREN'\n</code></pre>"},{"location":"api/enum/#atlas.enums.OrderType","title":"atlas.enums.OrderType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Market order type.</p> <p>Attributes:</p> Name Type Description <code>Buy</code> <p>Purchase order.</p> <code>Sell</code> <p>Sale order.</p>"},{"location":"api/enum/#atlas.enums.OrderType.Buy","title":"Buy  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>Buy = 'Buy'\n</code></pre>"},{"location":"api/enum/#atlas.enums.OrderType.Sell","title":"Sell  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>Sell = 'Sell'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product","title":"atlas.enums.Product","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Enumeration of electricity market products.</p> <p>Attributes:</p> Name Type Description <code>Intraday</code> <p>Intraday market product.</p> <code>DayAhead</code> <p>Day-ahead market product.</p> <code>AFRRUpProcurement</code> <p>Upward aFRR reserve procurement.</p> <code>AFRRDownProcurement</code> <p>Downward aFRR reserve procurement.</p> <code>MFRRUpProcurement</code> <p>Upward mFRR reserve procurement.</p> <code>MFRRDownProcurement</code> <p>Downward mFRR reserve procurement.</p> <code>RRUpProcurement</code> <p>Upward replacement reserve procurement.</p> <code>RRDownProcurement</code> <p>Downward replacement reserve procurement.</p> <code>AFRRActivation</code> <p>Activation of aFRR reserves.</p> <code>MFRRActivation</code> <p>Activation of mFRR reserves.</p> <code>RRActivation</code> <p>Activation of replacement reserves.</p> <code>FCRActivation</code> <p>Activation of frequency containment reserves.</p> <code>FCRUpProcurement</code> <p>Upward FCR reserve procurement.</p> <code>FCRDownProcurement</code> <p>Downward FCR reserve procurement.</p>"},{"location":"api/enum/#atlas.enums.Product.AFRRActivation","title":"AFRRActivation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AFRRActivation = 'AFRRActivation'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.AFRRDownProcurement","title":"AFRRDownProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AFRRDownProcurement = 'AFRRDownProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.AFRRUpProcurement","title":"AFRRUpProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AFRRUpProcurement = 'AFRRUpProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.DayAhead","title":"DayAhead  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>DayAhead = 'DayAhead'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.FCRActivation","title":"FCRActivation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FCRActivation = 'FCRActivation'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.FCRDownProcurement","title":"FCRDownProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FCRDownProcurement = 'FCRDownProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.FCRUpProcurement","title":"FCRUpProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FCRUpProcurement = 'FCRUpProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.Intraday","title":"Intraday  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>Intraday = 'Intraday'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.MFRRActivation","title":"MFRRActivation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MFRRActivation = 'MFRRActivation'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.MFRRDownProcurement","title":"MFRRDownProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MFRRDownProcurement = 'MFRRDownProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.MFRRUpProcurement","title":"MFRRUpProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MFRRUpProcurement = 'MFRRUpProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.RRActivation","title":"RRActivation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RRActivation = 'RRActivation'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.RRDownProcurement","title":"RRDownProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RRDownProcurement = 'RRDownProcurement'\n</code></pre>"},{"location":"api/enum/#atlas.enums.Product.RRUpProcurement","title":"RRUpProcurement  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RRUpProcurement = 'RRUpProcurement'\n</code></pre>"},{"location":"api/logging/","title":"Logging","text":"<p>This logger utility provides a project-wide logging solution using Loguru, with configuration driven by environment variables and an optional <code>.env</code> file.</p>"},{"location":"api/logging/#features","title":"Features","text":"<ul> <li>Log level and output customizable via environment variables</li> <li>Optional file logging with rotation and retention policies</li> <li>Pretty terminal output with function and line number context</li> <li>Easy integration in any Python script or module</li> <li><code>.env</code> support using <code>python-dotenv</code></li> </ul>"},{"location":"api/logging/#how-to-use","title":"How to use ?","text":"<p>Very simply, import the <code>logger</code> from <code>atlas</code> package</p> <pre><code>from atlas.config import logger\n\nlogger.info(\"Logger is working!\")\nlogger.debug(\"This is a debug message.\")\n</code></pre>"},{"location":"api/logging/#configuration","title":"Configuration","text":""},{"location":"api/logging/#environment-file-or-variables","title":"Environment file or variables","text":"<p>It is completely optional !</p> <p>You can either :</p> <ul> <li>Define environment variables to_fileing them directly.</li> <li>Define environment variables in a <code>.env</code> file at the root of your project:</li> </ul> <pre><code>LOG_LEVEL=DEBUG\nLOG_TO_FILE=true\nLOG_DIR=logs\nLOG_ROTATION=10 MB\nLOG_RETENTION=7 days\nLOG_NAME=atlas\nLOG_FORMAT=\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger","title":"atlas.logging.Logger","text":"<pre><code>Logger(**kwargs)\n</code></pre> <p>               Bases: <code>BaseSettings</code></p> <p>A configurable logger using Loguru, suitable for project-wide use.</p> <p>Logging configuration can be controlled via environment variables with LOG_ prefix:</p> <ul> <li>LOG_LEVEL: logging level (e.g., DEBUG, INFO, WARNING)</li> <li>LOG_TO_FILE: enable file logging (true/false)</li> <li>LOG_DIR: directory to store logs</li> <li>LOG_ROTATION: log rotation policy (e.g., \"10 MB\", \"1 week\")</li> <li>LOG_RETENTION: log retention policy (e.g., \"7 days\", \"1 month\")</li> <li>LOG_FORMAT: log message format string</li> <li>LOG_NAME: name of the logger (default: \"atlas\")</li> </ul> Source code in <code>atlas/logging.py</code> <pre><code>def __init__(self, **kwargs):\n    super().__init__(**kwargs)\n    # Normalize level to uppercase for consistency with loguru\n    self.level = self.level.upper()\n    self._configure_logger()\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.dir","title":"dir  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>dir: Path = Field(\n    default=Path(\"logs\"),\n    description=\"Directory to store log files\",\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.format","title":"format  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>format: str = Field(\n    default=\"&lt;green&gt;{time:HH:mm:ss}&lt;/green&gt; | &lt;level&gt;{level}&lt;/level&gt; | &lt;level&gt;{message}&lt;/level&gt;\",\n    description=\"Log message format string\",\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.level","title":"level  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>level: str = upper()\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.model_config","title":"model_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model_config = SettingsConfigDict(\n    env_prefix=\"LOG_\",\n    env_file=\".env\",\n    env_file_encoding=\"utf-8\",\n    case_sensitive=False,\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.name","title":"name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>name: str = Field(\n    default=\"atlas\", description=\"Name of the logger\"\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.retention","title":"retention  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>retention: str = Field(\n    default=\"2 days\", description=\"Log retention policy\"\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.rotation","title":"rotation  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>rotation: str = Field(\n    default=\"10 MB\", description=\"Log rotation policy\"\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.to_file","title":"to_file  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>to_file: bool = Field(\n    default=False, description=\"Enable file logging\"\n)\n</code></pre>"},{"location":"api/logging/#atlas.logging.Logger.get_logger","title":"get_logger","text":"<pre><code>get_logger()\n</code></pre> <p>Returns a bound logger instance, pre-configured.</p> <p>Returns:</p> Type Description <code>loguru.logger</code> <p>A Loguru logger instance.</p> Source code in <code>atlas/logging.py</code> <pre><code>def get_logger(self):  # noqa: ANN201\n    \"\"\"\n    Returns a bound logger instance, pre-configured.\n\n    :return: A Loguru logger instance.\n    :rtype: loguru.logger\n    \"\"\"\n    return logger.bind(logger_name=self.name)\n</code></pre>"},{"location":"api/io/atlas_dataset/","title":"Input Loader","text":""},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset","title":"atlas.AtlasDataset","text":"<p>               Bases: <code>BaseModel</code></p> <p>A Pydantic-based container for ATLAS BusinessModel objects with efficient lookup and I/O operations.</p> <p>This class provides:</p> <ul> <li>Type-safe attribute access to different BusinessModel types</li> <li>Serialization/deserialization via from_directory / to_directory, from_dict / to_dict methods, and pickle support</li> </ul> <p>Example:</p> <pre><code>&gt;&gt;&gt; dataset = AtlasDataset.from_directory(\"data/atlas-dataset\")\n&gt;&gt;&gt; # Attribute access\n&gt;&gt;&gt; nodes = dataset.node\n&gt;&gt;&gt; # Efficient lookup by name\n&gt;&gt;&gt; thermal_plant = dataset.get(\"thermal\", \"my_plant\")\n&gt;&gt;&gt; # Export to directory\n&gt;&gt;&gt; dataset.to_directory(\"output/atlas-dataset\")\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.control_block","title":"control_block  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>control_block: Container[ControlBlock] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.critical_branch","title":"critical_branch  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>critical_branch: Container[CriticalBranch] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.hydro","title":"hydro  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hydro: Container[Hydro] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.load","title":"load  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>load: Container[Load] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.market_area","title":"market_area  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>market_area: Container[MarketArea] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.market_area_ptdf","title":"market_area_ptdf  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>market_area_ptdf: Container[MarketAreaPtdf] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.market_border","title":"market_border  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>market_border: Container[MarketBorder] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.model_config","title":"model_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model_config = ConfigDict(\n    arbitrary_types_allowed=True, validate_assignment=True\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.node","title":"node  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>node: Container[Node] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.node_ptdf","title":"node_ptdf  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>node_ptdf: Container[NodePtdf] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.order","title":"order  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>order: Container[Order] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.order_coupling","title":"order_coupling  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>order_coupling: Container[OrderCoupling] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.other_non_dispatchable","title":"other_non_dispatchable  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>other_non_dispatchable: Container[OtherNonDispatchable] = (\n    Field(default_factory=lambda: Container())\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.portfolio","title":"portfolio  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>portfolio: Container[Portfolio] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.solar","title":"solar  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>solar: Container[Solar] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.storage","title":"storage  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>storage: Container[Storage] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.thermal","title":"thermal  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>thermal: Container[Thermal] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.wind","title":"wind  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>wind: Container[Wind] = Field(\n    default_factory=lambda: Container()\n)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__contains__","title":"__contains__","text":"<pre><code>__contains__(item: str | BusinessModel) -&gt; bool\n</code></pre> <p>Check if the dataset contains an object with the given name or instance.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>str | BusinessModel</code> <p>The object name (str) or BusinessModel instance to search for</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if an object with this name or instance exists in the dataset</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __contains__(self, item: str | BusinessModel) -&gt; bool:\n    \"\"\"\n    Check if the dataset contains an object with the given name or instance.\n\n    :param item: The object name (str) or BusinessModel instance to search for\n    :type item: str | BusinessModel\n    :return: True if an object with this name or instance exists in the dataset\n    :rtype: bool\n    \"\"\"\n    if isinstance(item, str):\n        for type_index in self._indices.values():\n            if item in type_index:\n                return True\n        return False\n    elif isinstance(item, BusinessModel):\n        for type_index in self._indices.values():\n            if item.name in type_index and type_index[item.name] is item:\n                return True\n        return False\n    else:\n        return False\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__getstate__","title":"__getstate__","text":"<pre><code>__getstate__() -&gt; dict[str, Any]\n</code></pre> <p>Prepare the object for pickling.</p> <p>Returns the model's state, excluding the _indices cache which will be rebuilt on unpickling.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary containing the object's state</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __getstate__(self) -&gt; dict[str, Any]:\n    \"\"\"\n    Prepare the object for pickling.\n\n    Returns the model's state, excluding the _indices cache which will be rebuilt on unpickling.\n\n    :return: Dictionary containing the object's state\n    :rtype: dict[str, Any]\n    \"\"\"\n    # Get the default state from Pydantic\n    state = self.__dict__.copy()\n    # Remove the _indices cache as it will be rebuilt by _build_indices validator\n    state.pop(\"_indices\", None)\n    return state\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__iter__","title":"__iter__","text":"<pre><code>__iter__()\n</code></pre> <p>Iterate over all objects in the dataset across all types.</p> <p>Yields objects in the order defined by MODEL_MAPPING_NAME configuration.</p> <p>:yield: All BusinessModel objects in the dataset</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __iter__(self):\n    \"\"\"\n    Iterate over all objects in the dataset across all types.\n\n    Yields objects in the order defined by MODEL_MAPPING_NAME configuration.\n\n    :yield: All BusinessModel objects in the dataset\n    \"\"\"\n    for object_type in cfg.MODEL_MAPPING_NAME.keys():\n        objects = getattr(self, object_type, [])\n        yield from objects\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Get the total number of objects across all types.</p> <p>Returns:</p> Type Description <code>int</code> <p>Total count of all BusinessModel objects</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Get the total number of objects across all types.\n\n    :return: Total count of all BusinessModel objects\n    :rtype: int\n    \"\"\"\n    total = 0\n    for object_type in cfg.MODEL_MAPPING_NAME.keys():\n        total += len(getattr(self, object_type, []))\n    return total\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>String representation showing counts of each object type.</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"String representation showing counts of each object type.\"\"\"\n    type_counts = []\n    for object_type in cfg.MODEL_MAPPING_NAME.keys():\n        objects = getattr(self, object_type, [])\n        if objects:\n            type_counts.append(f\"{object_type}={len(objects)}\")\n\n    return f\"AtlasDataset({', '.join(type_counts)})\"\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__setstate__","title":"__setstate__","text":"<pre><code>__setstate__(state: dict[str, Any]) -&gt; None\n</code></pre> <p>Restore the object from pickled state.</p> <p>Reconstructs the object and rebuilds the _indices cache.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>dict[str, Any]</code> <p>Dictionary containing the object's pickled state</p> required Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __setstate__(self, state: dict[str, Any]) -&gt; None:\n    \"\"\"\n    Restore the object from pickled state.\n\n    Reconstructs the object and rebuilds the _indices cache.\n\n    :param state: Dictionary containing the object's pickled state\n    :type state: dict[str, Any]\n    \"\"\"\n    # Restore the state\n    self.__dict__.update(state)\n    # Rebuild indices (the validator will be called automatically by Pydantic)\n    self._build_indices()\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.__str__","title":"__str__","text":"<pre><code>__str__() -&gt; str\n</code></pre> <p>User-friendly string representation.</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def __str__(self) -&gt; str:\n    \"\"\"User-friendly string representation.\"\"\"\n    return self.__repr__()\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(\n    data: dict[str, list[BusinessModel]],\n) -&gt; AtlasDataset\n</code></pre> <p>Create an AtlasDataset from a dictionary of BusinessModel objects.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, list[BusinessModel]]</code> <p>Dictionary mapping object type names to lists of BusinessModel objects.</p> required <p>Returns:</p> Type Description <code>AtlasDataset</code> <p>An AtlasDataset instance</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>@classmethod\ndef from_dict(cls, data: dict[str, list[BusinessModel]]) -&gt; AtlasDataset:\n    \"\"\"\n    Create an AtlasDataset from a dictionary of BusinessModel objects.\n\n    :param data: Dictionary mapping object type names to lists of BusinessModel objects.\n    :type data: dict[str, list[BusinessModel]]\n    :return: An AtlasDataset instance\n    :rtype: AtlasDataset\n    \"\"\"\n    return cls(**data)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.from_directory","title":"from_directory  <code>classmethod</code>","text":"<pre><code>from_directory(\n    directory_path: Path | str,\n    separator: str = \";\",\n    timeseries_file_extension: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"parquet\",\n    matrix_file_extension: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"parquet\",\n    lazy: bool = False,\n    timezone: str = \"UTC\",\n    date_format_forecasting_matrix: str = \"YYYY-MM-DD HH:mm:ss\",\n    date_format_input_files: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; AtlasDataset\n</code></pre> <p>Load an AtlasDataset from a directory structure.</p> <p>The input directory must follow a specific structure for successful parsing:</p> <pre><code>&lt;root_input_directory&gt;/\n\u251c\u2500\u2500 objects/\n\u2502   \u251c\u2500\u2500 hydro.csv\n\u2502   \u251c\u2500\u2500 wind.csv\n\u2502   \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 timeseries/\n\u2502   \u2514\u2500\u2500 hydro/\n\u2502       \u251c\u2500\u2500 fr_hydro.parquet\n\u2502       \u2514\u2500\u2500 ...\n\u251c\u2500\u2500 scenario_matrix/\n\u2502   \u2514\u2500\u2500 hydro/\n\u2502       \u251c\u2500\u2500 fr_hydro.parquet\n\u2502       \u2514\u2500\u2500 ...\n\u2514\u2500\u2500 forecasting_matrix/\n    \u2514\u2500\u2500 hydro/\n        \u251c\u2500\u2500 fr_hydro.parquet\n        \u2514\u2500\u2500 ...\n</code></pre> <ul> <li>The <code>objects/</code> directory contains CSV files, each named after an object type (e.g., <code>storage.csv</code>),   describing the business objects and their attributes. Each line in the CSV represents an object.</li> <li>The <code>timeseries/</code>, <code>scenario_matrix/</code>, and <code>forecasting_matrix/</code> directories contain subdirectories   for each object type, with files named after the object (e.g., <code>fr_storage.parquet</code>).</li> <li>Each matrix or timeseries file contains a column 'attribute' which is categorical and contains the timeseries or matrix name.   In a way that if a filter is applied on this column, the dataframe retrieved is the timeseries, or the matrix of the filter applied.</li> <li>Each timeseries or matrix file must match the expected file extension (default: <code>.parquet</code>).</li> <li>Attribute names in the objects CSV must be either the value itself of the attribute, or the type if a math objects (e.g timeseries,   forecasting_matrix, scenario_matrix)</li> </ul> <p>Parameters:</p> Name Type Description Default <code>directory_path</code> <code>Path | str</code> <p>The root path to the directory containing input data.</p> required <code>separator</code> <code>str</code> <p>The separator used in CSV files (default: \";\").</p> <code>';'</code> <code>timeseries_file_extension</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>File extension for timeseries files (default: \"parquet\").</p> <code>'parquet'</code> <code>matrix_file_extension</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>File extension for matrix files (default: \"parquet\").</p> <code>'parquet'</code> <code>lazy</code> <code>bool</code> <p>Whether to use lazy loading for timeseries and matrices (default: False).</p> <code>False</code> <code>timezone</code> <code>str</code> <p>Timezone for date parsing and object instantiation (default: \"UTC\").</p> <code>'UTC'</code> <code>date_format_forecasting_matrix</code> <code>str</code> <p>Date format used for forecasting matrix timestamps.</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>date_format_input_files</code> <code>str</code> <p>Date format used in object CSV data.</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <p>Returns:</p> Type Description <code>AtlasDataset</code> <p>An AtlasDataset instance containing all loaded BusinessModel objects.</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>@classmethod\ndef from_directory(\n    cls,\n    directory_path: Path | str,\n    separator: str = \";\",\n    timeseries_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"] = \"parquet\",\n    matrix_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"] = \"parquet\",\n    lazy: bool = False,\n    timezone: str = \"UTC\",\n    date_format_forecasting_matrix: str = \"YYYY-MM-DD HH:mm:ss\",\n    date_format_input_files: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; AtlasDataset:\n    \"\"\"\n    Load an AtlasDataset from a directory structure.\n\n    The input directory must follow a specific structure for successful parsing:\n\n        &lt;root_input_directory&gt;/\n        \u251c\u2500\u2500 objects/\n        \u2502   \u251c\u2500\u2500 hydro.csv\n        \u2502   \u251c\u2500\u2500 wind.csv\n        \u2502   \u2514\u2500\u2500 ...\n        \u251c\u2500\u2500 timeseries/\n        \u2502   \u2514\u2500\u2500 hydro/\n        \u2502       \u251c\u2500\u2500 fr_hydro.parquet\n        \u2502       \u2514\u2500\u2500 ...\n        \u251c\u2500\u2500 scenario_matrix/\n        \u2502   \u2514\u2500\u2500 hydro/\n        \u2502       \u251c\u2500\u2500 fr_hydro.parquet\n        \u2502       \u2514\u2500\u2500 ...\n        \u2514\u2500\u2500 forecasting_matrix/\n            \u2514\u2500\u2500 hydro/\n                \u251c\u2500\u2500 fr_hydro.parquet\n                \u2514\u2500\u2500 ...\n\n    - The `objects/` directory contains CSV files, each named after an object type (e.g., `storage.csv`),\n      describing the business objects and their attributes. Each line in the CSV represents an object.\n    - The `timeseries/`, `scenario_matrix/`, and `forecasting_matrix/` directories contain subdirectories\n      for each object type, with files named after the object (e.g., `fr_storage.parquet`).\n    - Each matrix or timeseries file contains a column 'attribute' which is categorical and contains the timeseries or matrix name.\n      In a way that if a filter is applied on this column, the dataframe retrieved is the timeseries, or the matrix of the filter applied.\n    - Each timeseries or matrix file must match the expected file extension (default: `.parquet`).\n    - Attribute names in the objects CSV must be either the value itself of the attribute, or the type if a math objects (e.g timeseries,\n      forecasting_matrix, scenario_matrix)\n\n    :param directory_path: The root path to the directory containing input data.\n    :type directory_path: str or pathlib.Path\n    :param separator: The separator used in CSV files (default: \";\").\n    :type separator: str\n    :param timeseries_file_extension: File extension for timeseries files (default: \"parquet\").\n    :type timeseries_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"]\n    :param matrix_file_extension: File extension for matrix files (default: \"parquet\").\n    :type matrix_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"]\n    :param lazy: Whether to use lazy loading for timeseries and matrices (default: False).\n    :type lazy: bool\n    :param timezone: Timezone for date parsing and object instantiation (default: \"UTC\").\n    :type timezone: str\n    :param date_format_forecasting_matrix: Date format used for forecasting matrix timestamps.\n    :type date_format_forecasting_matrix: str\n    :param date_format_input_files: Date format used in object CSV data.\n    :type date_format_input_files: str\n\n    :return: An AtlasDataset instance containing all loaded BusinessModel objects.\n    :rtype: AtlasDataset\n\n    \"\"\"\n    if isinstance(directory_path, str):\n        directory_path = Path(directory_path)\n\n    raw_data = load_from_directory(\n        directory_path=directory_path,\n        separator=separator,\n        timeseries_file_extension=timeseries_file_extension,\n        matrix_file_extension=matrix_file_extension,\n        lazy=lazy,\n        timezone=timezone,\n        date_format_forecasting_matrix=date_format_forecasting_matrix,\n        date_format_input_files=date_format_input_files,\n    )\n\n    return cls.from_dict(raw_data)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.from_pickle","title":"from_pickle  <code>classmethod</code>","text":"<pre><code>from_pickle(file_path: Path | str) -&gt; AtlasDataset\n</code></pre> <p>Load an AtlasDataset from a pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path | str</code> <p>Path to the pickle file to read</p> required <p>Returns:</p> Type Description <code>AtlasDataset</code> <p>An AtlasDataset instance</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>@classmethod\ndef from_pickle(cls, file_path: Path | str) -&gt; AtlasDataset:\n    \"\"\"\n    Load an AtlasDataset from a pickle file.\n\n    :param file_path: Path to the pickle file to read\n    :type file_path: str or pathlib.Path\n    :return: An AtlasDataset instance\n    :rtype: AtlasDataset\n    \"\"\"\n    if isinstance(file_path, str):\n        file_path = Path(file_path)\n\n    with open(file_path, \"rb\") as f:\n        return pickle.load(f)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.get","title":"get","text":"<pre><code>get(object_type: str, name: str) -&gt; BusinessModel | None\n</code></pre> <p>Get a BusinessModel object by type and name with O(1) lookup.</p> <p>Parameters:</p> Name Type Description Default <code>object_type</code> <code>str</code> <p>The type of object (e.g., \"hydro\", \"node\")</p> required <code>name</code> <code>str</code> <p>The name of the object to retrieve</p> required <p>Returns:</p> Type Description <code>BusinessModel | None</code> <p>The BusinessModel object if found, None otherwise</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def get(self, object_type: str, name: str) -&gt; BusinessModel | None:\n    \"\"\"\n    Get a BusinessModel object by type and name with O(1) lookup.\n\n    :param object_type: The type of object (e.g., \"hydro\", \"node\")\n    :type object_type: str\n    :param name: The name of the object to retrieve\n    :type name: str\n    :return: The BusinessModel object if found, None otherwise\n    :rtype: BusinessModel | None\n    \"\"\"\n    if object_type not in self._indices:\n        return None\n    return self._indices[object_type].get(name)\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.get_container_by_type","title":"get_container_by_type","text":"<pre><code>get_container_by_type(\n    object_type: str | type[BusinessModel],\n) -&gt; Container\n</code></pre> <p>Get a Container object by type with O(1) lookup.</p> <p>Parameters:</p> Name Type Description Default <code>object_type</code> <code>str | type[BusinessModel]</code> <p>The type of object (e.g., \"hydro\", \"node\")</p> required <p>Returns:</p> Type Description <code>Container</code> <p>The Container object if found, raise an error otherwise</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def get_container_by_type(self, object_type: str | type[BusinessModel]) -&gt; Container:\n    \"\"\"\n    Get a Container object by type with O(1) lookup.\n\n    :param object_type: The type of object (e.g., \"hydro\", \"node\")\n    :type object_type: str | type[BusinessModel]\n    :return: The Container object if found, raise an error otherwise\n    :rtype: Container\n    \"\"\"\n    if isinstance(object_type, type) and issubclass(object_type, BusinessModel):\n        object_type_str = cfg.INVERSE_MODEL_MAPPING_NAME[object_type]\n    elif isinstance(object_type, str):\n        object_type_str = BusinessModelName(object_type)\n    else:\n        raise TypeError(f\"Invalid type for object_type: {object_type!r}\")\n    container = getattr(self, object_type_str, None)\n    if container is None:\n        raise ValueError(f\"No container found for type {object_type_str}\")\n    return container\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.get_items_by_type","title":"get_items_by_type","text":"<pre><code>get_items_by_type(\n    object_type: str | type[BusinessModel],\n) -&gt; list[BusinessModel]\n</code></pre> <p>Get a Container object by type with O(1) lookup.</p> <p>Parameters:</p> Name Type Description Default <code>object_type</code> <code>str | type[BusinessModel]</code> <p>The type of object (e.g., \"hydro\", \"node\")</p> required <p>Returns:</p> Type Description <code>Container</code> <p>The Container object if found, raise an error otherwise</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def get_items_by_type(self, object_type: str | type[BusinessModel]) -&gt; list[BusinessModel]:\n    \"\"\"\n    Get a Container object by type with O(1) lookup.\n\n    :param object_type: The type of object (e.g., \"hydro\", \"node\")\n    :type object_type: str | type[BusinessModel]\n    :return: The Container object if found, raise an error otherwise\n    :rtype: Container\n    \"\"\"\n    container = self.get_container_by_type(object_type)\n    return container.all()\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.iter_by_types","title":"iter_by_types","text":"<pre><code>iter_by_types(*object_types: str)\n</code></pre> <p>Iterator over objects of one or more specific types.</p> <p>:yield: BusinessModel objects of the specified types</p> <p>Parameters:</p> Name Type Description Default <code>object_types</code> <code>str</code> <p>One or more object type names (e.g., \"hydro\", \"node\")</p> <code>()</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any object_type is not valid</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def iter_by_types(self, *object_types: str):\n    \"\"\"\n    Iterator over objects of one or more specific types.\n\n    :param object_types: One or more object type names (e.g., \"hydro\", \"node\")\n    :type object_types: str\n    :yield: BusinessModel objects of the specified types\n    :raises ValueError: If any object_type is not valid\n    \"\"\"\n    # Validate all object types first\n    for object_type in object_types:\n        if object_type not in cfg.MODEL_MAPPING_NAME:\n            raise ValueError(\n                f\"Invalid object type '{object_type}'. Valid types: {list(cfg.MODEL_MAPPING_NAME.keys())}\"\n            )\n\n    # Yield objects from each type\n    for object_type in object_types:\n        objects = getattr(self, object_type, [])\n        yield from objects\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, list[BusinessModel]]\n</code></pre> <p>Convert the AtlasDataset to a dictionary representation.</p> <p>Returns:</p> Type Description <code>dict[str, list[BusinessModel]]</code> <p>Dictionary mapping object type names to lists of BusinessModel objects.</p> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def to_dict(self) -&gt; dict[str, list[BusinessModel]]:\n    \"\"\"\n    Convert the AtlasDataset to a dictionary representation.\n\n    :return: Dictionary mapping object type names to lists of BusinessModel objects.\n    :rtype: dict[str, list[BusinessModel]]\n    \"\"\"\n    result: dict[str, list[BusinessModel]] = {}\n\n    for object_type in cfg.MODEL_MAPPING_NAME.keys():\n        objects = getattr(self, object_type, [])\n        if objects:  # Only include non-empty lists\n            result[object_type] = list(objects)\n\n    return result\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.to_directory","title":"to_directory","text":"<pre><code>to_directory(\n    directory_path: Path | str,\n    separator: str = \";\",\n    timeseries_file_extension: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"parquet\",\n    matrix_file_extension: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"parquet\",\n) -&gt; None\n</code></pre> <p>Write the AtlasDataset to a directory structure.</p> <p>This method generates data files (CSV, Parquet) in a structured directory and exports all BusinessModel objects along with their mathematical objects.</p> <p>Parameters:</p> Name Type Description Default <code>directory_path</code> <code>Path | str</code> <p>The root path to the directory to write data.</p> required <code>separator</code> <code>str</code> <p>The separator used in CSV files (default: \";\").</p> <code>';'</code> <code>timeseries_file_extension</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>File extension for timeseries files (default: \"parquet\").</p> <code>'parquet'</code> <code>matrix_file_extension</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>File extension for matrix files (default: \"parquet\").</p> <code>'parquet'</code> Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def to_directory(\n    self,\n    directory_path: Path | str,\n    separator: str = \";\",\n    timeseries_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"] = \"parquet\",\n    matrix_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"] = \"parquet\",\n) -&gt; None:\n    \"\"\"\n    Write the AtlasDataset to a directory structure.\n\n    This method generates data files (CSV, Parquet) in a structured directory and\n    exports all BusinessModel objects along with their mathematical objects.\n\n    :param directory_path: The root path to the directory to write data.\n    :type directory_path: str or pathlib.Path\n    :param separator: The separator used in CSV files (default: \";\").\n    :type separator: str\n    :param timeseries_file_extension: File extension for timeseries files (default: \"parquet\").\n    :type timeseries_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"]\n    :param matrix_file_extension: File extension for matrix files (default: \"parquet\").\n    :type matrix_file_extension: Literal[\"csv\", \"parquet\", \"pickle\"]\n\n    \"\"\"\n    if isinstance(directory_path, str):\n        directory_path = Path(directory_path)\n\n    dataset_dict = self.to_dict()\n\n    save_to_directory(\n        dataset=dataset_dict,\n        directory_path=directory_path,\n        separator=separator,\n        timeseries_file_extension=timeseries_file_extension,\n        matrix_file_extension=matrix_file_extension,\n    )\n</code></pre>"},{"location":"api/io/atlas_dataset/#atlas.AtlasDataset.to_pickle","title":"to_pickle","text":"<pre><code>to_pickle(file_path: Path | str) -&gt; None\n</code></pre> <p>Serialize the AtlasDataset to a pickle file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path | str</code> <p>Path to the pickle file to write</p> required Source code in <code>atlas/io_utils/atlas_dataset.py</code> <pre><code>def to_pickle(self, file_path: Path | str) -&gt; None:\n    \"\"\"\n    Serialize the AtlasDataset to a pickle file.\n\n    :param file_path: Path to the pickle file to write\n    :type file_path: str or pathlib.Path\n    \"\"\"\n    if isinstance(file_path, str):\n        file_path = Path(file_path)\n\n    with open(file_path, \"wb\") as f:\n        pickle.dump(self, f)\n</code></pre>"},{"location":"api/io/parameters/","title":"Parameters","text":""},{"location":"api/io/parameters/#atlas.Parameters","title":"atlas.Parameters","text":"<p>               Bases: <code>BaseModel</code></p> <p>A class to represent parameters.</p>"},{"location":"api/io/parameters/#atlas.ParametersParser","title":"atlas.ParametersParser","text":"<p>A class to parse parameters from a YAML or JSON file.</p>"},{"location":"api/io/parameters/#atlas.ParametersParser.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | Path) -&gt; Parameters\n</code></pre> <p>Load parameters from a YAML or JSON file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the parameters file.</p> required <p>Returns:</p> Type Description <code>Parameters</code> <p>A Parameters object containing the parsed and validated parameters.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the file extension is not supported.</p> Source code in <code>atlas/io_utils/parameters.py</code> <pre><code>@classmethod\ndef from_file(cls, file_path: str | Path) -&gt; Parameters:\n    \"\"\"Load parameters from a YAML or JSON file.\n    :param file_path: Path to the parameters file.\n    :type file_path: str or pathlib.Path\n    :return: A Parameters object containing the parsed and validated parameters.\n    :rtype: Parameters\n    :raises ValueError: If the file extension is not supported.\n    \"\"\"\n    file_extension = Path(file_path).suffix\n\n    if file_extension in (\".yaml\", \".yml\"):\n        parameters = cls._parse_yaml(file_path)\n    elif file_extension == \".json\":\n        parameters = cls._parse_json(file_path)\n    else:\n        raise ValueError(f\"Unsupported file extension: {file_extension}\")\n\n    return Parameters(**parameters)\n</code></pre>"},{"location":"api/math/forecasting_matrix/","title":"Forecasting ScenarioMatrix","text":""},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix","title":"atlas.ForecastingMatrix","text":"<pre><code>ForecastingMatrix(\n    matrix: DataFrame\n    | DataFrame\n    | ScenarioMatrix\n    | None = None,\n    timezone: str = \"UTC\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n)\n</code></pre> <p>               Bases: <code>ScenarioMatrix</code></p> <p>A matrix structure for managing collections of forecast time series, indexed by forecast generation time.</p> <p>The ForecastingMatrix is designed to store and organize multiple time series forecasts, where each column (except for the \"time\" column) represents a forecast generated at a specific datetime.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>DataFrame | DataFrame | ScenarioMatrix | None</code> <p>A DataFrame where each column (except \"time\") represents a forecast.</p> <code>None</code> <code>timezone</code> <code>str</code> <p>Timezone of the timeseries data.</p> <code>'UTC'</code> <code>date_format</code> <code>str</code> <p>Format used for parsing and displaying datetime indexes.</p> <code>'YYYY-MM-DD HH:mm:ss'</code> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __init__(\n    self,\n    matrix: pl.DataFrame | pd.DataFrame | ScenarioMatrix | None = None,\n    timezone: str = \"UTC\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; None:\n    \"\"\"\n    :param matrix: A DataFrame where each column (except \"time\") represents a forecast.\n    :type matrix: pl.DataFrame | pd.DataFrame\n    :param timezone: Timezone of the timeseries data.\n    :type timezone: str\n    :param date_format: Format used for parsing and displaying datetime indexes.\n    :type date_format: str\n    \"\"\"\n    super().__init__(matrix, timezone=timezone)\n\n    self._date_format: str = date_format\n    self._sort_indexes()\n    self._parsed_indexes_cache: pl.DataFrame | None = None\n    self._frequency_cache: dict[str, pendulum.Duration] = {}\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: DataFrame\n</code></pre> <p>Returns the ScenarioMatrix DataFrame</p>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.date_format","title":"date_format  <code>property</code>","text":"<pre><code>date_format: str\n</code></pre> <p>Returns the matrix date format.</p>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.index","title":"index  <code>property</code>","text":"<pre><code>index: list[str]\n</code></pre> <p>Returns the ScenarioMatrix indexes (e.g columns names)</p>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict[str, Any]\n</code></pre> <p>Return the metadata of the timeseries.</p>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the ScenarioMatrix shape</p>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.__contains__","title":"__contains__","text":"<pre><code>__contains__(index: str | datetime | DateTime) -&gt; bool\n</code></pre> <p>Check if an index exists in the forecasting matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime | DateTime</code> <p>Forecast generation datetime (as string or datetime object).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if index exists, False otherwise.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __contains__(self, index: str | datetime | pendulum.DateTime) -&gt; bool:\n    \"\"\"\n    Check if an index exists in the forecasting matrix.\n\n    :param index: Forecast generation datetime (as string or datetime object).\n    :type index: str | datetime | pendulum.DateTime\n    :return: True if index exists, False otherwise.\n    :rtype: bool\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n    return dt in self.indexes\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Check equality with another matrix.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>Another matrix instance.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if equal, False otherwise.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object to compare is not a ScenarioMatrix</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"\n    Check equality with another matrix.\n\n    :param other: Another matrix instance.\n    :type other: object\n    :raises TypeError: If the object to compare is not a ScenarioMatrix\n    :return: True if equal, False otherwise.\n    :rtype: bool\n    \"\"\"\n    if not isinstance(other, ScenarioMatrix):\n        raise TypeError(\"Cannot compare with non-ScenarioMatrix object\")\n\n    return self.matrix.equals(other.matrix)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: str | datetime | DateTime) -&gt; Timeseries\n</code></pre> <p>Retrieve a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime | DateTime</code> <p>Forecast generation datetime (as string or datetime object).</p> required <code>date_format</code> <code>str</code> <p>Date format if the index is a string.</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The corresponding Timeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the specified index is not found.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __getitem__(\n    self,\n    index: str | datetime | pendulum.DateTime,\n) -&gt; Timeseries:\n    \"\"\"\n    Retrieve a timeseries by index.\n\n    :param index: Forecast generation datetime (as string or datetime object).\n    :type index: str | datetime\n    :param date_format: Date format if the index is a string.\n    :type date_format: str\n    :raises KeyError: If the specified index is not found.\n    :return: The corresponding Timeseries object.\n    :rtype: Timeseries\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n\n    return super().__getitem__(dt)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of timeseries in the matrix.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of elements in the matrix.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Number of timeseries in the matrix.\n\n    :return: Number of elements in the matrix.\n    :rtype: int\n    \"\"\"\n    return len(self.indexes)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Provide a string representation of the ScenarioMatrix object.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __repr__(self):\n    \"\"\"Provide a string representation of the ScenarioMatrix object.\"\"\"\n    return f\"Forecasting ScenarioMatrix : {self.matrix}\"\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute absolute value of all numeric columns.</p> Source code in <code>atlas/math/abstract_scenario_matrix.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute absolute value of all numeric columns.\"\"\"\n    backend = self._get_data()\n    result = backend.select([\"time\"] + [pl.col(c).abs().alias(c) for c in self.index])\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.add","title":"add","text":"<pre><code>add(\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    index: str | datetime | DateTime,\n) -&gt; None\n</code></pre> <p>Add a Timeseries to the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str | datetime | DateTime</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def add(\n    self,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    index: str | datetime | pendulum.DateTime,\n) -&gt; None:\n    \"\"\"\n    Add a Timeseries to the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n\n    super().add(timeseries, dt)\n    self._sort_indexes()\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.delete","title":"delete","text":"<pre><code>delete(index: str | datetime | DateTime) -&gt; None\n</code></pre> <p>Delete a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime | DateTime</code> <p>Forecast generation datetime (as string or datetime object).</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index does not exist in the matrix.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def delete(self, index: str | datetime | pendulum.DateTime) -&gt; None:\n    \"\"\"\n    Delete a timeseries by index.\n\n    :param index: Forecast generation datetime (as string or datetime object).\n    :type index: str | datetime\n    :raises KeyError: If the index does not exist in the matrix.\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n\n    super().delete(dt)\n\n    self._sort_indexes()\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.describe","title":"describe","text":"<pre><code>describe() -&gt; dict[str, Any]\n</code></pre> <p>Get metadata about the ScenarioMatrix.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionnary containing matrix metadata</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def describe(self) -&gt; dict[str, Any]:\n    \"\"\"\n    Get metadata about the ScenarioMatrix.\n\n    :return: A dictionnary containing matrix metadata\n    :rtype: dict[str, Any]\n    \"\"\"\n    return get_metadata_from_frame(self.matrix)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; ForecastingMatrix\n</code></pre> <p>Load a ForecastingMatrix from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file (CSV or Parquet).</p> required <p>Returns:</p> Type Description <code>ForecastingMatrix</code> <p>A ForecastingMatrix object.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; ForecastingMatrix:\n    \"\"\"\n    Load a ForecastingMatrix from a file.\n\n    :param file_path: Path to the file (CSV or Parquet).\n    :type file_path: str | Path\n    :return: A ForecastingMatrix object.\n    :rtype: ForecastingMatrix\n    \"\"\"\n\n    return cls(read_data_file(file_path, filters, separator), timezone, date_format)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.get_forecast","title":"get_forecast","text":"<pre><code>get_forecast(\n    execution_date: datetime | str | DateTime,\n    start_date: datetime | str | DateTime,\n    end_date: datetime | str | DateTime,\n    timestep: str | timedelta | Duration | None = None,\n    default_value: float | None = None,\n) -&gt; Timeseries\n</code></pre> <p>Returns the most up-to-date forecast available per time row in the given window. Newer forecasts are prioritized. Gaps are filled from older forecasts.</p> <p>Parameters:</p> Name Type Description Default <code>execution_date</code> <code>datetime | str | DateTime</code> <p>The reference date for determining which forecasts are available. Only forecasts made on or before this date will be considered.</p> required <code>start_date</code> <code>datetime | str | DateTime</code> <p>Start date of the forecast window to retrieve.</p> required <code>end_date</code> <code>datetime | str | DateTime</code> <p>End date of the forecast window to retrieve.</p> required <code>timestep</code> <code>str | timedelta | Duration | None</code> <p>Target frequency for the output timeseries. If None, the lowest frequency found in the data will be used.</p> <code>None</code> <code>default_value</code> <code>float | None</code> <p>default value used for indexes where no value is found</p> <code>None</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>A timeseries containing the most recent forecast values for each timestamp in the specified window, with gaps filled using older forecasts.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If start_date is after end_date or if no forecasting dates are available before the execution date.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def get_forecast(\n    self,\n    execution_date: datetime | str | pendulum.DateTime,\n    start_date: datetime | str | pendulum.DateTime,\n    end_date: datetime | str | pendulum.DateTime,\n    timestep: str | timedelta | pendulum.Duration | None = None,\n    default_value: float | None = None,\n) -&gt; Timeseries:\n    \"\"\"\n    Returns the most up-to-date forecast available per time row in the given window.\n    Newer forecasts are prioritized. Gaps are filled from older forecasts.\n\n    :param execution_date: The reference date for determining which forecasts are available.\n                          Only forecasts made on or before this date will be considered.\n    :type execution_date: datetime | str | pendulum.DateTime\n    :param start_date: Start date of the forecast window to retrieve.\n    :type start_date: datetime | str | pendulum.DateTime\n    :param end_date: End date of the forecast window to retrieve.\n    :type end_date: datetime | str | pendulum.DateTime\n    :param timestep: Target frequency for the output timeseries. If None, the lowest\n                    frequency found in the data will be used.\n    :type timestep: str | pendulum.Duration | None\n    :param default_value: default value used for indexes where no value is found\n    :type default_value: float | None\n    :raises ValueError: If start_date is after end_date or if no forecasting dates\n                       are available before the execution date.\n    :return: A timeseries containing the most recent forecast values for each timestamp\n            in the specified window, with gaps filled using older forecasts.\n    :rtype: Timeseries\n    \"\"\"\n\n    execution_date = build_datetime(execution_date, self.date_format)\n    start_date = build_datetime(start_date, self.date_format)\n    end_date = build_datetime(end_date, self.date_format)\n\n    if start_date &gt; end_date:\n        raise ValueError(\"Start date must be before end date\")\n\n    forecast_cols = (\n        self._get_parsed_indexes()\n        .filter(pl.col(\"indexes_dt\") &lt;= execution_date)\n        .sort(\"indexes_dt\", descending=True)\n        .select(\"indexes_str\")\n        .to_series()\n        .to_list()\n    )\n\n    if not forecast_cols:\n        raise ValueError(\"No forecasting dates available before execution date\")\n\n    df = self.matrix.select(\"time\", *forecast_cols)\n\n    # OPTIMIZATION 3: Determine target frequency early\n    if timestep:\n        frequency_target = get_duration(timestep)\n    else:\n        frequency_target = get_lowest_frequency(df)\n\n    limits = {}\n    for col in forecast_cols:\n        freq = self._get_column_frequency(col, df)\n        limits[col] = freq / frequency_target\n\n    max_time = df[\"time\"].max()  # type: ignore[operator]\n    if end_date &gt; max_time:\n        dt = cast(\"pendulum.DateTime\", max_time)\n        last_row = df.filter(pl.col(\"time\").eq(dt))\n        column_wth_last_timestamp = None\n        for col in forecast_cols:\n            if last_row[col][0] is not None:\n                column_wth_last_timestamp = col\n                break\n\n        if column_wth_last_timestamp:\n            frequency_columns_last_timestamp = self._get_column_frequency(column_wth_last_timestamp, df)\n            if frequency_target &lt; frequency_columns_last_timestamp:\n                datetimes_to_add = generate_datetimes(\n                    start=dt,\n                    end=dt + frequency_columns_last_timestamp - frequency_target,\n                    freq=frequency_target,\n                    timezone=self.timezone,\n                )\n                if len(datetimes_to_add) &gt; 1:\n                    new_df = pl.DataFrame(\n                        {\n                            \"time\": datetimes_to_add[1:],\n                            column_wth_last_timestamp: [None] * len(datetimes_to_add[1:]),\n                        },\n                        schema={\n                            \"time\": pl.Datetime(\"us\", self.timezone),\n                            column_wth_last_timestamp: pl.Float64(),\n                        },\n                    )\n                    df = pl.concat([df, new_df], how=\"diagonal\")\n\n    interpolate_expr = [\n        pl.col(col).forward_fill(limit=int(limits[col])) for col in forecast_cols if limits[col] &gt; 1\n    ]\n\n    forecast_expr = pl.coalesce([pl.col(col) for col in forecast_cols])\n\n    df = (\n        df.upsample(\"time\", every=frequency_target)\n        .with_columns(interpolate_expr)\n        .filter(pl.col(\"time\").is_between(start_date, end_date))\n        .select(\n            [\n                pl.col(\"time\"),\n                forecast_expr.alias(\"forecast\"),\n            ]\n        )\n    )\n\n    if default_value is not None:\n        # we must set a value for each index between start and end date\n        # using default value if none is found\n        full_index = pl.DataFrame(\n            {\n                \"time\": generate_datetimes(\n                    start=start_date,\n                    end=end_date,\n                    freq=frequency_target,\n                    timezone=self.timezone,\n                )\n            }\n        )\n        df_complete = full_index.join(df, on=\"time\", how=\"left\").with_columns(\n            pl.col(\"forecast\").fill_null(default_value)\n        )\n        return Timeseries(df_complete, timezone=self.timezone)\n\n    return Timeseries(df, timezone=self.timezone)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.get_matrix","title":"get_matrix","text":"<pre><code>get_matrix() -&gt; DataFrame\n</code></pre> <p>Get the matrix data.</p> <p>Returns:</p> Type Description <code>pl.DataFrame</code> <p>The matrix data.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def get_matrix(self) -&gt; pl.DataFrame:\n    \"\"\"\n    Get the matrix data.\n\n    :return: The matrix data.\n    :rtype: pl.DataFrame\n    \"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; Figure\n</code></pre> <p>Generate an interactive Plotly figure for the ScenarioMatrix data with a slider to select indexes.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Plot title</p> <code>'ScenarioMatrix Timeseries Plot'</code> <code>height</code> <code>int</code> <p>Plot height in pixels</p> <code>500</code> <code>width</code> <code>int</code> <p>Plot width in pixels</p> <code>800</code> <code>show_grid</code> <code>bool</code> <p>Whether to show grid lines</p> <code>True</code> <code>line_shape</code> <code>Literal['hv', 'linear', 'spline']</code> <p>Shape of the plot lines</p> <code>'hv'</code> <code>template</code> <code>str</code> <p>Plotly template to use</p> <code>'plotly_white'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly figure object</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def plot(\n    self,\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; go.Figure:\n    \"\"\"\n    Generate an interactive Plotly figure for the ScenarioMatrix data with a slider to select indexes.\n\n    :param title: Plot title\n    :param height: Plot height in pixels\n    :param width: Plot width in pixels\n    :param show_grid: Whether to show grid lines\n    :param line_shape: Shape of the plot lines\n    :param template: Plotly template to use\n    :return: Plotly figure object\n    \"\"\"\n    df = self.get_matrix()\n    index_columns = self._get_indexes()\n    time_col = \"time\"\n\n    fig = go.Figure()\n\n    for i, idx in enumerate(index_columns):\n        visible = i == 0\n        fig.add_trace(\n            go.Scatter(\n                x=df[time_col],  # type: ignore[arg-type]\n                y=df[idx],  # type: ignore[arg-type]\n                mode=\"lines\",\n                name=idx,\n                line_shape=line_shape,\n                visible=visible,\n            )\n        )\n\n    steps = []\n    for i, idx in enumerate(index_columns):\n        step = {\n            \"method\": \"update\",\n            \"label\": idx,\n            \"args\": [\n                {\"visible\": [j == i for j in range(len(index_columns))]},\n                {\"title\": f\"{title} - {idx}\"},\n            ],\n        }\n        steps.append(step)\n\n    sliders = [\n        {\n            \"active\": 0,\n            \"currentvalue\": {\"prefix\": \"Index: \"},\n            \"pad\": {\"t\": 50},\n            \"steps\": steps,\n        }\n    ]\n\n    fig.update_layout(\n        sliders=sliders,\n        title=title,\n        height=height,\n        width=width,\n        template=template,\n        xaxis={\n            \"title\": \"Time\",\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n        yaxis={\n            \"title\": \"Value\",\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n    )\n\n    return fig\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.replace","title":"replace","text":"<pre><code>replace(\n    index: str | datetime | DateTime,\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n) -&gt; None\n</code></pre> <p>Replace a Timeseries in the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str | datetime | DateTime</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def replace(\n    self,\n    index: str | datetime | pendulum.DateTime,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n) -&gt; None:\n    \"\"\"\n    Replace a Timeseries in the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    self.delete(index=index)\n    self.add(timeseries=timeseries, index=index)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.select","title":"select","text":"<pre><code>select(index: str | datetime) -&gt; Timeseries\n</code></pre> <p>Get a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def select(self, index: str | datetime) -&gt; Timeseries:\n    \"\"\"\n    Get a timeseries by index.\n\n    :param index: Index key.\n    :type index: Index\n    :raises KeyError: If the index is not found.\n    :return: The Timeseries object.\n    :rtype: Timeseries\n    \"\"\"\n    return self.__getitem__(index)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | timedelta | Duration,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Change the frequency (timestep) of all scenario time series in the matrix.</p> <p>This method upscales or downscales all scenarios to the specified frequency, similar to the <code>set_frequency</code> method for individual timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | timedelta | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>ScenarioMatrix</code> <p>The resampled scenario matrix, either modified in place or as a new object.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def set_frequency(self, frequency: str | timedelta | pendulum.Duration, inplace: bool = True) -&gt; Self:\n    \"\"\"\n    Change the frequency (timestep) of all scenario time series in the matrix.\n\n    This method upscales or downscales all scenarios to the specified frequency,\n    similar to the `set_frequency` method for individual timeseries.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled scenario matrix, either modified in place or as a new object.\n    :rtype: ScenarioMatrix\n    \"\"\"\n    if len(self.matrix) == 0:\n        return self if inplace else self.__class__(self.matrix.clone(), self.timezone)\n\n    new_timestep = get_duration(frequency)\n    current_frequency = infer_frequency(self.matrix)\n\n    if new_timestep &gt; current_frequency:\n        df = self._downsample(new_timestep)\n    elif new_timestep &lt; current_frequency:\n        df = self._upsample(new_timestep)\n    else:\n        df = self.matrix.clone() if not inplace else self.matrix\n\n    if inplace:\n        self.matrix = df\n        return self\n    else:\n        return self.__class__(df, self.timezone)\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the time series to a file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"\n    Export the time series to a file.\n\n    :param path: Destination file path\n    :type path: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path = str(path)\n    if not path.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    if file_format_lower == \"csv\":\n        self.matrix.write_csv(path, separator=separator)\n    elif file_format_lower == \"parquet\":\n        self.matrix.write_parquet(path)\n    elif file_format_lower == \"pickle\":\n        with open(path, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the ScenarioMatrix to a file with an attribute column.</p> <p>If the file already exists and concatenate is True, the new data will be appended to the existing data.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>attribute</code> <code>str</code> <p>Attribute name to add as a column</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <code>concatenate</code> <code>bool</code> <p>If True, concatenate with existing file data, defaults to True</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"\n    Export the ScenarioMatrix to a file with an attribute column.\n\n    If the file already exists and concatenate is True, the new data will be\n    appended to the existing data.\n\n    :param path: Destination file path\n    :type path: str or Path\n    :param attribute: Attribute name to add as a column\n    :type attribute: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :param concatenate: If True, concatenate with existing file data, defaults to True\n    :type concatenate: bool, optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path_str = str(path)\n    else:\n        path_str = path\n\n    if not path_str.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    df_to_write = self.matrix.clone().insert_column(1, pl.lit(attribute).alias(\"attribute\"))\n\n    if concatenate:\n        path_obj = Path(path_str)\n        if path_obj.exists() and file_format_lower != \"pickle\":\n            try:\n                if file_format_lower == \"csv\":\n                    existing_df = pl.read_csv(path_str, separator=separator, try_parse_dates=True)\n                elif file_format_lower == \"parquet\":\n                    existing_df = pl.read_parquet(path_str)\n\n                df_to_write = pl.concat([existing_df, df_to_write])\n            except Exception:\n                pass\n\n    if file_format_lower == \"csv\":\n        df_to_write.write_csv(path_str, separator=separator)\n    elif file_format_lower == \"parquet\":\n        df_to_write.write_parquet(path_str)\n    elif file_format_lower == \"pickle\":\n        with open(path_str, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/forecasting_matrix/#atlas.ForecastingMatrix.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Convert the internal Polars DataFrame to a LazyFrame.</p> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>A Polars LazyFrame representation of the time series</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"\n    Convert the internal Polars DataFrame to a LazyFrame.\n\n    :return: A Polars LazyFrame representation of the time series\n    :rtype: pl.LazyFrame\n    \"\"\"\n    return self.matrix.lazy()\n</code></pre>"},{"location":"api/math/lazy_matrix/","title":"Lazy ScenarioMatrix","text":""},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix","title":"atlas.LazyScenarioMatrix","text":"<pre><code>LazyScenarioMatrix(\n    matrix: LazyFrame | LazyScenarioMatrix | ScenarioMatrix,\n    timezone: str = \"UTC\",\n)\n</code></pre> <p>               Bases: <code>AbstractScenarioMatrix[LazyFrame]</code></p> <p>Base class for lazily storing Timeseries-like data indexed by scenario keys or datetimes.</p> <p>Initialize the LazyScenarioMatrix.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>LazyFrame | LazyScenarioMatrix | ScenarioMatrix</code> <p>LazyFrame, ScenarioMatrix, or LazyScenarioMatrix object.</p> required <code>timezone</code> <code>str</code> <p>Timezone for the datetime column.</p> <code>'UTC'</code> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __init__(self, matrix: pl.LazyFrame | LazyScenarioMatrix | ScenarioMatrix, timezone: str = \"UTC\") -&gt; None:\n    \"\"\"\n    Initialize the LazyScenarioMatrix.\n\n    :param matrix: LazyFrame, ScenarioMatrix, or LazyScenarioMatrix object.\n    :type matrix: pl.LazyFrame | LazyScenarioMatrix | ScenarioMatrix\n    :param timezone: Timezone for the datetime column.\n    :type timezone: str\n    \"\"\"\n    check_timezone(timezone)\n    self.timezone = timezone\n\n    if isinstance(matrix, LazyScenarioMatrix):\n        self.matrix = matrix.get_matrix()\n        self.timezone = matrix.timezone\n    elif isinstance(matrix, ScenarioMatrix):\n        self.matrix = matrix.to_lazy()\n        self.timezone = matrix.timezone\n    elif isinstance(matrix, pl.LazyFrame):\n        schema = matrix.collect_schema()\n\n        time_columns = [name for name, dtype in schema.items() if dtype.is_temporal()]\n        value_columns = [name for name, dtype in schema.items() if dtype.is_numeric()]\n\n        if len(time_columns) != 1:\n            raise ValueError(\"LazyScenarioMatrix must have exactly one datetime column\")\n\n        if len(time_columns) + len(value_columns) != len(schema):\n            raise ValueError(\"LazyScenarioMatrix must have N columns one for datetime and N-1 for numerical values\")\n\n        self.matrix = (\n            matrix.rename({time_columns[0]: \"time\"})\n            .with_columns(pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)))\n            .sort(\"time\")\n        )\n    else:\n        raise TypeError(\"LazyScenarioMatrix requires a LazyFrame, ScenarioMatrix, or LazyScenarioMatrix\")\n\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: LazyFrame\n</code></pre> <p>Returns the underlying LazyFrame.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.index","title":"index  <code>property</code>","text":"<pre><code>index: list[str]\n</code></pre> <p>Returns the ScenarioMatrix indexes (e.g columns names)</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.lazyframe","title":"lazyframe  <code>property</code>","text":"<pre><code>lazyframe: LazyFrame\n</code></pre> <p>Returns the ScenarioMatrix DataFrame</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict\n</code></pre> <p>Return the metadata of the matrix.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the ScenarioMatrix shape.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.__contains__","title":"__contains__","text":"<pre><code>__contains__(index: str) -&gt; bool\n</code></pre> <p>Check if an index exists in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __contains__(self, index: str) -&gt; bool:\n    \"\"\"Check if an index exists in the matrix.\"\"\"\n    return index in self.indexes\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Check equality with another matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check equality with another matrix.\"\"\"\n    if not isinstance(other, LazyScenarioMatrix):\n        raise TypeError(\"Cannot compare with non-LazyScenarioMatrix object\")\n    return self.matrix.collect().equals(other.matrix.collect())\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a timeseries by index.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __getitem__(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"Get a timeseries by index.\"\"\"\n    return self.select(index)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of timeseries in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Number of timeseries in the matrix.\"\"\"\n    return len(self.indexes)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the ScenarioMatrix</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __repr__(self):\n    \"\"\"String representation of the ScenarioMatrix\"\"\"\n    return f\"LazyScenarioMatrix with schema : {self.matrix.collect_schema()}\"\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute absolute value of all numeric columns.</p> Source code in <code>atlas/math/abstract_scenario_matrix.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute absolute value of all numeric columns.\"\"\"\n    backend = self._get_data()\n    result = backend.select([\"time\"] + [pl.col(c).abs().alias(c) for c in self.index])\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.add","title":"add","text":"<pre><code>add(\n    timeseries: LazyTimeseries\n    | LazyFrame\n    | Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    index: str,\n) -&gt; None\n</code></pre> <p>Add a timeseries to the lazy matrix.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>LazyTimeseries | LazyFrame | Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries to add.</p> required <code>index</code> <code>str</code> <p>Index to add into the matrix</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index already exists in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def add(\n    self,\n    timeseries: LazyTimeseries | pl.LazyFrame | Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    index: str,\n) -&gt; None:\n    \"\"\"\n    Add a timeseries to the lazy matrix.\n\n    :param timeseries: Timeseries to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | dict[str, list]\n    :param index: Index to add into the matrix\n    :type index: str\n    :raises KeyError: If index already exists in the matrix.\n    \"\"\"\n    if index in self.indexes:\n        raise KeyError(f\"Index {index} already exists in the matrix.\")\n\n    if isinstance(timeseries, LazyTimeseries):\n        lazy_ts = timeseries.lazyframe\n    elif isinstance(timeseries, pl.LazyFrame):\n        lazy_ts = timeseries\n    else:\n        lazy_ts = Timeseries(timeseries).to_frame(engine=\"polars\").rename({\"value\": index}).lazy()  # type: ignore [operator]\n\n    # Join with the existing matrix\n    self.matrix = self.matrix.join(\n        lazy_ts,\n        on=\"time\",\n        how=\"full\",\n        coalesce=True,\n    )\n\n    # Update indexes\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.collect","title":"collect","text":"<pre><code>collect() -&gt; ScenarioMatrix\n</code></pre> <p>Collect the lazy frame and return a regular ScenarioMatrix object.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def collect(self) -&gt; ScenarioMatrix:\n    \"\"\"Collect the lazy frame and return a regular ScenarioMatrix object.\"\"\"\n    return ScenarioMatrix(self.matrix.collect(), timezone=self.timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.delete","title":"delete","text":"<pre><code>delete(index: str) -&gt; None\n</code></pre> <p>Delete a timeseries by index from the lazy matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key to delete from the matrix</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index is not found.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def delete(self, index: str) -&gt; None:\n    \"\"\"\n    Delete a timeseries by index from the lazy matrix.\n\n    :param index: Index key to delete from the matrix\n    :type index: str\n    :raises KeyError: If index is not found.\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"No timeseries to delete at index: {index}\")\n\n    self.matrix = self.matrix.drop(index)\n\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.describe","title":"describe","text":"<pre><code>describe() -&gt; dict\n</code></pre> <p>Get metadata about the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def describe(self) -&gt; dict:\n    \"\"\"Get metadata about the matrix.\"\"\"\n\n    return get_metadata_from_frame(self.matrix.collect())\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyScenarioMatrix\n</code></pre> <p>Load a LazyScenarioMatrix from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file</p> required <code>separator</code> <code>str</code> <p>CSV separator (if applicable)</p> <code>';'</code> <code>timezone</code> <code>str</code> <p>Timezone to apply</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyScenarioMatrix</code> <p>LazyScenarioMatrix instance</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyScenarioMatrix:\n    \"\"\"\n    Load a LazyScenarioMatrix from a file.\n\n    :param file_path: Path to the file\n    :param separator: CSV separator (if applicable)\n    :param timezone: Timezone to apply\n    :return: LazyScenarioMatrix instance\n    \"\"\"\n\n    return cls(scan_data_file(file_path, filters, separator), timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.get_matrix","title":"get_matrix","text":"<pre><code>get_matrix() -&gt; LazyFrame\n</code></pre> <p>Return internal lazy frame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def get_matrix(self) -&gt; pl.LazyFrame:\n    \"\"\"Return internal lazy frame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n)\n</code></pre> <p>Generate an interactive Plotly figure.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def plot(\n    self,\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n):\n    \"\"\"Generate an interactive Plotly figure.\"\"\"\n    return self.collect().plot(title, height, width, show_grid, line_shape, template)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.replace","title":"replace","text":"<pre><code>replace(\n    index: str,\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n) -&gt; None\n</code></pre> <p>Replace a Timeseries in the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def replace(\n    self,\n    index: str,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n) -&gt; None:\n    \"\"\"\n    Replace a Timeseries in the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    self.delete(index=index)\n    self.add(timeseries=timeseries, index=index)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.select","title":"select","text":"<pre><code>select(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a lazy timeseries by index.</p> <p>This operation does not materialize the data, keeping it lazy.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The LazyTimeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def select(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"\n    Get a lazy timeseries by index.\n\n    This operation does not materialize the data, keeping it lazy.\n\n    :param index: Index key.\n    :type index: str\n    :raises KeyError: If the index is not found.\n    :return: The LazyTimeseries object.\n    :rtype: LazyTimeseries\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"Index {index} not found in the matrix.\")\n\n    # Select time and the specified column, rename to 'value'\n    lazy_ts = self.matrix.select([\"time\", index]).rename({index: \"value\"})\n    return LazyTimeseries(lazy_ts, timezone=self.timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | timedelta | Duration,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Change the frequency (timestep) of all scenario time series in the lazy matrix.</p> <p>This method collects the lazy frame, applies set_frequency, then converts back to lazy.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | timedelta | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyScenarioMatrix</code> <p>The resampled lazy scenario matrix, either modified in place or as a new object.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def set_frequency(self, frequency: str | timedelta | pendulum.Duration, inplace: bool = True) -&gt; Self:\n    \"\"\"\n    Change the frequency (timestep) of all scenario time series in the lazy matrix.\n\n    This method collects the lazy frame, applies set_frequency, then converts back to lazy.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled lazy scenario matrix, either modified in place or as a new object.\n    :rtype: LazyScenarioMatrix\n    \"\"\"\n    resampled_sm = self.collect().set_frequency(frequency, inplace=False)\n    if inplace:\n        self.matrix = resampled_sm.to_lazy()\n        return self\n    else:\n        return self.__class__(resampled_sm.to_lazy(), timezone=self.timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the matrix to a file.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"Export the matrix to a file.\"\"\"\n    self.collect().to_file(path, file_format, separator)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the matrix to a file with an attribute column.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"Export the matrix to a file with an attribute column.\"\"\"\n    self.collect().to_file_with_attribute(path, attribute, file_format, separator, concatenate)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyScenarioMatrix.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Return the internal LazyFrame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"Return the internal LazyFrame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix","title":"atlas.LazyForecastingMatrix","text":"<pre><code>LazyForecastingMatrix(\n    matrix: LazyScenarioMatrix | LazyFrame | ScenarioMatrix,\n    timezone: str = \"UTC\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n)\n</code></pre> <p>               Bases: <code>LazyScenarioMatrix</code></p> <p>Stores Timeseries objects lazily by scenario name, with access and deletion by name.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __init__(\n    self,\n    matrix: LazyScenarioMatrix | pl.LazyFrame | ScenarioMatrix,\n    timezone: str = \"UTC\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; None:\n    super().__init__(matrix, timezone)\n    self.date_format: str = date_format\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: LazyFrame\n</code></pre> <p>Returns the underlying LazyFrame.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.index","title":"index  <code>property</code>","text":"<pre><code>index: list[str]\n</code></pre> <p>Returns the ScenarioMatrix indexes (e.g columns names)</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.lazyframe","title":"lazyframe  <code>property</code>","text":"<pre><code>lazyframe: LazyFrame\n</code></pre> <p>Returns the ScenarioMatrix DataFrame</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict\n</code></pre> <p>Return the metadata of the matrix.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the ScenarioMatrix shape.</p>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.__contains__","title":"__contains__","text":"<pre><code>__contains__(index: str | datetime | DateTime) -&gt; bool\n</code></pre> <p>Check if an index exists in the lazy forecasting matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime | DateTime</code> <p>Forecast generation datetime (as string or datetime object).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if index exists, False otherwise.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __contains__(self, index: str | datetime | pendulum.DateTime) -&gt; bool:\n    \"\"\"\n    Check if an index exists in the lazy forecasting matrix.\n\n    :param index: Forecast generation datetime (as string or datetime object).\n    :type index: str | datetime | pendulum.DateTime\n    :return: True if index exists, False otherwise.\n    :rtype: bool\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n    return dt in self.indexes\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Check equality with another matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check equality with another matrix.\"\"\"\n    if not isinstance(other, LazyScenarioMatrix):\n        raise TypeError(\"Cannot compare with non-LazyScenarioMatrix object\")\n    return self.matrix.collect().equals(other.matrix.collect())\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a timeseries by index.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __getitem__(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"Get a timeseries by index.\"\"\"\n    return self.select(index)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of timeseries in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Number of timeseries in the matrix.\"\"\"\n    return len(self.indexes)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the matrix</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def __repr__(self):\n    \"\"\"String representation of the matrix\"\"\"\n    return f\"LazyForecastingMatrix with schema : {self.matrix.collect_schema()}\"\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute absolute value of all numeric columns.</p> Source code in <code>atlas/math/abstract_scenario_matrix.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute absolute value of all numeric columns.\"\"\"\n    backend = self._get_data()\n    result = backend.select([\"time\"] + [pl.col(c).abs().alias(c) for c in self.index])\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.add","title":"add","text":"<pre><code>add(\n    timeseries: LazyTimeseries\n    | LazyFrame\n    | Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    index: str | datetime | DateTime,\n) -&gt; None\n</code></pre> <p>Add a timeseries to the lazy forecasting matrix.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>LazyTimeseries | LazyFrame | Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str | datetime | DateTime</code> <p>Datetime key for the new forecast.</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index already exists in the matrix.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def add(\n    self,\n    timeseries: LazyTimeseries | pl.LazyFrame | Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    index: str | datetime | pendulum.DateTime,\n) -&gt; None:\n    \"\"\"\n    Add a timeseries to the lazy forecasting matrix.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime | pendulum.DateTime\n    :raises KeyError: If index already exists in the matrix.\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n    super().add(timeseries, dt)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.collect","title":"collect","text":"<pre><code>collect() -&gt; ForecastingMatrix\n</code></pre> <p>Collect the lazy frame and return a regular ForecastingMatrix object.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def collect(self) -&gt; ForecastingMatrix:\n    \"\"\"Collect the lazy frame and return a regular ForecastingMatrix object.\"\"\"\n    return ForecastingMatrix(self.matrix.collect(), timezone=self.timezone, date_format=self.date_format)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.delete","title":"delete","text":"<pre><code>delete(index: str | datetime | DateTime) -&gt; None\n</code></pre> <p>Delete a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str | datetime | DateTime</code> <p>Forecast generation datetime (as string or datetime object).</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index does not exist in the matrix.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def delete(self, index: str | datetime | pendulum.DateTime) -&gt; None:\n    \"\"\"\n    Delete a timeseries by index.\n\n    :param index: Forecast generation datetime (as string or datetime object).\n    :type index: str | datetime | pendulum.DateTime\n    :raises KeyError: If the index does not exist in the matrix.\n    \"\"\"\n    dt: str = build_datetime(index, self.date_format).format(self.date_format)\n    super().delete(dt)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.describe","title":"describe","text":"<pre><code>describe() -&gt; dict\n</code></pre> <p>Get metadata about the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def describe(self) -&gt; dict:\n    \"\"\"Get metadata about the matrix.\"\"\"\n\n    return get_metadata_from_frame(self.matrix.collect())\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; LazyForecastingMatrix\n</code></pre> <p>Load a LazyForecastingMatrix from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file</p> required <code>timezone</code> <code>str</code> <p>Timezone to apply</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyForecastingMatrix</code> <p>LazyForecastingMatrix instance</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; LazyForecastingMatrix:\n    \"\"\"\n    Load a LazyForecastingMatrix from a file.\n\n    :param file_path: Path to the file\n    :type file_path: str | Path\n    :param timezone: Timezone to apply\n    :type timezone: str\n    :return: LazyForecastingMatrix instance\n    :rtype: LazyForecastingMatrix\n    \"\"\"\n    return cls(\n        super().from_file(\n            file_path,\n            timezone=timezone,\n            filters=filters,\n            separator=separator,\n        ),\n        timezone,\n        date_format,\n    )\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.get_forecast","title":"get_forecast","text":"<pre><code>get_forecast(\n    execution_date: datetime | str | DateTime,\n    start_date: datetime | str | DateTime,\n    end_date: datetime | str | DateTime,\n    timestep: str | Duration | None = None,\n    default_value: float | None = None,\n) -&gt; Timeseries\n</code></pre> <p>Returns the most up-to-date forecast available per time row in the given window. Newer forecasts are prioritized. Gaps are filled from older forecasts.</p> <p>Parameters:</p> Name Type Description Default <code>execution_date</code> <code>datetime | str | DateTime</code> <p>The reference date for determining which forecasts are available. Only forecasts made on or before this date will be considered.</p> required <code>start_date</code> <code>datetime | str | DateTime</code> <p>Start date of the forecast window to retrieve.</p> required <code>end_date</code> <code>datetime | str | DateTime</code> <p>End date of the forecast window to retrieve.</p> required <code>timestep</code> <code>str | Duration | None</code> <p>Target frequency for the output timeseries. If None, the lowest frequency found in the data will be used.</p> <code>None</code> <code>default_value</code> <code>float | None</code> <p>Default value used for indexes where no value is found</p> <code>None</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>A timeseries containing the most recent forecast values for each timestamp in the specified window, with gaps filled using older forecasts.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If start_date is after end_date or if no forecasting dates are available before the execution date.</p> Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def get_forecast(\n    self,\n    execution_date: datetime | str | pendulum.DateTime,\n    start_date: datetime | str | pendulum.DateTime,\n    end_date: datetime | str | pendulum.DateTime,\n    timestep: str | pendulum.Duration | None = None,\n    default_value: float | None = None,\n) -&gt; Timeseries:\n    \"\"\"\n    Returns the most up-to-date forecast available per time row in the given window.\n    Newer forecasts are prioritized. Gaps are filled from older forecasts.\n\n    :param execution_date: The reference date for determining which forecasts are available.\n                          Only forecasts made on or before this date will be considered.\n    :type execution_date: datetime | str | pendulum.DateTime\n    :param start_date: Start date of the forecast window to retrieve.\n    :type start_date: datetime | str | pendulum.DateTime\n    :param end_date: End date of the forecast window to retrieve.\n    :type end_date: datetime | str | pendulum.DateTime\n    :param timestep: Target frequency for the output timeseries. If None, the lowest\n                    frequency found in the data will be used.\n    :type timestep: str | pendulum.Duration | None\n    :param default_value: Default value used for indexes where no value is found\n    :type default_value: float | None\n    :raises ValueError: If start_date is after end_date or if no forecasting dates\n                       are available before the execution date.\n    :return: A timeseries containing the most recent forecast values for each timestamp\n            in the specified window, with gaps filled using older forecasts.\n    :rtype: Timeseries\n    \"\"\"\n    return self.collect().get_forecast(\n        execution_date=execution_date,\n        start_date=start_date,\n        end_date=end_date,\n        timestep=timestep,\n        default_value=default_value,\n    )\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.get_matrix","title":"get_matrix","text":"<pre><code>get_matrix() -&gt; LazyFrame\n</code></pre> <p>Return internal lazy frame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def get_matrix(self) -&gt; pl.LazyFrame:\n    \"\"\"Return internal lazy frame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n)\n</code></pre> <p>Generate an interactive Plotly figure.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def plot(\n    self,\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n):\n    \"\"\"Generate an interactive Plotly figure.\"\"\"\n    return self.collect().plot(title, height, width, show_grid, line_shape, template)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.replace","title":"replace","text":"<pre><code>replace(\n    index: str | datetime | DateTime,\n    timeseries: LazyTimeseries\n    | LazyFrame\n    | Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n) -&gt; None\n</code></pre> <p>Replace a Timeseries in the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>LazyTimeseries | LazyFrame | Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str | datetime | DateTime</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/forecasting_matrix.py</code> <pre><code>def replace(\n    self,\n    index: str | datetime | pendulum.DateTime,\n    timeseries: LazyTimeseries | pl.LazyFrame | Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n) -&gt; None:\n    \"\"\"\n    Replace a Timeseries in the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | dict[str, list]\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    self.delete(index=index)\n    self.add(timeseries=timeseries, index=index)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.select","title":"select","text":"<pre><code>select(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a lazy timeseries by index.</p> <p>This operation does not materialize the data, keeping it lazy.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The LazyTimeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def select(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"\n    Get a lazy timeseries by index.\n\n    This operation does not materialize the data, keeping it lazy.\n\n    :param index: Index key.\n    :type index: str\n    :raises KeyError: If the index is not found.\n    :return: The LazyTimeseries object.\n    :rtype: LazyTimeseries\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"Index {index} not found in the matrix.\")\n\n    # Select time and the specified column, rename to 'value'\n    lazy_ts = self.matrix.select([\"time\", index]).rename({index: \"value\"})\n    return LazyTimeseries(lazy_ts, timezone=self.timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | timedelta | Duration,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Change the frequency (timestep) of all scenario time series in the lazy matrix.</p> <p>This method collects the lazy frame, applies set_frequency, then converts back to lazy.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | timedelta | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyScenarioMatrix</code> <p>The resampled lazy scenario matrix, either modified in place or as a new object.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def set_frequency(self, frequency: str | timedelta | pendulum.Duration, inplace: bool = True) -&gt; Self:\n    \"\"\"\n    Change the frequency (timestep) of all scenario time series in the lazy matrix.\n\n    This method collects the lazy frame, applies set_frequency, then converts back to lazy.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled lazy scenario matrix, either modified in place or as a new object.\n    :rtype: LazyScenarioMatrix\n    \"\"\"\n    resampled_sm = self.collect().set_frequency(frequency, inplace=False)\n    if inplace:\n        self.matrix = resampled_sm.to_lazy()\n        return self\n    else:\n        return self.__class__(resampled_sm.to_lazy(), timezone=self.timezone)\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the matrix to a file.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"Export the matrix to a file.\"\"\"\n    self.collect().to_file(path, file_format, separator)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the matrix to a file with an attribute column.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"Export the matrix to a file with an attribute column.\"\"\"\n    self.collect().to_file_with_attribute(path, attribute, file_format, separator, concatenate)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/lazy_matrix/#atlas.LazyForecastingMatrix.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Return the internal LazyFrame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"Return the internal LazyFrame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/lazy_timeseries/","title":"Lazy Timeseries","text":""},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries","title":"atlas.LazyTimeseries","text":"<pre><code>LazyTimeseries(\n    timeseries: LazyFrame\n    | LazyTimeseries\n    | Any\n    | None = None,\n    timezone: str = \"UTC\",\n)\n</code></pre> <p>               Bases: <code>AbstractTimeseries[LazyFrame]</code></p> <p>A lazy-evaluated time series class using a Polars LazyFrame backend.</p> <p>LazyTimeseries enables efficient, deferred computation on large time series datasets by leveraging Polars' lazy execution model. This is particularly useful for workflows where data is too large to fit in memory or when chaining multiple transformations before materializing results.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>LazyFrame | LazyTimeseries | Any | None</code> <p>The input time series data</p> <code>None</code> <code>timezone</code> <code>str</code> <p>Timezone string used to convert datetime values, defaults to \"UTC\"</p> <code>'UTC'</code> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __init__(\n    self,\n    timeseries: pl.LazyFrame | LazyTimeseries | Any | None = None,\n    timezone: str = \"UTC\",\n) -&gt; None:\n    \"\"\"\n    :param timeseries: The input time series data\n    :type timeseries: pl.LazyFrame or LazyTimeseries or Timeseries\n    :param timezone: Timezone string used to convert datetime values, defaults to \"UTC\"\n    :type timezone: str, optional\n    \"\"\"\n\n    check_timezone(timezone)\n    self.timezone: str = timezone\n\n    if timeseries is None:\n        self.timeseries: pl.LazyFrame = pl.LazyFrame(\n            schema={\n                \"time\": pl.Datetime(\"us\", time_zone=self.timezone),\n                \"value\": pl.Float64(),\n            }\n        )\n    elif isinstance(timeseries, LazyTimeseries):\n        self.timeseries = timeseries.to_frame()\n        self.timezone = timeseries.timezone\n    elif isinstance(timeseries, Timeseries):\n        self.timeseries = timeseries.to_lazy()\n        self.timezone = timeseries.timezone\n    elif isinstance(timeseries, pl.LazyFrame):\n        schema = timeseries.collect_schema()\n\n        time_columns = [name for name, dtype in schema.items() if dtype.is_temporal()]\n        value_columns = [name for name, dtype in schema.items() if dtype.is_numeric()]\n\n        if len(value_columns) != 1:\n            raise ValueError(\"Timeseries must have exactly one numeric column\")\n        if len(time_columns) != 1:\n            raise ValueError(\"Timeseries must have exactly one datetime column\")\n\n        self.timeseries = (\n            timeseries.rename({time_columns[0]: \"time\", value_columns[0]: \"value\"})\n            .with_columns(pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=timezone)))\n            .sort(\"time\")\n        )\n\n    else:\n        raise ValueError(\"LazyTimeseries requires a LazyFrame or another Timeseries object\")\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: LazyFrame\n</code></pre> <p>Returns the underlying LazyFrame.</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.index","title":"index  <code>property</code>","text":"<pre><code>index: list[datetime]\n</code></pre> <p>Returns the LazyTimeseries indexes.</p> <p>Warning: This property materializes the entire time column into memory. Use with caution on large datasets. Consider using filter() or slice() operations instead to work with subsets of the data lazily.</p> <p>Returns:</p> Type Description <code>list[datetime]</code> <p>List of datetime indexes</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.lazyframe","title":"lazyframe  <code>property</code>","text":"<pre><code>lazyframe: LazyFrame\n</code></pre> <p>Return the internal Polars LazyFrame.</p> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>The internal lazy time series data</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict\n</code></pre> <p>Return the metadata of the timeseries.</p> <p>Warning: This property requires collecting the data.</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the Timeseries shape.</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.timestep","title":"timestep  <code>property</code>","text":"<pre><code>timestep: Duration\n</code></pre> <p>Return the frequency of the timeseries index.</p> <p>Warning: This property requires collecting the data to infer frequency.</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.values","title":"values  <code>property</code>","text":"<pre><code>values: list[float]\n</code></pre> <p>Returns the LazyTimeseries values.</p> <p>Warning: This property materializes the entire value column into memory. Use with caution on large datasets.</p> <p>Returns:</p> Type Description <code>list[float]</code> <p>List of values</p>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__add__","title":"__add__","text":"<pre><code>__add__(\n    other: float | LazyTimeseries | Timeseries,\n) -&gt; LazyTimeseries\n</code></pre> <p>Add all numeric columns by a scalar or timeseries.</p> <p>\u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | LazyTimeseries | Timeseries</code> <p>Other timeseries or scalar</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Added LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __add__(self, other: float | LazyTimeseries | Timeseries) -&gt; LazyTimeseries:\n    \"\"\"\n    Add all numeric columns by a scalar or timeseries.\n\n    \u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.\n\n    :param other: Other timeseries or scalar\n    :type other: float | LazyTimeseries | Timeseries\n    :return: Added LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        lf = self.timeseries.with_columns(pl.col(\"value\").add(other))\n        return LazyTimeseries(lf, self.timezone)\n    else:\n        if isinstance(other, LazyTimeseries):\n            other = other.collect()\n        result = self.collect() + other\n        return LazyTimeseries(result.to_lazy(), self.timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__contains__","title":"__contains__","text":"<pre><code>__contains__(item: datetime | str | DateTime) -&gt; bool\n</code></pre> <p>Check if a temporal index exists in the LazyTimeseries.</p> <p>This operation only collects the count of matching rows, not the entire dataset.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>datetime | str | DateTime</code> <p>Datetime to check for existence</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the datetime exists in the LazyTimeseries index, False otherwise</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __contains__(self, item: datetime | str | pendulum.DateTime) -&gt; bool:\n    \"\"\"Check if a temporal index exists in the LazyTimeseries.\n\n    This operation only collects the count of matching rows, not the entire dataset.\n\n    :param item: Datetime to check for existence\n    :type item: datetime or str or pendulum.DateTime\n    :return: True if the datetime exists in the LazyTimeseries index, False otherwise\n    :rtype: bool\n    \"\"\"\n    try:\n        dt = build_datetime(item).in_tz(self.timezone)\n        count = self.timeseries.filter(pl.col(\"time\") == dt).select(pl.len()).collect().item()\n        return count &gt; 0\n    except Exception:\n        return False\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Implement the equality between two LazyTimeseries.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The LazyTimeseries to compare with</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if equal, False otherwise</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"\n    Implement the equality between two LazyTimeseries.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param other: The LazyTimeseries to compare with\n    :type other: LazyTimeseries\n    :return: True if equal, False otherwise\n    :rtype: bool\n    \"\"\"\n    if isinstance(other, LazyTimeseries):\n        return self.timeseries.collect().equals(other.timeseries.collect())\n    raise TypeError(\"Comparison with non-LazyTimeseries objects is not supported\")\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(column_name: str) -&gt; list[float | datetime]\n</code></pre> <p>Get column values by name.</p> <p>\u26a0\ufe0f WARNING: This method materializes the column into memory.</p> <p>Parameters:</p> Name Type Description Default <code>column_name</code> <code>str</code> <p>Column name ('time' or 'value')</p> required <p>Returns:</p> Type Description <code>list[float | datetime]</code> <p>List of column values</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __getitem__(self, column_name: str) -&gt; list[float | datetime]:\n    \"\"\"\n    Get column values by name.\n\n    \u26a0\ufe0f WARNING: This method materializes the column into memory.\n\n    :param column_name: Column name ('time' or 'value')\n    :type column_name: str\n    :return: List of column values\n    :rtype: list[float | datetime]\n    \"\"\"\n    if column_name not in (\"time\", \"value\"):\n        raise KeyError(\"Column name has to be either time or value\")\n    return self.timeseries.select(column_name).collect().to_series().to_list()\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Return the number of rows in the LazyTimeseries.</p> <p>This operation only collects the count, not the entire dataset.</p> <p>Returns:</p> Type Description <code>int</code> <p>The number of rows</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of rows in the LazyTimeseries.\n\n    This operation only collects the count, not the entire dataset.\n\n    :return: The number of rows\n    :rtype: int\n    \"\"\"\n    return self.timeseries.select(pl.len()).collect().item()\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__mul__","title":"__mul__","text":"<pre><code>__mul__(\n    other: float | LazyTimeseries | Timeseries,\n) -&gt; LazyTimeseries\n</code></pre> <p>Multiply all numeric columns by a scalar or another timeseries.</p> <p>\u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | LazyTimeseries | Timeseries</code> <p>Other timeseries or scalar</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Multiplied LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __mul__(self, other: float | LazyTimeseries | Timeseries) -&gt; LazyTimeseries:\n    \"\"\"\n    Multiply all numeric columns by a scalar or another timeseries.\n\n    \u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.\n\n    :param other: Other timeseries or scalar\n    :type other: float | LazyTimeseries | Timeseries\n    :return: Multiplied LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        lf = self.timeseries.with_columns(pl.col(\"value\").mul(other))\n        return LazyTimeseries(lf, self.timezone)\n    else:\n        # For timeseries operations, collect and compute\n        if isinstance(other, LazyTimeseries):\n            other = other.collect()\n        result = self.collect() * other\n        return LazyTimeseries(result.to_lazy(), self.timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __repr__(self):\n    \"\"\"String representation of the LazyTimeseries\"\"\"\n    return f\"LazyTimeseries with schema : {self.timeseries.collect_schema()}\"\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__sub__","title":"__sub__","text":"<pre><code>__sub__(\n    other: float | LazyTimeseries | Timeseries,\n) -&gt; LazyTimeseries\n</code></pre> <p>Subtract all numeric columns by a scalar or timeseries.</p> <p>\u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | LazyTimeseries | Timeseries</code> <p>Other timeseries or scalar</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Subtracted LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __sub__(self, other: float | LazyTimeseries | Timeseries) -&gt; LazyTimeseries:\n    \"\"\"\n    Subtract all numeric columns by a scalar or timeseries.\n\n    \u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.\n\n    :param other: Other timeseries or scalar\n    :type other: float | LazyTimeseries | Timeseries\n    :return: Subtracted LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        lf = self.timeseries.with_columns(pl.col(\"value\").sub(other))\n        return LazyTimeseries(lf, self.timezone)\n    else:\n        if isinstance(other, LazyTimeseries):\n            other = other.collect()\n        result = self.collect() - other\n        return LazyTimeseries(result.to_lazy(), self.timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.__truediv__","title":"__truediv__","text":"<pre><code>__truediv__(\n    other: float | LazyTimeseries | Timeseries,\n) -&gt; LazyTimeseries\n</code></pre> <p>Divide all numeric columns by a scalar or timeseries.</p> <p>\u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | LazyTimeseries | Timeseries</code> <p>Other timeseries or scalar</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Divided LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def __truediv__(self, other: float | LazyTimeseries | Timeseries) -&gt; LazyTimeseries:\n    \"\"\"\n    Divide all numeric columns by a scalar or timeseries.\n\n    \u26a0\ufe0f WARNING: Operations with other timeseries materialize data into memory.\n\n    :param other: Other timeseries or scalar\n    :type other: float | LazyTimeseries | Timeseries\n    :return: Divided LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        if other == 0:\n            raise ZeroDivisionError(\"Division by zero is not allowed\")\n        lf = self.timeseries.with_columns(pl.col(\"value\").truediv(other))\n        return LazyTimeseries(lf, self.timezone)\n    else:\n        if isinstance(other, LazyTimeseries):\n            other = other.collect()\n        result = self.collect() / other\n        return LazyTimeseries(result.to_lazy(), self.timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute the absolute value of each timestamp</p> <p>Parameters:</p> Name Type Description Default <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The timeseries with absolute values</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute the absolute value of each timestamp\n\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: The timeseries with absolute values\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n    result = backend.with_columns(pl.col(\"value\").abs())\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.add_index","title":"add_index","text":"<pre><code>add_index(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Add index to the LazyTimeseries based on an index and a value.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to add</p> required <code>value</code> <code>float</code> <p>Value to add</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the added index</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def add_index(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Add index to the LazyTimeseries based on an index and a value.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param time: Datetime to add\n    :type time: datetime or str\n    :param value: Value to add\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the added index\n    :rtype: LazyTimeseries\n    \"\"\"\n    updated_ts = self.collect().add_index(time, value, date_format, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.add_indexes","title":"add_indexes","text":"<pre><code>add_indexes(\n    other: LazyTimeseries\n    | Timeseries\n    | DataFrame\n    | LazyFrame,\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Add indexes to the LazyTimeseries based on another timeseries.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>LazyTimeseries | Timeseries | DataFrame | LazyFrame</code> <p>Other timeseries with indexes / values to add</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the added indexes</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def add_indexes(\n    self,\n    other: LazyTimeseries | Timeseries | pl.DataFrame | pl.LazyFrame,\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Add indexes to the LazyTimeseries based on another timeseries.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param other: Other timeseries with indexes / values to add\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the added indexes\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, LazyTimeseries):\n        other = other.collect()\n    updated_ts = self.collect().add_indexes(other, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.collect","title":"collect","text":"<pre><code>collect()\n</code></pre> <p>Collect the lazy dataframe and return as a regular Timeseries.</p> <p>Returns:</p> Type Description <code>Timeseries</code> <p>A regular Timeseries object with the collected data</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def collect(self):\n    \"\"\"\n    Collect the lazy dataframe and return as a regular Timeseries.\n\n    :return: A regular Timeseries object with the collected data\n    :rtype: Timeseries\n    \"\"\"\n\n    return Timeseries(\n        self.timeseries.collect(),\n        timezone=self.timezone,\n    )\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.describe","title":"describe","text":"<pre><code>describe() -&gt; dict\n</code></pre> <p>Get metadata about the timeseries.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Returns:</p> Type Description <code>dict</code> <p>A dictionary containing timeseries metadata</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def describe(self) -&gt; dict:\n    \"\"\"\n    Get metadata about the timeseries.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :return: A dictionary containing timeseries metadata\n    :rtype: dict\n    \"\"\"\n    return get_metadata_from_frame(self.timeseries.collect())\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.filter","title":"filter","text":"<pre><code>filter(\n    item: list[datetime]\n    | list[DateTime]\n    | list[str]\n    | datetime\n    | DateTime\n    | str,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Filter the Timeseries based on a list of datetime.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>list[datetime] | list[DateTime] | list[str] | datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>Filtered Timeseries</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def filter(\n    self,\n    item: list[datetime] | list[pendulum.DateTime] | list[str] | datetime | pendulum.DateTime | str,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"\n    Filter the Timeseries based on a list of datetime.\n\n    :param item: Datetime to filter the Timeseries\n    :type item: list[datetime] or datetime or pendulum.DateTime or str\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Filtered Timeseries\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n\n    if isinstance(item, list):\n        item = [build_datetime(i, date_format=date_format).in_tz(self.timezone) for i in item]\n        filtered = backend.filter(pl.col(\"time\").is_in(item))\n    else:\n        date = build_datetime(item, date_format=date_format).in_tz(self.timezone)\n        filtered = backend.filter(pl.col(\"time\") == date)\n\n    return self._return(filtered, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.first_date","title":"first_date","text":"<pre><code>first_date() -&gt; DateTime | None\n</code></pre> <p>Return the first date in the LazyTimeseries index.</p> <p>This operation only collects the first row's timestamp, not the entire dataset.</p> <p>Returns:</p> Type Description <code>pendulum.DateTime | None</code> <p>The first date in the Timeseries index</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def first_date(self) -&gt; pendulum.DateTime | None:\n    \"\"\"\n    Return the first date in the LazyTimeseries index.\n\n    This operation only collects the first row's timestamp, not the entire dataset.\n\n    :return: The first date in the Timeseries index\n    :rtype: pendulum.DateTime or None\n    \"\"\"\n    result = self.timeseries.select(\"time\").head(1).collect()\n    if result.height &gt; 0:\n        return pendulum.instance(result.item())\n    return None\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.from_dataframe","title":"from_dataframe  <code>classmethod</code>","text":"<pre><code>from_dataframe(\n    dataframe: DataFrame | DataFrame | LazyFrame,\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries\n</code></pre> <p>Create a LazyTimeseries object from a dataframe-like object.</p> <p>Parameters:</p> Name Type Description Default <code>dataframe</code> <code>DataFrame | DataFrame | LazyFrame</code> <p>The input dataframe</p> required <code>timezone</code> <code>str</code> <p>The timezone of the LazyTimeseries, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The LazyTimeseries instantiated from the dataframe-like object</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>@classmethod\ndef from_dataframe(\n    cls,\n    dataframe: pl.DataFrame | pd.DataFrame | pl.LazyFrame,\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries:\n    \"\"\"Create a LazyTimeseries object from a dataframe-like object.\n\n    :param dataframe: The input dataframe\n    :type dataframe: pl.DataFrame | pd.DataFrame | pl.LazyFrame\n    :param timezone: The timezone of the LazyTimeseries, defaults to \"UTC\"\n    :type timezone: str, optional\n    :return: The LazyTimeseries instantiated from the dataframe-like object\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(dataframe, pl.LazyFrame):\n        return cls(dataframe, timezone)\n    elif isinstance(dataframe, pl.DataFrame):\n        return cls(dataframe.lazy(), timezone)\n    elif isinstance(dataframe, pd.DataFrame):\n        return cls(pl.from_pandas(dataframe).lazy(), timezone)\n    else:\n        raise TypeError(\"Input has to be a dataframe-like object.\")\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyTimeseries\n</code></pre> <p>Load a LazyTimeseries object from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Loaded LazyTimeseries object</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Load a LazyTimeseries object from a file.\n\n    :param file_path: Path to the file\n    :type file_path: str or Path\n    :return: Loaded LazyTimeseries object\n    :rtype: LazyTimeseries\n    \"\"\"\n\n    return cls(scan_data_file(file_path, filters, separator), timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.from_index","title":"from_index  <code>classmethod</code>","text":"<pre><code>from_index(\n    start_date: str | datetime | DateTime,\n    frequency: str | timedelta | Duration,\n    end_date: str | datetime | DateTime,\n    default_value: list[float] | float = 0,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries\n</code></pre> <p>Create a LazyTimeseries from a time range and a default value or list of values.</p> <p>Parameters:</p> Name Type Description Default <code>start_date</code> <code>str | datetime | DateTime</code> <p>Start date of the timeseries</p> required <code>frequency</code> <code>str | timedelta | Duration</code> <p>Frequency of the timeseries (e.g., \"1h\", \"15m\")</p> required <code>end_date</code> <code>str | datetime | DateTime</code> <p>End date of the timeseries</p> required <code>default_value</code> <code>list[float] | float</code> <p>A scalar value or a list of values to fill the timeseries</p> <code>0</code> <code>date_format</code> <code>str</code> <p>Format to interpret date strings, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>timezone</code> <code>str</code> <p>Timezone string, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>A LazyTimeseries object with the specified index and values</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>@classmethod\ndef from_index(\n    cls,\n    start_date: str | datetime | pendulum.DateTime,\n    frequency: str | timedelta | pendulum.Duration,\n    end_date: str | datetime | pendulum.DateTime,\n    default_value: list[float] | float = 0,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Create a LazyTimeseries from a time range and a default value or list of values.\n\n    :param start_date: Start date of the timeseries\n    :param frequency: Frequency of the timeseries (e.g., \"1h\", \"15m\")\n    :param end_date: End date of the timeseries\n    :param default_value: A scalar value or a list of values to fill the timeseries\n    :param date_format: Format to interpret date strings, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :param timezone: Timezone string, defaults to \"UTC\"\n    :return: A LazyTimeseries object with the specified index and values\n    :rtype: LazyTimeseries\n    \"\"\"\n\n    start = build_datetime(start_date, date_format).in_tz(timezone)\n    end = build_datetime(end_date, date_format).in_tz(timezone)\n\n    datetimes = generate_datetimes(start, end, frequency, timezone)\n\n    if isinstance(default_value, list):\n        if len(default_value) != len(datetimes):\n            raise ValueError(\n                f\"Values  passed is of size {len(default_value)} when datetimes generated is of size {len(datetimes)}\"\n            )\n        else:\n            df = pl.DataFrame(\n                {\"time\": datetimes, \"value\": default_value},\n                schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n            )\n    elif isinstance(default_value, float | int):\n        df = pl.DataFrame(\n            {\"time\": datetimes, \"value\": [default_value] * len(datetimes)},\n            schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n        )\n\n    return cls(df.lazy(), timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.from_timeseries","title":"from_timeseries  <code>classmethod</code>","text":"<pre><code>from_timeseries(\n    timeseries: Any, default_value: float | None = None\n) -&gt; LazyTimeseries\n</code></pre> <p>Create a LazyTimeseries from another timeseries, using its structure.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Any</code> <p>The input timeseries object</p> required <code>default_value</code> <code>float | None</code> <p>default value to pass to all timestamp values, defaults to None</p> <code>None</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The LazyTimeseries object instantiated</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>@classmethod\ndef from_timeseries(cls, timeseries: Any, default_value: float | None = None) -&gt; LazyTimeseries:\n    \"\"\"Create a LazyTimeseries from another timeseries, using its structure.\n\n    :param timeseries: The input timeseries object\n    :param default_value: default value to pass to all timestamp values, defaults to None\n    :type default_value: float | None, optional\n    :return: The LazyTimeseries object instantiated\n    :rtype: LazyTimeseries\n    \"\"\"\n\n    # Accept both Timeseries and LazyTimeseries\n    if isinstance(timeseries, LazyTimeseries):\n        if default_value is not None:\n            lf = timeseries.to_frame().with_columns(pl.lit(default_value).alias(\"value\"))\n            return cls(lf, timezone=timeseries.timezone)\n        else:\n            return cls(timeseries)\n    elif isinstance(timeseries, Timeseries):\n        if default_value is not None:\n            lf = timeseries.to_lazy().with_columns(pl.lit(default_value).alias(\"value\"))\n            return cls(lf, timezone=timeseries.timezone)\n        else:\n            return cls(timeseries.to_lazy(), timezone=timeseries.timezone)\n    else:\n        raise TypeError(\"Input has to be a timeseries object, if using a dataframe, use 'from_dataframe' \")\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.from_values","title":"from_values  <code>classmethod</code>","text":"<pre><code>from_values(\n    start_date: str | datetime | DateTime,\n    frequency: str | timedelta | Duration,\n    values: list[float],\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries\n</code></pre> <p>Create a LazyTimeseries from start date, frequency and a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>start_date</code> <code>str | datetime | DateTime</code> <p>Start date of the timeseries</p> required <code>frequency</code> <code>str | timedelta | Duration</code> <p>Frequency of the timeseries (e.g., \"1h\", \"15m\")</p> required <code>values</code> <code>list[float]</code> <p>List of values corresponding to the time intervals</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>timezone</code> <code>str</code> <p>Timezone string, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>A LazyTimeseries object with the specified parameters</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>@classmethod\ndef from_values(\n    cls,\n    start_date: str | datetime | pendulum.DateTime,\n    frequency: str | timedelta | pendulum.Duration,\n    values: list[float],\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Create a LazyTimeseries from start date, frequency and a list of values.\n\n    :param start_date: Start date of the timeseries\n    :param frequency: Frequency of the timeseries (e.g., \"1h\", \"15m\")\n    :param values: List of values corresponding to the time intervals\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :param timezone: Timezone string, defaults to \"UTC\"\n    :return: A LazyTimeseries object with the specified parameters\n    :rtype: LazyTimeseries\n    \"\"\"\n\n    if len(values) &lt; 2:\n        raise ValueError(\"Timeseries must contains at least 2 values\")\n\n    start = build_datetime(start_date, date_format).in_tz(timezone)\n    end = build_datetime(start + (len(values) - 1) * get_duration(frequency)).in_tz(timezone)\n\n    datetimes = generate_datetimes(start, end, frequency, timezone)\n\n    df = pl.DataFrame(\n        {\"time\": datetimes, \"value\": values},\n        schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n    )\n\n    return cls(df.lazy(), timezone)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.get_value","title":"get_value","text":"<pre><code>get_value(\n    datetime: str | datetime | DateTime,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; float\n</code></pre> <p>Return values at the given datetime. Raises KeyError if exact match is not found.</p> <p>This operation only collects the matching row, not the entire dataset.</p> <p>Parameters:</p> Name Type Description Default <code>datetime</code> <code>str | datetime | DateTime</code> <p>Datetime to get value for</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <p>Returns:</p> Type Description <code>float</code> <p>The value at the timestamp requested</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the timeseries is empty</p> <code>KeyError</code> <p>If the datetime is not found in the timeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def get_value(\n    self,\n    datetime: str | datetime | pendulum.DateTime,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; float:\n    \"\"\"Return values at the given datetime. Raises KeyError if exact match is not found.\n\n    This operation only collects the matching row, not the entire dataset.\n\n    :param datetime: Datetime to get value for\n    :type datetime: str or datetime\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :return: The value at the timestamp requested\n    :rtype: float\n    :raises ValueError: If the timeseries is empty\n    :raises KeyError: If the datetime is not found in the timeseries\n    \"\"\"\n\n    if len(self) == 0:\n        raise ValueError(\"Can't get value on empty timeseries.\")\n\n    dt = build_datetime(datetime, date_format).in_tz(self.timezone)\n\n    result = self.timeseries.filter(pl.col(\"time\") == dt).select(\"value\").collect()\n\n    if result.height &gt; 0:\n        return result.item()\n    else:\n        raise KeyError(f\"Value for {dt.to_datetime_string()} not found in the Timeseries.\")\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.groupby","title":"groupby","text":"<pre><code>groupby(\n    frequency: str | Duration,\n    agg: str = \"mean\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Group the LazyTimeseries dynamically by time intervals.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>Grouping interval (e.g., \"1h\", \"1d\")</p> required <code>agg</code> <code>str</code> <p>Aggregation method, defaults to \"mean\"</p> <code>'mean'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Grouped LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def groupby(\n    self,\n    frequency: str | pendulum.Duration,\n    agg: str = \"mean\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Group the LazyTimeseries dynamically by time intervals.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param frequency: Grouping interval (e.g., \"1h\", \"1d\")\n    :type frequency: str or pendulum.Duration\n    :param agg: Aggregation method, defaults to \"mean\"\n    :type agg: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Grouped LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    grouped_ts = self.collect().groupby(frequency, agg, inplace=False)\n    lf = grouped_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.interpolate","title":"interpolate","text":"<pre><code>interpolate(\n    interpolation_method: str = \"constant\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Interpolate the LazyTimeseries to fill in missing values.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>interpolation_method</code> <code>str</code> <p>Interpolation method, defaults to \"constant\"</p> <code>'constant'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Interpolated LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def interpolate(self, interpolation_method: str = \"constant\", inplace: bool = True) -&gt; LazyTimeseries:\n    \"\"\"\n    Interpolate the LazyTimeseries to fill in missing values.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param interpolation_method: Interpolation method, defaults to \"constant\"\n    :type interpolation_method: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Interpolated LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    interpolated_ts = self.collect().interpolate(interpolation_method, inplace=False)\n    lf = interpolated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.iter_rows","title":"iter_rows","text":"<pre><code>iter_rows() -&gt; Generator[\n    tuple[datetime, float], None, None\n]\n</code></pre> <p>Iterate over rows of the LazyTimeseries, yielding (time, value) tuples.</p> <p>Note: This method will collect the LazyFrame into memory before iterating.</p> <p>Returns:</p> Type Description <code>Generator[tuple[datetime, float], None, None]</code> <p>A generator yielding tuples containing (time, value) for each row</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def iter_rows(self) -&gt; Generator[tuple[datetime, float], None, None]:\n    \"\"\"\n    Iterate over rows of the LazyTimeseries, yielding (time, value) tuples.\n\n    Note: This method will collect the LazyFrame into memory before iterating.\n\n    :return: A generator yielding tuples containing (time, value) for each row\n    :rtype: Generator[tuple[datetime, float], None, None]\n    \"\"\"\n    for row in self.timeseries.collect().iter_rows(named=True):\n        yield (row[\"time\"], row[\"value\"])\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.last_date","title":"last_date","text":"<pre><code>last_date() -&gt; DateTime | None\n</code></pre> <p>Return the last date in the LazyTimeseries index.</p> <p>This operation only collects the last row's timestamp, not the entire dataset.</p> <p>Returns:</p> Type Description <code>pendulum.DateTime | None</code> <p>The last date in the Timeseries index</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def last_date(self) -&gt; pendulum.DateTime | None:\n    \"\"\"\n    Return the last date in the LazyTimeseries index.\n\n    This operation only collects the last row's timestamp, not the entire dataset.\n\n    :return: The last date in the Timeseries index\n    :rtype: pendulum.DateTime or None\n    \"\"\"\n    result = self.timeseries.select(\"time\").tail(1).collect()\n    if result.height &gt; 0:\n        return pendulum.instance(result.item())\n    return None\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.max","title":"max","text":"<pre><code>max() -&gt; float\n</code></pre> <p>Return the max value column.</p> <p>This operation only collects the aggregated max value, not the entire dataset.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The Timeseries max value</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def max(self) -&gt; float:\n    \"\"\"Return the max value column.\n\n    This operation only collects the aggregated max value, not the entire dataset.\n\n    :return: The Timeseries max value\n    :rtype: float or None\n    \"\"\"\n    result = self.timeseries.select(pl.col(\"value\").max()).collect().item()\n    if result is None:\n        raise RuntimeError(\"Timeseries is empty, can't get the maximum value\")\n    return result\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.min","title":"min","text":"<pre><code>min() -&gt; float\n</code></pre> <p>Return the min value column.</p> <p>This operation only collects the aggregated min value, not the entire dataset.</p> <p>Returns:</p> Type Description <code>float</code> <p>The Timeseries min value</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def min(self) -&gt; float:\n    \"\"\"Return the min value column.\n\n    This operation only collects the aggregated min value, not the entire dataset.\n\n    :return: The Timeseries min value\n    :rtype: float\n    \"\"\"\n    result = self.timeseries.select(pl.col(\"value\").min()).collect().item()\n    if result is None:\n        raise RuntimeError(\"Timeseries is empty, can't get the minimum value\")\n    return result\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.mul_value_at","title":"mul_value_at","text":"<pre><code>mul_value_at(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Multiply to an existing value at a specific datetime.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to multiply by the value</p> required <code>value</code> <code>float</code> <p>Value to multiply to the precedent value</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the value multiplied</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def mul_value_at(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Multiply to an existing value at a specific datetime.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param time: Datetime to multiply by the value\n    :type time: datetime or str\n    :param value: Value to multiply to the precedent value\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the value multiplied\n    :rtype: LazyTimeseries\n    \"\"\"\n    updated_ts = self.collect().mul_value_at(time, value, date_format, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"Time Series Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_color: str = \"#1f77b4\",\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n)\n</code></pre> <p>Generate a Plotly figure for the LazyTimeseries data.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Plot title</p> <code>'Time Series Plot'</code> <code>height</code> <code>int</code> <p>Plot height in pixels</p> <code>500</code> <code>width</code> <code>int</code> <p>Plot width in pixels</p> <code>800</code> <code>show_grid</code> <code>bool</code> <p>Whether to show grid lines</p> <code>True</code> <code>line_color</code> <code>str</code> <p>Color of the line plot</p> <code>'#1f77b4'</code> <code>line_shape</code> <code>str</code> <p>Shape of the plot</p> <code>'hv'</code> <code>template</code> <code>str</code> <p>Plotly template to use</p> <code>'plotly_white'</code> <p>Returns:</p> Type Description <p>Plotly figure object</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def plot(\n    self,\n    title: str = \"Time Series Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_color: str = \"#1f77b4\",\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n):\n    \"\"\"\n    Generate a Plotly figure for the LazyTimeseries data.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param title: Plot title\n    :param height: Plot height in pixels\n    :param width: Plot width in pixels\n    :param show_grid: Whether to show grid lines\n    :param line_color: Color of the line plot\n    :param line_shape: Shape of the plot\n    :param template: Plotly template to use\n    :return: Plotly figure object\n    \"\"\"\n    return self.collect().plot(title, height, width, show_grid, line_color, line_shape, template)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.round","title":"round","text":"<pre><code>round(\n    rounding_precision: int = 0,\n    mode: Literal[\n        \"half_to_even\", \"half_away_from_zero\"\n    ] = \"half_to_even\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Returns a copy of the timeseries with all the numerical values rounded.</p> <p>Parameters:</p> Name Type Description Default <code>rounding_precision</code> <code>int</code> <p>Number of decimals used to round numerical values.</p> <code>0</code> <code>mode</code> <code>Literal['half_to_even', 'half_away_from_zero']</code> <p>Rounding strategy.</p> <code>'half_to_even'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>Rounded Timeseries</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def round(\n    self,\n    rounding_precision: int = 0,\n    mode: Literal[\"half_to_even\", \"half_away_from_zero\"] = \"half_to_even\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"\n    Returns a copy of the timeseries with all the numerical values rounded.\n    :param rounding_precision: Number of decimals used to round numerical values.\n    :type rounding_precision: int\n    :param mode: Rounding strategy.\n    :type mode: str\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Rounded Timeseries\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n    result = backend.with_columns(pl.col(\"value\").round(rounding_precision, mode))\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | Duration, inplace: bool = True\n) -&gt; LazyTimeseries\n</code></pre> <p>Change the frequency (timestep) of the lazy time series.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The resampled lazy time series, either modified in place or as a new object.</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def set_frequency(self, frequency: str | pendulum.Duration, inplace: bool = True) -&gt; LazyTimeseries:\n    \"\"\"\n    Change the frequency (timestep) of the lazy time series.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled lazy time series, either modified in place or as a new object.\n    :rtype: LazyTimeseries\n    \"\"\"\n    resampled_ts = self.collect().set_frequency(frequency, inplace=False)\n    lf = resampled_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.set_timezone","title":"set_timezone","text":"<pre><code>set_timezone(timezone: str) -&gt; None\n</code></pre> <p>Convert the datetime column to a new timezone.</p> <p>Parameters:</p> Name Type Description Default <code>timezone</code> <code>str</code> <p>Timezone string</p> required Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def set_timezone(self, timezone: str) -&gt; None:\n    \"\"\"\n    Convert the datetime column to a new timezone.\n\n    :param timezone: Timezone string\n    :type timezone: str\n    \"\"\"\n    check_timezone(timezone)\n    self.timezone = timezone\n    self.timeseries = self.timeseries.with_columns(\n        pl.col(\"time\").dt.convert_time_zone(timezone),\n    )\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.set_value","title":"set_value","text":"<pre><code>set_value(\n    time: datetime | str,\n    value: float | None,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Update a value at a specific datetime.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to set</p> required <code>value</code> <code>float | None</code> <p>Value to set</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the set value</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def set_value(\n    self,\n    time: datetime | str,\n    value: float | None,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Update a value at a specific datetime.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param time: Datetime to set\n    :type time: datetime or str\n    :param value: Value to set\n    :type value: float or int\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the set value\n    :rtype: LazyTimeseries\n    \"\"\"\n    updated_ts = self.collect().set_value(time, value, date_format, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.set_values","title":"set_values","text":"<pre><code>set_values(\n    other: LazyTimeseries\n    | Timeseries\n    | DataFrame\n    | LazyFrame,\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Set or update values. If the datetime exists, it is overwritten.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>LazyTimeseries | Timeseries | DataFrame | LazyFrame</code> <p>other timeseries with values to updated self</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the new values</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def set_values(\n    self,\n    other: LazyTimeseries | Timeseries | pl.DataFrame | pl.LazyFrame,\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Set or update values. If the datetime exists, it is overwritten.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param other: other timeseries with values to updated self\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the new values\n    :rtype: LazyTimeseries\n    \"\"\"\n    if isinstance(other, LazyTimeseries):\n        other = other.collect()\n    updated_ts = self.collect().set_values(other, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.slice","title":"slice","text":"<pre><code>slice(\n    start_bound: datetime | DateTime | str,\n    end_bound: datetime | DateTime | str,\n    closed: Literal[\n        \"left\", \"right\", \"both\", \"none\"\n    ] = \"both\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Get a slice of the Timeseries</p> <p>Parameters:</p> Name Type Description Default <code>start_bound</code> <code>datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code>end_bound</code> <code>datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code></code> <code>closed</code> <p>{'both', 'left', 'right', 'none'} Define which sides of the interval are closed (inclusive).</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The Timeseries object</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def slice(\n    self,\n    start_bound: datetime | pendulum.DateTime | str,\n    end_bound: datetime | pendulum.DateTime | str,\n    closed: Literal[\"left\", \"right\", \"both\", \"none\"] = \"both\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"Get a slice of the Timeseries\n\n    :param start_bound: Datetime to filter the Timeseries\n    :param end_bound: Datetime to filter the Timeseries\n    :param closed : {'both', 'left', 'right', 'none'}\n        Define which sides of the interval are closed (inclusive).\n    :param inplace: Whether to modify the current instance, defaults to True\n    :return: The Timeseries object\n    \"\"\"\n    backend = self._get_data()\n    date_start = build_datetime(start_bound).in_tz(self.timezone)\n    date_end = build_datetime(end_bound).in_tz(self.timezone)\n    sliced = backend.filter(pl.col(\"time\").is_between(date_start, date_end, closed))\n\n    return self._return(sliced, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.slice_with_offset","title":"slice_with_offset","text":"<pre><code>slice_with_offset(\n    offset: int,\n    length: int | None = None,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Get a slice of the Timeseries</p> <p>Parameters:</p> Name Type Description Default <code>offset</code> <code>int</code> <p>Start index. Negative indexing is supported.</p> required <code>length</code> <code>int | None</code> <p>Length of the slice. If set to <code>None</code>, all rows starting at the offset will be selected.</p> <code>None</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The Timeseries object</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def slice_with_offset(\n    self,\n    offset: int,\n    length: int | None = None,\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"Get a slice of the Timeseries\n\n    :param offset: Start index. Negative indexing is supported.\n    :param length: Length of the slice. If set to `None`, all rows starting at the offset will be selected.\n    :param inplace: Whether to modify the current instance, defaults to True\n    :return: The Timeseries object\n    \"\"\"\n    backend = self._get_data()\n    sliced = backend.slice(offset, length)\n    return self._return(sliced, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.sort","title":"sort","text":"<pre><code>sort(inplace: bool = True) -&gt; LazyTimeseries\n</code></pre> <p>Sort the LazyTimeseries by time.</p> <p>Parameters:</p> Name Type Description Default <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Sorted LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def sort(self, inplace: bool = True) -&gt; LazyTimeseries:\n    \"\"\"\n    Sort the LazyTimeseries by time.\n\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Sorted LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    return self._return(self.timeseries, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.sum","title":"sum","text":"<pre><code>sum() -&gt; float\n</code></pre> <p>Return the sum of the 'value' column.</p> <p>This operation only collects the aggregated sum value, not the entire dataset. Returns 0.0 for empty timeseries.</p> <p>Returns:</p> Type Description <code>float</code> <p>The Timeseries sum value</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def sum(self) -&gt; float:\n    \"\"\"Return the sum of the 'value' column.\n\n    This operation only collects the aggregated sum value, not the entire dataset.\n    Returns 0.0 for empty timeseries.\n\n    :return: The Timeseries sum value\n    :rtype: float\n    \"\"\"\n    return self.timeseries.select(pl.col(\"value\").sum()).collect().item()\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.sum_value_at","title":"sum_value_at","text":"<pre><code>sum_value_at(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Add to an existing value at a specific datetime.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to add to the value</p> required <code>value</code> <code>float</code> <p>Value to add to the precedent value</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>LazyTimeseries with the value added</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def sum_value_at(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Add to an existing value at a specific datetime.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param time: Datetime to add to the value\n    :type time: datetime or str\n    :param value: Value to add to the precedent value\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: LazyTimeseries with the value added\n    :rtype: LazyTimeseries\n    \"\"\"\n    updated_ts = self.collect().sum_value_at(time, value, date_format, inplace=False)\n    lf = updated_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the LazyTimeseries to a file.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>file_format</code> <code>str</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"\n    Export the LazyTimeseries to a file.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param path: Destination file path\n    :type path: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: str, optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    \"\"\"\n    self.collect().to_file(path, file_format, separator)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the LazyTimeseries to a file with an attribute column.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>attribute</code> <code>str</code> <p>Attribute name to add as a column</p> required <code>file_format</code> <code>str</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <code>concatenate</code> <code>bool</code> <p>If True, concatenate with existing file data, defaults to True</p> <code>True</code> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"\n    Export the LazyTimeseries to a file with an attribute column.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param path: Destination file path\n    :type path: str or Path\n    :param attribute: Attribute name to add as a column\n    :type attribute: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: str, optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :param concatenate: If True, concatenate with existing file data, defaults to True\n    :type concatenate: bool, optional\n    \"\"\"\n    self.collect().to_file_with_attribute(path, attribute, file_format, separator, concatenate)\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.to_frame","title":"to_frame","text":"<pre><code>to_frame(engine: str = 'polars') -&gt; LazyFrame\n</code></pre> <p>Return the internal Polars LazyFrame.</p> <p>Parameters:</p> Name Type Description Default <code>engine</code> <code>str</code> <p>Engine to use (only 'polars' supported for LazyTimeseries)</p> <code>'polars'</code> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>The internal lazy time series data</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def to_frame(\n    self,\n    engine: str = \"polars\",\n) -&gt; pl.LazyFrame:\n    \"\"\"\n    Return the internal Polars LazyFrame.\n\n    :param engine: Engine to use (only 'polars' supported for LazyTimeseries)\n    :type engine: str\n    :return: The internal lazy time series data\n    :rtype: pl.LazyFrame\n    \"\"\"\n    if engine != \"polars\":\n        raise ValueError(\"LazyTimeseries only supports engine='polars'\")\n    return self.timeseries\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Return the internal LazyFrame.</p> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>The internal lazy time series data</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"\n    Return the internal LazyFrame.\n\n    :return: The internal lazy time series data\n    :rtype: pl.LazyFrame\n    \"\"\"\n    return self.timeseries\n</code></pre>"},{"location":"api/math/lazy_timeseries/#atlas.LazyTimeseries.upsample","title":"upsample","text":"<pre><code>upsample(\n    frequency: str | Duration,\n    interpolation_method: str = \"constant\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries\n</code></pre> <p>Upsample the LazyTimeseries to a higher frequency.</p> <p>\u26a0\ufe0f WARNING: This method materializes all data into memory.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>Frequency string (e.g., \"15m\", \"1h\")</p> required <code>interpolation_method</code> <code>str</code> <p>Method to fill missing values</p> <code>'constant'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>Upsampled LazyTimeseries</p> Source code in <code>atlas/math/lazy_timeseries.py</code> <pre><code>def upsample(\n    self,\n    frequency: str | pendulum.Duration,\n    interpolation_method: str = \"constant\",\n    inplace: bool = True,\n) -&gt; LazyTimeseries:\n    \"\"\"\n    Upsample the LazyTimeseries to a higher frequency.\n\n    \u26a0\ufe0f WARNING: This method materializes all data into memory.\n\n    :param frequency: Frequency string (e.g., \"15m\", \"1h\")\n    :type frequency: str | pendulum.Duration\n    :param interpolation_method: Method to fill missing values\n    :type interpolation_method: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Upsampled LazyTimeseries\n    :rtype: LazyTimeseries\n    \"\"\"\n    upsampled_ts = self.collect().upsample(frequency, interpolation_method, inplace=False)\n    lf = upsampled_ts.to_lazy()\n    return self._return(lf, inplace)\n</code></pre>"},{"location":"api/math/scenario_matrix/","title":"Scenario matrix","text":""},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix","title":"atlas.ScenarioMatrix","text":"<pre><code>ScenarioMatrix(\n    matrix: DataFrame\n    | DataFrame\n    | ScenarioMatrix\n    | None = None,\n    timezone: str = \"UTC\",\n)\n</code></pre> <p>               Bases: <code>AbstractScenarioMatrix[DataFrame]</code></p> <p>A container for time-indexed <code>Timeseries</code> data, supporting both eager and lazy operations.</p> <p>This class abstracts over Polars and Pandas DataFrames to provide a uniform way to manage multiple time series, each associated with a unique index or scenario key.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>DataFrame | DataFrame | ScenarioMatrix | None</code> <p>DataFrame containing the matrix data.</p> <code>None</code> <code>timezone</code> <code>str</code> <p>Timezone for the datetime column.</p> <code>'UTC'</code> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __init__(\n    self, matrix: pd.DataFrame | pl.DataFrame | ScenarioMatrix | None = None, timezone: str = \"UTC\"\n) -&gt; None:\n    \"\"\"\n    :param matrix: DataFrame containing the matrix data.\n    :type matrix: pd.DataFrame | pl.DataFrame | ScenarioMatrix\n    :param timezone: Timezone for the datetime column.\n    :type timezone: str\n    \"\"\"\n    check_timezone(timezone)\n    self._check_matrix(matrix)\n\n    self._set_matrix(matrix=matrix, timezone=timezone)\n    self.indexes: list[str] = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: DataFrame\n</code></pre> <p>Returns the ScenarioMatrix DataFrame</p>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.index","title":"index  <code>property</code>","text":"<pre><code>index: list[str]\n</code></pre> <p>Returns the ScenarioMatrix indexes (e.g columns names)</p>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict[str, Any]\n</code></pre> <p>Return the metadata of the timeseries.</p>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the ScenarioMatrix shape</p>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.__contains__","title":"__contains__","text":"<pre><code>__contains__(index: str) -&gt; bool\n</code></pre> <p>Check if an index exists in the matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>The index to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if index exists, False otherwise.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __contains__(self, index: str) -&gt; bool:\n    \"\"\"\n    Check if an index exists in the matrix.\n\n    :param index: The index to check.\n    :type index: Index\n    :return: True if index exists, False otherwise.\n    :rtype: bool\n    \"\"\"\n    return index in self.indexes\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Check equality with another matrix.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>Another matrix instance.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if equal, False otherwise.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object to compare is not a ScenarioMatrix</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"\n    Check equality with another matrix.\n\n    :param other: Another matrix instance.\n    :type other: object\n    :raises TypeError: If the object to compare is not a ScenarioMatrix\n    :return: True if equal, False otherwise.\n    :rtype: bool\n    \"\"\"\n    if not isinstance(other, ScenarioMatrix):\n        raise TypeError(\"Cannot compare with non-ScenarioMatrix object\")\n\n    return self.matrix.equals(other.matrix)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: str) -&gt; Timeseries\n</code></pre> <p>Get a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __getitem__(self, index: str) -&gt; Timeseries:\n    \"\"\"\n    Get a timeseries by index.\n\n    :param index: Index key.\n    :type index: Index\n    :raises KeyError: If the index is not found.\n    :return: The Timeseries object.\n    :rtype: Timeseries\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"No timeseries found for index: {index}\")\n    return Timeseries(self.matrix.select(\"time\", index))\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of timeseries in the matrix.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of elements in the matrix.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"\n    Number of timeseries in the matrix.\n\n    :return: Number of elements in the matrix.\n    :rtype: int\n    \"\"\"\n    return len(self.indexes)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Provide a string representation of the ScenarioMatrix object.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def __repr__(self):\n    \"\"\"Provide a string representation of the ScenarioMatrix object.\"\"\"\n    return f\"ScenarioMatrix : {self.matrix}\"\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute absolute value of all numeric columns.</p> Source code in <code>atlas/math/abstract_scenario_matrix.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute absolute value of all numeric columns.\"\"\"\n    backend = self._get_data()\n    result = backend.select([\"time\"] + [pl.col(c).abs().alias(c) for c in self.index])\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.add","title":"add","text":"<pre><code>add(\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    index: str,\n) -&gt; None\n</code></pre> <p>Add a timeseries to the matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index to add into the matrix</p> required <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries to add.</p> required Source code in <code>atlas/math/matrix.py</code> <pre><code>def add(\n    self,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    index: str,\n) -&gt; None:\n    \"\"\"\n    Add a timeseries to the matrix.\n\n    :param index: Index to add into the matrix\n    :type index: str\n    :param timeseries: Timeseries to add.\n    :type timeseries: Timeseries\n    \"\"\"\n    if index in self.indexes:\n        raise KeyError(f\"Index {index} already exists in the matrix.\")\n    timeseries = Timeseries(timeseries) if not isinstance(timeseries, Timeseries) else timeseries\n\n    self.matrix = self.matrix.join(\n        timeseries.to_frame(engine=\"polars\").rename({\"value\": index}),  # type: ignore[arg-type]\n        on=\"time\",\n        how=\"full\",\n        coalesce=True,\n    )\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.delete","title":"delete","text":"<pre><code>delete(index: str) -&gt; None\n</code></pre> <p>Delete a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key to delete from the matrix</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index is not found.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def delete(self, index: str) -&gt; None:\n    \"\"\"\n    Delete a timeseries by index.\n\n    :param index: Index key to delete from the matrix\n    :type index: str\n    :raises KeyError: If index is not found.\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"No timeseries to delete at index: {index}\")\n\n    self.matrix = self.matrix.drop(index)\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.describe","title":"describe","text":"<pre><code>describe() -&gt; dict[str, Any]\n</code></pre> <p>Get metadata about the ScenarioMatrix.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionnary containing matrix metadata</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def describe(self) -&gt; dict[str, Any]:\n    \"\"\"\n    Get metadata about the ScenarioMatrix.\n\n    :return: A dictionnary containing matrix metadata\n    :rtype: dict[str, Any]\n    \"\"\"\n    return get_metadata_from_frame(self.matrix)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; ScenarioMatrix\n</code></pre> <p>Load a ScenarioMatrix from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file (CSV or Parquet).</p> required <p>Returns:</p> Type Description <code>ScenarioMatrix</code> <p>A ScenarioMatrix object.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; ScenarioMatrix:\n    \"\"\"\n    Load a ScenarioMatrix from a file.\n\n    :param file_path: Path to the file (CSV or Parquet).\n    :type file_path: str | Path\n    :return: A ScenarioMatrix object.\n    :rtype: ScenarioMatrix\n    \"\"\"\n\n    return cls(read_data_file(file_path, filters, separator), timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.get_matrix","title":"get_matrix","text":"<pre><code>get_matrix() -&gt; DataFrame\n</code></pre> <p>Get the matrix data.</p> <p>Returns:</p> Type Description <code>pl.DataFrame</code> <p>The matrix data.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def get_matrix(self) -&gt; pl.DataFrame:\n    \"\"\"\n    Get the matrix data.\n\n    :return: The matrix data.\n    :rtype: pl.DataFrame\n    \"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; Figure\n</code></pre> <p>Generate an interactive Plotly figure for the ScenarioMatrix data with a slider to select indexes.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Plot title</p> <code>'ScenarioMatrix Timeseries Plot'</code> <code>height</code> <code>int</code> <p>Plot height in pixels</p> <code>500</code> <code>width</code> <code>int</code> <p>Plot width in pixels</p> <code>800</code> <code>show_grid</code> <code>bool</code> <p>Whether to show grid lines</p> <code>True</code> <code>line_shape</code> <code>Literal['hv', 'linear', 'spline']</code> <p>Shape of the plot lines</p> <code>'hv'</code> <code>template</code> <code>str</code> <p>Plotly template to use</p> <code>'plotly_white'</code> <p>Returns:</p> Type Description <code>Figure</code> <p>Plotly figure object</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def plot(\n    self,\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; go.Figure:\n    \"\"\"\n    Generate an interactive Plotly figure for the ScenarioMatrix data with a slider to select indexes.\n\n    :param title: Plot title\n    :param height: Plot height in pixels\n    :param width: Plot width in pixels\n    :param show_grid: Whether to show grid lines\n    :param line_shape: Shape of the plot lines\n    :param template: Plotly template to use\n    :return: Plotly figure object\n    \"\"\"\n    df = self.get_matrix()\n    index_columns = self._get_indexes()\n    time_col = \"time\"\n\n    fig = go.Figure()\n\n    for i, idx in enumerate(index_columns):\n        visible = i == 0\n        fig.add_trace(\n            go.Scatter(\n                x=df[time_col],  # type: ignore[arg-type]\n                y=df[idx],  # type: ignore[arg-type]\n                mode=\"lines\",\n                name=idx,\n                line_shape=line_shape,\n                visible=visible,\n            )\n        )\n\n    steps = []\n    for i, idx in enumerate(index_columns):\n        step = {\n            \"method\": \"update\",\n            \"label\": idx,\n            \"args\": [\n                {\"visible\": [j == i for j in range(len(index_columns))]},\n                {\"title\": f\"{title} - {idx}\"},\n            ],\n        }\n        steps.append(step)\n\n    sliders = [\n        {\n            \"active\": 0,\n            \"currentvalue\": {\"prefix\": \"Index: \"},\n            \"pad\": {\"t\": 50},\n            \"steps\": steps,\n        }\n    ]\n\n    fig.update_layout(\n        sliders=sliders,\n        title=title,\n        height=height,\n        width=width,\n        template=template,\n        xaxis={\n            \"title\": \"Time\",\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n        yaxis={\n            \"title\": \"Value\",\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n    )\n\n    return fig\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.replace","title":"replace","text":"<pre><code>replace(\n    index: str,\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n) -&gt; None\n</code></pre> <p>Replace a Timeseries in the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/matrix.py</code> <pre><code>def replace(\n    self,\n    index: str,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n) -&gt; None:\n    \"\"\"\n    Replace a Timeseries in the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    self.delete(index=index)\n    self.add(timeseries=timeseries, index=index)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.select","title":"select","text":"<pre><code>select(index: str) -&gt; Timeseries\n</code></pre> <p>Get a timeseries by index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def select(self, index: str) -&gt; Timeseries:\n    \"\"\"\n    Get a timeseries by index.\n\n    :param index: Index key.\n    :type index: Index\n    :raises KeyError: If the index is not found.\n    :return: The Timeseries object.\n    :rtype: Timeseries\n    \"\"\"\n    return self.__getitem__(index)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | timedelta | Duration,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Change the frequency (timestep) of all scenario time series in the matrix.</p> <p>This method upscales or downscales all scenarios to the specified frequency, similar to the <code>set_frequency</code> method for individual timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | timedelta | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>ScenarioMatrix</code> <p>The resampled scenario matrix, either modified in place or as a new object.</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def set_frequency(self, frequency: str | timedelta | pendulum.Duration, inplace: bool = True) -&gt; Self:\n    \"\"\"\n    Change the frequency (timestep) of all scenario time series in the matrix.\n\n    This method upscales or downscales all scenarios to the specified frequency,\n    similar to the `set_frequency` method for individual timeseries.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled scenario matrix, either modified in place or as a new object.\n    :rtype: ScenarioMatrix\n    \"\"\"\n    if len(self.matrix) == 0:\n        return self if inplace else self.__class__(self.matrix.clone(), self.timezone)\n\n    new_timestep = get_duration(frequency)\n    current_frequency = infer_frequency(self.matrix)\n\n    if new_timestep &gt; current_frequency:\n        df = self._downsample(new_timestep)\n    elif new_timestep &lt; current_frequency:\n        df = self._upsample(new_timestep)\n    else:\n        df = self.matrix.clone() if not inplace else self.matrix\n\n    if inplace:\n        self.matrix = df\n        return self\n    else:\n        return self.__class__(df, self.timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the time series to a file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"\n    Export the time series to a file.\n\n    :param path: Destination file path\n    :type path: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path = str(path)\n    if not path.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    if file_format_lower == \"csv\":\n        self.matrix.write_csv(path, separator=separator)\n    elif file_format_lower == \"parquet\":\n        self.matrix.write_parquet(path)\n    elif file_format_lower == \"pickle\":\n        with open(path, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the ScenarioMatrix to a file with an attribute column.</p> <p>If the file already exists and concatenate is True, the new data will be appended to the existing data.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>attribute</code> <code>str</code> <p>Attribute name to add as a column</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <code>concatenate</code> <code>bool</code> <p>If True, concatenate with existing file data, defaults to True</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"\n    Export the ScenarioMatrix to a file with an attribute column.\n\n    If the file already exists and concatenate is True, the new data will be\n    appended to the existing data.\n\n    :param path: Destination file path\n    :type path: str or Path\n    :param attribute: Attribute name to add as a column\n    :type attribute: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :param concatenate: If True, concatenate with existing file data, defaults to True\n    :type concatenate: bool, optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path_str = str(path)\n    else:\n        path_str = path\n\n    if not path_str.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    df_to_write = self.matrix.clone().insert_column(1, pl.lit(attribute).alias(\"attribute\"))\n\n    if concatenate:\n        path_obj = Path(path_str)\n        if path_obj.exists() and file_format_lower != \"pickle\":\n            try:\n                if file_format_lower == \"csv\":\n                    existing_df = pl.read_csv(path_str, separator=separator, try_parse_dates=True)\n                elif file_format_lower == \"parquet\":\n                    existing_df = pl.read_parquet(path_str)\n\n                df_to_write = pl.concat([existing_df, df_to_write])\n            except Exception:\n                pass\n\n    if file_format_lower == \"csv\":\n        df_to_write.write_csv(path_str, separator=separator)\n    elif file_format_lower == \"parquet\":\n        df_to_write.write_parquet(path_str)\n    elif file_format_lower == \"pickle\":\n        with open(path_str, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.ScenarioMatrix.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Convert the internal Polars DataFrame to a LazyFrame.</p> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>A Polars LazyFrame representation of the time series</p> Source code in <code>atlas/math/matrix.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"\n    Convert the internal Polars DataFrame to a LazyFrame.\n\n    :return: A Polars LazyFrame representation of the time series\n    :rtype: pl.LazyFrame\n    \"\"\"\n    return self.matrix.lazy()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix","title":"atlas.LazyScenarioMatrix","text":"<pre><code>LazyScenarioMatrix(\n    matrix: LazyFrame | LazyScenarioMatrix | ScenarioMatrix,\n    timezone: str = \"UTC\",\n)\n</code></pre> <p>               Bases: <code>AbstractScenarioMatrix[LazyFrame]</code></p> <p>Base class for lazily storing Timeseries-like data indexed by scenario keys or datetimes.</p> <p>Initialize the LazyScenarioMatrix.</p> <p>Parameters:</p> Name Type Description Default <code>matrix</code> <code>LazyFrame | LazyScenarioMatrix | ScenarioMatrix</code> <p>LazyFrame, ScenarioMatrix, or LazyScenarioMatrix object.</p> required <code>timezone</code> <code>str</code> <p>Timezone for the datetime column.</p> <code>'UTC'</code> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __init__(self, matrix: pl.LazyFrame | LazyScenarioMatrix | ScenarioMatrix, timezone: str = \"UTC\") -&gt; None:\n    \"\"\"\n    Initialize the LazyScenarioMatrix.\n\n    :param matrix: LazyFrame, ScenarioMatrix, or LazyScenarioMatrix object.\n    :type matrix: pl.LazyFrame | LazyScenarioMatrix | ScenarioMatrix\n    :param timezone: Timezone for the datetime column.\n    :type timezone: str\n    \"\"\"\n    check_timezone(timezone)\n    self.timezone = timezone\n\n    if isinstance(matrix, LazyScenarioMatrix):\n        self.matrix = matrix.get_matrix()\n        self.timezone = matrix.timezone\n    elif isinstance(matrix, ScenarioMatrix):\n        self.matrix = matrix.to_lazy()\n        self.timezone = matrix.timezone\n    elif isinstance(matrix, pl.LazyFrame):\n        schema = matrix.collect_schema()\n\n        time_columns = [name for name, dtype in schema.items() if dtype.is_temporal()]\n        value_columns = [name for name, dtype in schema.items() if dtype.is_numeric()]\n\n        if len(time_columns) != 1:\n            raise ValueError(\"LazyScenarioMatrix must have exactly one datetime column\")\n\n        if len(time_columns) + len(value_columns) != len(schema):\n            raise ValueError(\"LazyScenarioMatrix must have N columns one for datetime and N-1 for numerical values\")\n\n        self.matrix = (\n            matrix.rename({time_columns[0]: \"time\"})\n            .with_columns(pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)))\n            .sort(\"time\")\n        )\n    else:\n        raise TypeError(\"LazyScenarioMatrix requires a LazyFrame, ScenarioMatrix, or LazyScenarioMatrix\")\n\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: LazyFrame\n</code></pre> <p>Returns the underlying LazyFrame.</p>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.index","title":"index  <code>property</code>","text":"<pre><code>index: list[str]\n</code></pre> <p>Returns the ScenarioMatrix indexes (e.g columns names)</p>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.lazyframe","title":"lazyframe  <code>property</code>","text":"<pre><code>lazyframe: LazyFrame\n</code></pre> <p>Returns the ScenarioMatrix DataFrame</p>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict\n</code></pre> <p>Return the metadata of the matrix.</p>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the ScenarioMatrix shape.</p>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.__contains__","title":"__contains__","text":"<pre><code>__contains__(index: str) -&gt; bool\n</code></pre> <p>Check if an index exists in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __contains__(self, index: str) -&gt; bool:\n    \"\"\"Check if an index exists in the matrix.\"\"\"\n    return index in self.indexes\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Check equality with another matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check equality with another matrix.\"\"\"\n    if not isinstance(other, LazyScenarioMatrix):\n        raise TypeError(\"Cannot compare with non-LazyScenarioMatrix object\")\n    return self.matrix.collect().equals(other.matrix.collect())\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.__getitem__","title":"__getitem__","text":"<pre><code>__getitem__(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a timeseries by index.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __getitem__(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"Get a timeseries by index.\"\"\"\n    return self.select(index)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of timeseries in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Number of timeseries in the matrix.\"\"\"\n    return len(self.indexes)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>String representation of the ScenarioMatrix</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def __repr__(self):\n    \"\"\"String representation of the ScenarioMatrix\"\"\"\n    return f\"LazyScenarioMatrix with schema : {self.matrix.collect_schema()}\"\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute absolute value of all numeric columns.</p> Source code in <code>atlas/math/abstract_scenario_matrix.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute absolute value of all numeric columns.\"\"\"\n    backend = self._get_data()\n    result = backend.select([\"time\"] + [pl.col(c).abs().alias(c) for c in self.index])\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.add","title":"add","text":"<pre><code>add(\n    timeseries: LazyTimeseries\n    | LazyFrame\n    | Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    index: str,\n) -&gt; None\n</code></pre> <p>Add a timeseries to the lazy matrix.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>LazyTimeseries | LazyFrame | Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries to add.</p> required <code>index</code> <code>str</code> <p>Index to add into the matrix</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index already exists in the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def add(\n    self,\n    timeseries: LazyTimeseries | pl.LazyFrame | Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    index: str,\n) -&gt; None:\n    \"\"\"\n    Add a timeseries to the lazy matrix.\n\n    :param timeseries: Timeseries to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | dict[str, list]\n    :param index: Index to add into the matrix\n    :type index: str\n    :raises KeyError: If index already exists in the matrix.\n    \"\"\"\n    if index in self.indexes:\n        raise KeyError(f\"Index {index} already exists in the matrix.\")\n\n    if isinstance(timeseries, LazyTimeseries):\n        lazy_ts = timeseries.lazyframe\n    elif isinstance(timeseries, pl.LazyFrame):\n        lazy_ts = timeseries\n    else:\n        lazy_ts = Timeseries(timeseries).to_frame(engine=\"polars\").rename({\"value\": index}).lazy()  # type: ignore [operator]\n\n    # Join with the existing matrix\n    self.matrix = self.matrix.join(\n        lazy_ts,\n        on=\"time\",\n        how=\"full\",\n        coalesce=True,\n    )\n\n    # Update indexes\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.collect","title":"collect","text":"<pre><code>collect() -&gt; ScenarioMatrix\n</code></pre> <p>Collect the lazy frame and return a regular ScenarioMatrix object.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def collect(self) -&gt; ScenarioMatrix:\n    \"\"\"Collect the lazy frame and return a regular ScenarioMatrix object.\"\"\"\n    return ScenarioMatrix(self.matrix.collect(), timezone=self.timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.delete","title":"delete","text":"<pre><code>delete(index: str) -&gt; None\n</code></pre> <p>Delete a timeseries by index from the lazy matrix.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key to delete from the matrix</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>If index is not found.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def delete(self, index: str) -&gt; None:\n    \"\"\"\n    Delete a timeseries by index from the lazy matrix.\n\n    :param index: Index key to delete from the matrix\n    :type index: str\n    :raises KeyError: If index is not found.\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"No timeseries to delete at index: {index}\")\n\n    self.matrix = self.matrix.drop(index)\n\n    self.indexes = self._get_indexes()\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.describe","title":"describe","text":"<pre><code>describe() -&gt; dict\n</code></pre> <p>Get metadata about the matrix.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def describe(self) -&gt; dict:\n    \"\"\"Get metadata about the matrix.\"\"\"\n\n    return get_metadata_from_frame(self.matrix.collect())\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyScenarioMatrix\n</code></pre> <p>Load a LazyScenarioMatrix from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file</p> required <code>separator</code> <code>str</code> <p>CSV separator (if applicable)</p> <code>';'</code> <code>timezone</code> <code>str</code> <p>Timezone to apply</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>LazyScenarioMatrix</code> <p>LazyScenarioMatrix instance</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; LazyScenarioMatrix:\n    \"\"\"\n    Load a LazyScenarioMatrix from a file.\n\n    :param file_path: Path to the file\n    :param separator: CSV separator (if applicable)\n    :param timezone: Timezone to apply\n    :return: LazyScenarioMatrix instance\n    \"\"\"\n\n    return cls(scan_data_file(file_path, filters, separator), timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.get_matrix","title":"get_matrix","text":"<pre><code>get_matrix() -&gt; LazyFrame\n</code></pre> <p>Return internal lazy frame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def get_matrix(self) -&gt; pl.LazyFrame:\n    \"\"\"Return internal lazy frame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n)\n</code></pre> <p>Generate an interactive Plotly figure.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def plot(\n    self,\n    title: str = \"ScenarioMatrix Timeseries Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_shape: str = \"hv\",\n    template: str = \"plotly_white\",\n):\n    \"\"\"Generate an interactive Plotly figure.\"\"\"\n    return self.collect().plot(title, height, width, show_grid, line_shape, template)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.replace","title":"replace","text":"<pre><code>replace(\n    index: str,\n    timeseries: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n) -&gt; None\n</code></pre> <p>Replace a Timeseries in the matrix and keep indexes sorted.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Timeseries data to add.</p> required <code>index</code> <code>str</code> <p>Datetime key for the new forecast.</p> required Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def replace(\n    self,\n    index: str,\n    timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n) -&gt; None:\n    \"\"\"\n    Replace a Timeseries in the matrix and keep indexes sorted.\n\n    :param timeseries: Timeseries data to add.\n    :type timeseries: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict\n    :param index: Datetime key for the new forecast.\n    :type index: str | datetime\n    \"\"\"\n    self.delete(index=index)\n    self.add(timeseries=timeseries, index=index)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.select","title":"select","text":"<pre><code>select(index: str) -&gt; LazyTimeseries\n</code></pre> <p>Get a lazy timeseries by index.</p> <p>This operation does not materialize the data, keeping it lazy.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>str</code> <p>Index key.</p> required <p>Returns:</p> Type Description <code>LazyTimeseries</code> <p>The LazyTimeseries object.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If the index is not found.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def select(self, index: str) -&gt; LazyTimeseries:\n    \"\"\"\n    Get a lazy timeseries by index.\n\n    This operation does not materialize the data, keeping it lazy.\n\n    :param index: Index key.\n    :type index: str\n    :raises KeyError: If the index is not found.\n    :return: The LazyTimeseries object.\n    :rtype: LazyTimeseries\n    \"\"\"\n    if index not in self.indexes:\n        raise KeyError(f\"Index {index} not found in the matrix.\")\n\n    # Select time and the specified column, rename to 'value'\n    lazy_ts = self.matrix.select([\"time\", index]).rename({index: \"value\"})\n    return LazyTimeseries(lazy_ts, timezone=self.timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | timedelta | Duration,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Change the frequency (timestep) of all scenario time series in the lazy matrix.</p> <p>This method collects the lazy frame, applies set_frequency, then converts back to lazy.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | timedelta | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>LazyScenarioMatrix</code> <p>The resampled lazy scenario matrix, either modified in place or as a new object.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def set_frequency(self, frequency: str | timedelta | pendulum.Duration, inplace: bool = True) -&gt; Self:\n    \"\"\"\n    Change the frequency (timestep) of all scenario time series in the lazy matrix.\n\n    This method collects the lazy frame, applies set_frequency, then converts back to lazy.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled lazy scenario matrix, either modified in place or as a new object.\n    :rtype: LazyScenarioMatrix\n    \"\"\"\n    resampled_sm = self.collect().set_frequency(frequency, inplace=False)\n    if inplace:\n        self.matrix = resampled_sm.to_lazy()\n        return self\n    else:\n        return self.__class__(resampled_sm.to_lazy(), timezone=self.timezone)\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the matrix to a file.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"Export the matrix to a file.\"\"\"\n    self.collect().to_file(path, file_format, separator)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the matrix to a file with an attribute column.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: str = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"Export the matrix to a file with an attribute column.\"\"\"\n    self.collect().to_file_with_attribute(path, attribute, file_format, separator, concatenate)  # type: ignore[arg-type]\n</code></pre>"},{"location":"api/math/scenario_matrix/#atlas.LazyScenarioMatrix.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Return the internal LazyFrame.</p> Source code in <code>atlas/math/lazy_matrix.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"Return the internal LazyFrame.\"\"\"\n    return self.matrix\n</code></pre>"},{"location":"api/math/timeseries/","title":"Timeseries","text":""},{"location":"api/math/timeseries/#atlas.Timeseries","title":"atlas.Timeseries","text":"<pre><code>Timeseries(\n    timeseries: DataFrame\n    | Timeseries\n    | DataFrame\n    | TimeseriesDict\n    | None = None,\n    timezone: str = \"UTC\",\n)\n</code></pre> <p>               Bases: <code>AbstractTimeseries[DataFrame]</code></p> <p>A flexible and efficient time series class using a Polars backend.</p> <p>The Timeseries class provides a unified interface for handling, analyzing, and visualizing time series data. It supports a variety of input formats (Polars DataFrame, Pandas DataFrame, dictionary, or another Timeseries instance) and ensures robust handling of time zones and interpolation methods.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>DataFrame | Timeseries | DataFrame | TimeseriesDict | None</code> <p>The input Timeseries data.</p> <code>None</code> <code>timezone</code> <code>str</code> <p>Timezone string used to convert datetime values, defaults to \"UTC\"</p> <code>'UTC'</code> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __init__(\n    self,\n    timeseries: pl.DataFrame | Timeseries | pd.DataFrame | TimeseriesDict | None = None,\n    timezone: str = \"UTC\",\n) -&gt; None:\n    \"\"\"\n    :param timeseries: The input Timeseries data.\n    :type timeseries: pl.DataFrame or Timeseries\n    :param timezone: Timezone string used to convert datetime values, defaults to \"UTC\"\n    :type timezone: str, optional\n    \"\"\"\n    check_timezone(timezone)\n    self.timezone: str = timezone\n\n    self._check_timeseries(timeseries)\n    self._set_timeseries(timeseries, timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.dataframe","title":"dataframe  <code>property</code>","text":"<pre><code>dataframe: DataFrame\n</code></pre> <p>Returns the Timeseries DataFrame</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.index","title":"index  <code>property</code>","text":"<pre><code>index: list[datetime]\n</code></pre> <p>Returns the Timeseries indexes</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.metadata","title":"metadata  <code>property</code>","text":"<pre><code>metadata: dict[str, Any]\n</code></pre> <p>Return the metadata of the timeseries.</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int]\n</code></pre> <p>Returns the Timeseries shape</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.timestep","title":"timestep  <code>property</code>","text":"<pre><code>timestep: Duration\n</code></pre> <p>Return the frequency string of the timeseries index.</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.values","title":"values  <code>property</code>","text":"<pre><code>values: list[float]\n</code></pre> <p>Returns the Timeseries values</p>"},{"location":"api/math/timeseries/#atlas.Timeseries.__add__","title":"__add__","text":"<pre><code>__add__(other: float | Timeseries) -&gt; Timeseries\n</code></pre> <p>Add all numeric columns by a scalar or timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | Timeseries</code> <p>Other timeseries or scalar to add to value of the current Timeseries</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries where a scalar or another Timeseries are added to all numeric columns</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object is not a timeseries or a float</p> <code>ValueError</code> <p>If operation could not be done</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __add__(self, other: float | Timeseries) -&gt; Timeseries:\n    \"\"\"Add all numeric columns by a scalar or timeseries.\n\n    :param other: Other timeseries or scalar to add to value of the current Timeseries\n    :type other: float | Timeseries\n    :raises TypeError: If the object is not a timeseries or a float\n    :raises ValueError: If operation could not be done\n    :return: The Timeseries where a scalar or another Timeseries are added to all numeric columns\n    :rtype: Timeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        df = self.timeseries.with_columns(pl.selectors.numeric().add(other))\n    elif isinstance(other, Timeseries):\n        if self.frequency != other.frequency:\n            raise ValueError(\"Could not perform addition on Timeseries because frequencies don't match\")\n        elif not other.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).all():\n            raise ValueError(\n                \"Could not perform addition on Timeseries because indexes of Timeseries to add are \"\n                \"not in current Timeseries\"\n            )\n        else:\n            my_ts = Timeseries(self)\n\n        df = (\n            my_ts._join(\n                other=other,\n                how=\"full\",\n            )\n            .fill_null(0)\n            .with_columns(pl.col(\"value\").add(pl.col(\"value_right\")).alias(\"value\"))\n            .select(\"time\", \"value\")\n        )\n    else:\n        raise TypeError(\"Timeseries can't perform addition\")\n\n    return Timeseries(df, self.timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__contains__","title":"__contains__","text":"<pre><code>__contains__(item: datetime | str | DateTime) -&gt; bool\n</code></pre> <p>Check if a temporal index exists in the Timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>datetime | str | DateTime</code> <p>Datetime to check for existence</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the datetime exists in the Timeseries index, False otherwise</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __contains__(self, item: datetime | str | pendulum.DateTime) -&gt; bool:\n    \"\"\"Check if a temporal index exists in the Timeseries.\n\n    :param item: Datetime to check for existence\n    :type item: datetime or str or pendulum.DateTime\n    :return: True if the datetime exists in the Timeseries index, False otherwise\n    :rtype: bool\n    \"\"\"\n    try:\n        dt = build_datetime(item).in_tz(self.timezone)\n        return self.timeseries.filter(pl.col(\"time\") == dt).height &gt; 0\n    except Exception:\n        return False\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__eq__","title":"__eq__","text":"<pre><code>__eq__(other: object) -&gt; bool\n</code></pre> <p>Implement the equality between two Timeseries:</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>The Polars DataFrame to compare with</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the DataFrames are equal, False otherwise</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If the object to compare is not a Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"\n    Implement the equality between two Timeseries:\n\n    :param other: The Polars DataFrame to compare with\n    :type other: pl.DataFrame\n    :raises NotImplementedError: If the object to compare is not a Timeseries\n    :return: True if the DataFrames are equal, False otherwise\n    :rtype: bool\n    \"\"\"\n    if isinstance(other, Timeseries):\n        other = other.dataframe\n        return self.timeseries.equals(other)\n    raise TypeError(\"Comparison with non-Timeseries objects is not supported\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Return the number of rows in the Timeseries.</p> <p>Returns:</p> Type Description <code>bool</code> <p>Number of rows in the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __len__(self) -&gt; int:\n    \"\"\"Return the number of rows in the Timeseries.\n\n    :return: Number of rows in the Timeseries\n    :rtype: bool\n    \"\"\"\n    return self.timeseries.height\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__mul__","title":"__mul__","text":"<pre><code>__mul__(other: float | Timeseries) -&gt; Timeseries\n</code></pre> <p>Multiply all numeric columns by a scalar or another Timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | Timeseries</code> <p>Other timeseries or scalar to multiply the value of he current Timeseries</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries where all numeric columns are multiplied by a scalar or another Timeseries</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object is not a timeseries or a float</p> <code>ValueError</code> <p>If operation could not be done</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __mul__(self, other: float | Timeseries) -&gt; Timeseries:\n    \"\"\"Multiply all numeric columns by a scalar or another Timeseries.\n\n    :param other: Other timeseries or scalar to multiply the value of he current Timeseries\n    :type other: float | Timeseries\n    :raises TypeError: If the object is not a timeseries or a float\n    :raises ValueError: If operation could not be done\n    :return: The Timeseries where all numeric columns are multiplied by a scalar or another Timeseries\n    :rtype: Timeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        df = self.timeseries.with_columns(pl.selectors.numeric().mul(other))\n    elif isinstance(other, Timeseries):\n        if self.frequency != other.frequency:\n            raise ValueError(\"Could not perform multiplication on Timeseries because frequencies don't match\")\n        elif not other.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).all():\n            raise ValueError(\n                \"Could not perform multiplication on Timeseries because indexes of Timeseries to add \"\n                \"are not in current Timeseries\"\n            )\n        else:\n            my_ts = Timeseries(self)\n\n        df = (\n            my_ts._join(\n                other=other,\n                how=\"full\",\n            )\n            .fill_null(1)\n            .with_columns(pl.col(\"value\").mul(pl.col(\"value_right\")).alias(\"value\"))\n            .select(\"time\", \"value\")\n        )\n    else:\n        raise TypeError(\"Timeseries can't be multiplied\")\n\n    return Timeseries(df, self.timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__repr__","title":"__repr__","text":"<pre><code>__repr__()\n</code></pre> <p>Provide a string representation of the Timeseries object.</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __repr__(self):\n    \"\"\"Provide a string representation of the Timeseries object.\"\"\"\n    return f\"Timeseries : {self.timeseries}\"\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__sub__","title":"__sub__","text":"<pre><code>__sub__(other: float | Timeseries) -&gt; Timeseries\n</code></pre> <p>Subtract all numeric columns by a scalar or timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | Timeseries</code> <p>Other timeseries or scalar to subtract to the value the current Timeseries</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries where a scalar or another Timeseries are subtract to all numeric columns</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object is not a timeseries or a float</p> <code>ValueError</code> <p>If operation could not be done</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __sub__(self, other: float | Timeseries) -&gt; Timeseries:\n    \"\"\"Subtract all numeric columns by a scalar or timeseries.\n\n    :param other: Other timeseries or scalar to subtract to the value the current Timeseries\n    :type other: float | Timeseries\n    :raises TypeError: If the object is not a timeseries or a float\n    :raises ValueError: If operation could not be done\n    :return: The Timeseries where a scalar or another Timeseries are subtract to all numeric columns\n    :rtype: Timeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        df = self.timeseries.with_columns(pl.selectors.numeric().sub(other))\n    elif isinstance(other, Timeseries):\n        if self.frequency != other.frequency:\n            raise ValueError(\"Could not perform subtraction on Timeseries because frequencies don't match\")\n        elif not other.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).all():\n            raise ValueError(\n                \"Could not perform subtraction on Timeseries because indexes of Timeseries to add are \"\n                \"not in current Timeseries\"\n            )\n        else:\n            my_ts = Timeseries(self)\n\n        df = (\n            my_ts._join(\n                other=other,\n                how=\"full\",\n            )\n            .fill_null(0)\n            .with_columns(pl.col(\"value\").sub(pl.col(\"value_right\")).alias(\"value\"))\n            .select(\"time\", \"value\")\n        )\n    else:\n        raise TypeError(\"Timeseries can't perform subtraction\")\n\n    return Timeseries(df, self.timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.__truediv__","title":"__truediv__","text":"<pre><code>__truediv__(other: float | Timeseries) -&gt; Timeseries\n</code></pre> <p>Divide all numeric columns by a scalar or timeseries.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float | Timeseries</code> <p>Other timeseries or scalar to divide to the value the current Timeseries</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>The Timeseries where all numeric columns are divided by a scalar or another Timeseries</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the object is not a timeseries or a float</p> <code>ValueError</code> <p>If operation could not be done</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def __truediv__(self, other: float | Timeseries) -&gt; Timeseries:\n    \"\"\"Divide all numeric columns by a scalar or timeseries.\n\n    :param other: Other timeseries or scalar to divide to the value the current Timeseries\n    :type other: float | Timeseries\n    :raises TypeError: If the object is not a timeseries or a float\n    :raises ValueError: If operation could not be done\n    :return: The Timeseries where all numeric columns are divided by a scalar or another Timeseries\n    :rtype: Timeseries\n    \"\"\"\n    if isinstance(other, int | float):\n        if other == 0:\n            raise ZeroDivisionError(\"Division by zero is not allowed\")\n        df = self.timeseries.with_columns(pl.selectors.numeric().truediv(other))\n    elif isinstance(other, Timeseries):\n        if self.frequency != other.frequency:\n            raise ValueError(\"Could not perform division on Timeseries because frequencies don't match\")\n        elif not other.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).all():\n            raise ValueError(\n                \"Could not perform division on Timeseries because indexes of Timeseries to add are \"\n                \"not in current Timeseries\"\n            )\n        elif (other.dataframe[\"value\"] == 0).any():\n            raise ValueError(\"Could not perform division on Timeseries because zero values are present\")\n        else:\n            my_ts = Timeseries(self)\n\n        df = (\n            my_ts._join(\n                other=other,\n                how=\"full\",\n            )\n            .fill_null(1)\n            .with_columns(pl.col(\"value\").truediv(pl.col(\"value_right\")).alias(\"value\"))\n            .select(\"time\", \"value\")\n        )\n    else:\n        raise TypeError(\"Timeseries can't be divided\")\n\n    return Timeseries(df, self.timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.abs","title":"abs","text":"<pre><code>abs(inplace: bool = True) -&gt; Self\n</code></pre> <p>Compute the absolute value of each timestamp</p> <p>Parameters:</p> Name Type Description Default <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The timeseries with absolute values</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def abs(self, inplace: bool = True) -&gt; Self:\n    \"\"\"Compute the absolute value of each timestamp\n\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: The timeseries with absolute values\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n    result = backend.with_columns(pl.col(\"value\").abs())\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.add_index","title":"add_index","text":"<pre><code>add_index(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Add index to the Timeseries based on an index and a value</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to add</p> required <code>value</code> <code>float</code> <p>Value to add</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the added index</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the time is in the current Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def add_index(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Add index to the Timeseries based on an index and a value\n\n    :param time: Datetime to add\n    :type time: datetime or str\n    :param value: Value to add\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If the time is in the current Timeseries\n    :return: Timeseries with the added index\n    :rtype: Timeseries\n    \"\"\"\n    dt: pendulum.DateTime = build_datetime(time, date_format).in_tz(self.timezone)\n\n    if len(self.timeseries) == 0:\n        raise ValueError(\"Timeseries should not be empty\")\n\n    if dt in self.dataframe[\"time\"]:\n        raise ValueError(\n            \"Could not add indexes on Timeseries because some indexes to add are not present in Timeseries\"\n        )\n\n    new_row = pl.DataFrame({\"time\": [dt], \"value\": [value]}).with_columns(\n        pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)),\n        pl.col(\"value\").cast(pl.Float64()),\n    )\n    df = pl.concat([self.timeseries, new_row])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.add_indexes","title":"add_indexes","text":"<pre><code>add_indexes(\n    other: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Add indexes to the Timeseries based on another timeseries</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>Other timeseries with indexes / values to add to the current Timeseries</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the added indexes</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If frequency doesn't match or at least one time of the other Timeseries is in the current one</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def add_indexes(\n    self,\n    other: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Add indexes to the Timeseries based on another timeseries\n\n    :param other: Other timeseries with indexes / values to add to the current Timeseries\n    :type other: Timeseries\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If frequency doesn't match or at least one time of the other Timeseries is in the\n    current one\n    :return: Timeseries with the added indexes\n    :rtype: Timeseries\n    \"\"\"\n    other = Timeseries(other)\n    if len(self.timeseries) == 0:\n        other_df = other.dataframe\n        return self._return(other_df, inplace)\n\n    other_ts = Timeseries(other)\n    if self.frequency != other_ts.frequency:\n        raise ValueError(\"Could not perform add indexes on Timeseries because frequency does not match\")\n    if other_ts.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).any():\n        raise ValueError(\n            \"Could not add indexes on Timeseries because some indexes to add are not present in Timeseries\"\n        )\n\n    df = pl.concat([self.timeseries, other_ts.dataframe])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.collect","title":"collect","text":"<pre><code>collect() -&gt; Timeseries\n</code></pre> <p>Return self (no-op for eager Timeseries).</p> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Self</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def collect(self) -&gt; Timeseries:\n    \"\"\"\n    Return self (no-op for eager Timeseries).\n\n    :return: Self\n    :rtype: Timeseries\n    \"\"\"\n    return self\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.describe","title":"describe","text":"<pre><code>describe() -&gt; dict[str, Any]\n</code></pre> <p>Get metadata about the timeseries.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary containing timeseries metadata</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def describe(self) -&gt; dict[str, Any]:\n    \"\"\"\n    Get metadata about the timeseries.\n\n    :return: A dictionary containing timeseries metadata\n    :rtype: dict[str, Any]\n    \"\"\"\n    return get_metadata_from_frame(self.timeseries)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.filter","title":"filter","text":"<pre><code>filter(\n    item: list[datetime]\n    | list[DateTime]\n    | list[str]\n    | datetime\n    | DateTime\n    | str,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Filter the Timeseries based on a list of datetime.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>list[datetime] | list[DateTime] | list[str] | datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>Filtered Timeseries</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def filter(\n    self,\n    item: list[datetime] | list[pendulum.DateTime] | list[str] | datetime | pendulum.DateTime | str,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"\n    Filter the Timeseries based on a list of datetime.\n\n    :param item: Datetime to filter the Timeseries\n    :type item: list[datetime] or datetime or pendulum.DateTime or str\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Filtered Timeseries\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n\n    if isinstance(item, list):\n        item = [build_datetime(i, date_format=date_format).in_tz(self.timezone) for i in item]\n        filtered = backend.filter(pl.col(\"time\").is_in(item))\n    else:\n        date = build_datetime(item, date_format=date_format).in_tz(self.timezone)\n        filtered = backend.filter(pl.col(\"time\") == date)\n\n    return self._return(filtered, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.first_date","title":"first_date","text":"<pre><code>first_date() -&gt; DateTime | None\n</code></pre> <p>Return the first date in the Timeseries index.</p> <p>Returns:</p> Type Description <code>DateTime | None</code> <p>The first date in the Timeseries index</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def first_date(self) -&gt; pendulum.DateTime | None:\n    \"\"\"\n    Return the first date in the Timeseries index.\n\n    :return: The first date in the Timeseries index\n    :rtype: DateTime or None\n    \"\"\"\n    if len(self.timeseries) &gt; 0:\n        return cast(pendulum.DateTime, pendulum.instance(self.timeseries.select(\"time\").head(1).item()))\n    return None\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.from_dataframe","title":"from_dataframe  <code>classmethod</code>","text":"<pre><code>from_dataframe(\n    dataframe: DataFrame | DataFrame, timezone=\"UTC\"\n) -&gt; Timeseries\n</code></pre> <p>Create a Timeseries object from a dataframe-like object.</p> <p>Parameters:</p> Name Type Description Default <code>dataframe</code> <code>DataFrame | DataFrame</code> <p>The input dataframe</p> required <code>timezone</code> <code>(str, optional)</code> <p>The timezone of the Timeseries, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>The timeseries instantiated from the dataframe-like object</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>@classmethod\ndef from_dataframe(\n    cls,\n    dataframe: pl.DataFrame | pd.DataFrame,\n    timezone=\"UTC\",\n) -&gt; Timeseries:\n    \"\"\"Create a Timeseries object from a dataframe-like object.\n\n    :param dataframe: The input dataframe\n    :type dataframe: pl.DataFrame | Timeseries | pd.DataFrame\n    :param timezone: The timezone of the Timeseries, defaults to \"UTC\"\n    :type timezone: str, optional\n    :return: The timeseries instantiated from the dataframe-like object\n    :rtype: Timeseries\n    \"\"\"\n    if not isinstance(dataframe, pl.DataFrame | pd.DataFrame | dict):\n        raise TypeError(\"Input has to be a dataframe-like object.\")\n\n    return cls(dataframe, timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; Timeseries\n</code></pre> <p>Load a Timeseries object from a file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the file</p> required <code>timezone</code> <code>str</code> <p>Timezone string used to convert datetime values, defaults to \"UTC\"</p> <code>'UTC'</code> <code>filters</code> <code>tuple[str, str] | None</code> <p>Filters columns, defaults to \";\"</p> <code>None</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Loaded Timeseries object</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file format is not supported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>@classmethod\ndef from_file(\n    cls,\n    file_path: str | Path,\n    timezone: str = \"UTC\",\n    filters: tuple[str, str] | None = None,\n    separator: str = \";\",\n) -&gt; Timeseries:\n    \"\"\"\n    Load a Timeseries object from a file.\n\n    :param file_path: Path to the file\n    :type file_path: str or Path\n    :param timezone: Timezone string used to convert datetime values, defaults to \"UTC\"\n    :type timezone: str, optional\n    :param filters: Filters columns, defaults to \";\"\n    :type filters: tuple[str, str] or None, optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :raises ValueError: If file format is not supported\n    :return: Loaded Timeseries object\n    :rtype: Timeseries\n    \"\"\"\n\n    return cls(read_data_file(file_path, filters, separator), timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.from_index","title":"from_index  <code>classmethod</code>","text":"<pre><code>from_index(\n    start_date: str | datetime | DateTime,\n    frequency: str | timedelta | Duration,\n    end_date: str | datetime | DateTime,\n    default_value: list[float] | float = 0,\n    date_format=\"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; Timeseries\n</code></pre> <p>Create a Timeseries from a time range and a default value or list of values.</p> <p>Parameters:</p> Name Type Description Default <code>start_date</code> <code>str | datetime | DateTime</code> <p>Start date of the timeseries</p> required <code>frequency</code> <code>str | timedelta | Duration</code> <p>Frequency of the timeseries (e.g., \"1h\", \"15m\")</p> required <code>end_date</code> <code>str | datetime | DateTime</code> <p>End date of the timeseries</p> required <code>default_value</code> <code>list[float] | float</code> <p>A scalar value or a list of values to fill the timeseries</p> <code>0</code> <code>date_format</code> <code>(str, optional)</code> <p>Format to interpret date strings, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>timezone</code> <code>str</code> <p>Timezone string, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>A Timeseries object with the specified index and values</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If default_value is a list with length mismatch</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>@classmethod\ndef from_index(\n    cls,\n    start_date: str | datetime | pendulum.DateTime,\n    frequency: str | timedelta | pendulum.Duration,\n    end_date: str | datetime | pendulum.DateTime,\n    default_value: list[float] | float = 0,\n    date_format=\"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; Timeseries:\n    \"\"\"\n    Create a Timeseries from a time range and a default value or list of values.\n\n    :param start_date: Start date of the timeseries\n    :type start_date: str or datetime or pendulum.DateTime\n    :param frequency: Frequency of the timeseries (e.g., \"1h\", \"15m\")\n    :type frequency: str or pendulum.Duration\n    :param end_date: End date of the timeseries\n    :type end_date: str or datetime or pendulum.DateTime\n    :param default_value: A scalar value or a list of values to fill the timeseries\n    :type default_value: list[float] or float, optional\n    :param date_format: Format to interpret date strings, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param timezone: Timezone string, defaults to \"UTC\"\n    :type timezone: str, optional\n    :raises ValueError: If default_value is a list with length mismatch\n    :return: A Timeseries object with the specified index and values\n    :rtype: Timeseries\n    \"\"\"\n\n    start = build_datetime(start_date, date_format).in_tz(timezone)\n    end = build_datetime(end_date, date_format).in_tz(timezone)\n\n    datetimes = generate_datetimes(start, end, frequency, timezone)\n\n    if isinstance(default_value, list):\n        if len(default_value) != len(datetimes):\n            raise ValueError(\n                f\"Values  passed is of size {len(default_value)} when datetimes generated is of size {len(datetimes)}\"\n            )\n        else:\n            df = pl.DataFrame(\n                {\"time\": datetimes, \"value\": default_value},\n                schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n            )\n    elif isinstance(default_value, float | int):\n        df = pl.DataFrame(\n            {\"time\": datetimes, \"value\": [default_value] * len(datetimes)},\n            schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n        )\n\n    return cls(df, timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.from_timeseries","title":"from_timeseries  <code>classmethod</code>","text":"<pre><code>from_timeseries(\n    timeseries: Timeseries,\n    default_value: float | None = None,\n) -&gt; Timeseries\n</code></pre> <p>Create a Timeseries from another, using its structure.</p> <p>Parameters:</p> Name Type Description Default <code>timeseries</code> <code>Timeseries</code> <p>The input timeseries object</p> required <code>default_value</code> <code>float | None</code> <p>default value to pass to all timestamp values, defaults to None</p> <code>None</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>The timeseries object instantiated</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>@classmethod\ndef from_timeseries(cls, timeseries: Timeseries, default_value: float | None = None) -&gt; Timeseries:\n    \"\"\"Create a Timeseries from another, using its structure.\n\n    :param timeseries: The input timeseries object\n    :type timeseries: Timeseries\n    :param default_value: default value to pass to all timestamp values, defaults to None\n    :type default_value: float | None, optional\n    :return: The timeseries object instantiated\n    :rtype: Timeseries\n    \"\"\"\n\n    if not isinstance(timeseries, Timeseries):\n        raise TypeError(\"Input has to be a timeseries object, if using a dataframe, use 'from_dataframe' \")\n    if default_value is not None:\n        df = timeseries.dataframe.with_columns(pl.lit(default_value).alias(\"value\"))\n        return cls(df, timezone=timeseries.timezone)\n    else:\n        return cls(timeseries)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.from_values","title":"from_values  <code>classmethod</code>","text":"<pre><code>from_values(\n    start_date: str | datetime | DateTime,\n    frequency: str | timedelta | Duration,\n    values: list[float],\n    date_format=\"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; Timeseries\n</code></pre> <p>Create a Timeseries from start date, frequency and a list of values.</p> <p>Parameters:</p> Name Type Description Default <code>start_date</code> <code>str | datetime | DateTime</code> <p>Start date of the timeseries</p> required <code>frequency</code> <code>str | timedelta | Duration</code> <p>Frequency of the timeseries (e.g., \"1h\", \"15m\")</p> required <code>values</code> <code>list[float]</code> <p>List of values corresponding to the time intervals</p> required <code>timezone</code> <code>str</code> <p>Timezone string, defaults to \"UTC\"</p> <code>'UTC'</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>A Timeseries object with the specified parameters</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file there is no value to insert in the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>@classmethod\ndef from_values(\n    cls,\n    start_date: str | datetime | pendulum.DateTime,\n    frequency: str | timedelta | pendulum.Duration,\n    values: list[float],\n    date_format=\"YYYY-MM-DD HH:mm:ss\",\n    timezone: str = \"UTC\",\n) -&gt; Timeseries:\n    \"\"\"\n    Create a Timeseries from start date, frequency and a list of values.\n\n    :param start_date: Start date of the timeseries\n    :type start_date: str or datetime or pendulum.DateTime\n    :param frequency: Frequency of the timeseries (e.g., \"1h\", \"15m\")\n    :type frequency: str or pendulum.Duration\n    :param values: List of values corresponding to the time intervals\n    :type values: list[float]\n    :param timezone: Timezone string, defaults to \"UTC\"\n    :type timezone: str, optional\n    :raises ValueError: If file there is no value to insert in the Timeseries\n    :return: A Timeseries object with the specified parameters\n    :rtype: Timeseries\n    \"\"\"\n    if len(values) &lt; 2:\n        raise ValueError(\"Timeseries must contains at least 2 values\")\n\n    start = build_datetime(start_date, date_format).in_tz(timezone)\n    end = build_datetime(start + (len(values) - 1) * get_duration(frequency)).in_tz(timezone)\n\n    datetimes = generate_datetimes(start, end, frequency, timezone)\n\n    df = pl.DataFrame(\n        {\"time\": datetimes, \"value\": values},\n        schema={\"time\": pl.Datetime(\"us\", time_zone=timezone), \"value\": pl.Float64()},\n    )\n\n    return cls(df, timezone)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.get_value","title":"get_value","text":"<pre><code>get_value(\n    datetime: str | datetime | DateTime,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; float\n</code></pre> <p>Return values at the given datetime. If exact match is not found, interpolate.</p> <p>Parameters:</p> Name Type Description Default <code>datetime</code> <code>str | datetime | DateTime</code> <p>Datetime to get value for</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <p>Returns:</p> Type Description <code>float</code> <p>The value at the timestamp requested</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def get_value(\n    self,\n    datetime: str | datetime | pendulum.DateTime,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n) -&gt; float:\n    \"\"\"Return values at the given datetime. If exact match is not found, interpolate.\n\n    :param datetime: Datetime to get value for\n    :type datetime: str or datetime\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :return: The value at the timestamp requested\n    :rtype: float\n    \"\"\"\n    if len(self.timeseries) == 0:\n        raise ValueError(\"Can't get value on empty timeseries.\")\n    dt = build_datetime(datetime, date_format).in_tz(self.timezone)\n\n    df: pl.DataFrame = self.filter(datetime, date_format, inplace=False).dataframe\n    if len(df) &gt; 0:\n        return df.to_dicts()[0][\"value\"]\n    else:\n        raise KeyError(f\"Value for {dt.to_datetime_string()} not found in the Timeseries.\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.groupby","title":"groupby","text":"<pre><code>groupby(\n    frequency: str | Duration,\n    agg: Literal[\"mean\", \"sum\", \"min\", \"max\"] = \"mean\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Group the Timeseries dynamically by time intervals.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>Grouping interval (e.g., \"1h\", \"1d\")</p> required <code>agg</code> <code>Literal['mean', 'sum', 'min', 'max']</code> <p>Aggregation method, defaults to \"mean\"</p> <code>'mean'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Grouped Timeseries</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If the aggregation function is unsupported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def groupby(\n    self,\n    frequency: str | pendulum.Duration,\n    agg: Literal[\"mean\", \"sum\", \"min\", \"max\"] = \"mean\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Group the Timeseries dynamically by time intervals.\n\n    :param frequency: Grouping interval (e.g., \"1h\", \"1d\")\n    :type frequency: str or pendulum.Duration\n    :param agg: Aggregation method, defaults to \"mean\"\n    :type agg: Literal[\"mean\", \"sum\", \"min\", \"max\"], optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises NotImplementedError: If the aggregation function is unsupported\n    :return: Grouped Timeseries\n    :rtype: Timeseries\n    \"\"\"\n    grouped_df = self.timeseries.group_by_dynamic(\"time\", every=frequency)\n    if agg == \"mean\":\n        df = grouped_df.agg(\n            pl.selectors.numeric().mean(),\n        )\n    elif agg == \"sum\":\n        df = grouped_df.agg(\n            pl.selectors.numeric().sum(),\n        )\n    elif agg == \"min\":\n        df = grouped_df.agg(\n            pl.selectors.numeric().min(),\n        )\n    elif agg == \"max\":\n        df = grouped_df.agg(\n            pl.selectors.numeric().max(),\n        )\n    else:\n        raise NotImplementedError(\"Unsupported aggregation function\")\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.interpolate","title":"interpolate","text":"<pre><code>interpolate(\n    interpolation_method: Literal[\n        \"linear\", \"constant\"\n    ] = \"constant\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Interpolate the Timeseries to fill in missing values.</p> <p>Parameters:</p> Name Type Description Default <code>interpolation_method</code> <code>Literal['linear', 'constant']</code> <p>Interpolation method, defaults to \"constant\"</p> <code>'constant'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Interpolated Timeseries</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If the method is not supported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def interpolate(\n    self, interpolation_method: Literal[\"linear\", \"constant\"] = \"constant\", inplace: bool = True\n) -&gt; Timeseries:\n    \"\"\"Interpolate the Timeseries to fill in missing values.\n\n    :param interpolation_method: Interpolation method, defaults to \"constant\"\n    :type interpolation_method: Literal[\"linear\", \"constant\"], optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises NotImplementedError: If the method is not supported\n    :return: Interpolated Timeseries\n    :rtype: Timeseries\n    \"\"\"\n    if interpolation_method == \"linear\":\n        df = self.timeseries.with_columns(pl.col(\"value\").interpolate_by(\"time\"))\n    elif interpolation_method == \"constant\":\n        df = self.timeseries.fill_null(strategy=\"forward\")\n    else:\n        raise NotImplementedError(\"Unsupported interpolation method, use 'linear' or 'constant'\")\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.iter_rows","title":"iter_rows","text":"<pre><code>iter_rows() -&gt; Generator[\n    tuple[datetime, float], None, None\n]\n</code></pre> <p>Iterate over rows of the Timeseries, yielding (time, value) tuples.</p> <p>Returns:</p> Type Description <code>Generator[tuple[datetime, float], None, None]</code> <p>A generator yielding tuples containing (time, value) for each row</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def iter_rows(self) -&gt; Generator[tuple[datetime, float], None, None]:\n    \"\"\"\n    Iterate over rows of the Timeseries, yielding (time, value) tuples.\n\n    :return: A generator yielding tuples containing (time, value) for each row\n    :rtype: Generator[tuple[datetime, float], None, None]\n    \"\"\"\n    for row in self.timeseries.iter_rows(named=True):\n        yield (row[\"time\"], row[\"value\"])\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.last_date","title":"last_date","text":"<pre><code>last_date() -&gt; DateTime | None\n</code></pre> <p>Return the last date in the Timeseries index.</p> <p>Returns:</p> Type Description <code>DateTime | None</code> <p>The last date in the Timeseries index</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def last_date(self) -&gt; pendulum.DateTime | None:\n    \"\"\"\n    Return the last date in the Timeseries index.\n\n    :return: The last date in the Timeseries index\n    :rtype: DateTime or None\n    \"\"\"\n    if len(self.timeseries) &gt; 0:\n        return cast(pendulum.DateTime, pendulum.instance(self.timeseries.select(\"time\").tail(1).item()))\n    return None\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.max","title":"max","text":"<pre><code>max() -&gt; float\n</code></pre> <p>Return the max value in the 'value' column.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The Timeseries max value</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def max(self) -&gt; float:  # type:ignore[return]\n    \"\"\"Return the max value in the 'value' column.\n\n    :return: The Timeseries max value\n    :rtype: float or None\n    \"\"\"\n    if len(self.timeseries) &gt; 0:\n        return cast(\"float\", self.timeseries[\"value\"].max())\n    else:\n        RuntimeError(\"Timeseries is empty, can't get the maximum value\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.min","title":"min","text":"<pre><code>min() -&gt; float\n</code></pre> <p>Return the min value in the 'value' column.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The Timeseries min value</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def min(self) -&gt; float:  # type:ignore[return]\n    \"\"\"Return the min value in the 'value' column.\n\n    :return: The Timeseries min value\n    :rtype: float or None\n    \"\"\"\n    if len(self.timeseries) &gt; 0:\n        return cast(\"float\", self.timeseries[\"value\"].min())\n    else:\n        RuntimeError(\"Timeseries is empty, can't get the minimum value\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.mul_value_at","title":"mul_value_at","text":"<pre><code>mul_value_at(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Multiply to an existing value at a specific datetime.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to multiply by the value</p> required <code>value</code> <code>float</code> <p>Value to multiply to the precedent value</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the value multiply by the precedent value at a specific index</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If time is not present in the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def mul_value_at(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Multiply to an existing value at a specific datetime.\n\n    :param time: Datetime to multiply by the value\n    :type time: datetime or str\n    :param value: Value to multiply to the precedent value\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If time is not present in the Timeseries\n    :return: Timeseries with the value multiply by the precedent value at a specific index\n    :rtype: Timeseries\n    \"\"\"\n    dt: pendulum.DateTime = build_datetime(time, date_format).in_tz(self.timezone)\n\n    if dt not in self.dataframe[\"time\"]:\n        raise ValueError(f\"Could not add value at {dt} because timestamp is not in the Timeseries\")\n\n    old_value = self.get_value(dt)\n    df = self.timeseries.filter(pl.col(\"time\") != dt)\n    new_row = pl.DataFrame({\"time\": [dt], \"value\": [old_value * value]}).with_columns(\n        pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)),\n        pl.col(\"value\").cast(pl.Float64()),\n    )\n    df = pl.concat([df, new_row])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.plot","title":"plot","text":"<pre><code>plot(\n    title: str = \"Time Series Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_color: str = \"#1f77b4\",\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; Figure\n</code></pre> <p>Generate a Plotly figure for the Timeseries data.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Plot title, defaults to \"Time Series Plot\"</p> <code>'Time Series Plot'</code> <code>height</code> <code>int</code> <p>Plot height in pixels, defaults to 500</p> <code>500</code> <code>width</code> <code>int</code> <p>Plot width in pixels, defaults to 800</p> <code>800</code> <code>show_grid</code> <code>bool</code> <p>Whether to show grid lines, defaults to True</p> <code>True</code> <code>line_color</code> <code>str</code> <p>Color of the line plot, defaults to \"#1f77b4\" (Plotly default blue)</p> <code>'#1f77b4'</code> <code>line_shape</code> <code>Literal['hv', 'linear', 'spline']</code> <p>Shape of the plot, defaults to \"hv\"</p> <code>'hv'</code> <code>template</code> <code>str</code> <p>Plotly template to use, defaults to \"plotly_white\"</p> <code>'plotly_white'</code> <p>Returns:</p> Type Description <code>plotly.graph_objects.Figure</code> <p>Plotly figure object</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def plot(\n    self,\n    title: str = \"Time Series Plot\",\n    height: int = 500,\n    width: int = 800,\n    show_grid: bool = True,\n    line_color: str = \"#1f77b4\",\n    line_shape: Literal[\"hv\", \"linear\", \"spline\"] = \"hv\",\n    template: str = \"plotly_white\",\n) -&gt; plotly.graph_objects.Figure:\n    \"\"\"\n    Generate a Plotly figure for the Timeseries data.\n\n    :param title: Plot title, defaults to \"Time Series Plot\"\n    :type title: str, optional\n    :param height: Plot height in pixels, defaults to 500\n    :type height: int, optional\n    :param width: Plot width in pixels, defaults to 800\n    :type width: int, optional\n    :param show_grid: Whether to show grid lines, defaults to True\n    :type show_grid: bool, optional\n    :param line_color: Color of the line plot, defaults to \"#1f77b4\" (Plotly default blue)\n    :type line_color: str, optional\n    :param line_shape: Shape of the plot, defaults to \"hv\"\n    :type line_shape: Literal[\"hv\", \"linear\", \"spline\"], optional\n    :param template: Plotly template to use, defaults to \"plotly_white\"\n    :type template: str, optional\n    :return: Plotly figure object\n    :rtype: plotly.graph_objects.Figure\n    \"\"\"\n    # Create the figure using Plotly Express\n    fig = px.line(\n        self.timeseries,\n        x=\"time\",\n        y=self.timeseries.select(pl.selectors.numeric()).columns,\n        title=title,\n        height=height,\n        width=width,\n        template=template,  # type:ignore [arg-type]\n        line_shape=line_shape,\n        color_discrete_sequence=[line_color] if line_color else None,\n    )\n\n    # Update layout for grid settings\n    fig.update_layout(\n        hovermode=\"x unified\",\n        xaxis={\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n        yaxis={\n            \"showgrid\": show_grid,\n            \"gridcolor\": \"lightgray\" if show_grid else None,\n        },\n    )\n\n    return fig\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.round","title":"round","text":"<pre><code>round(\n    rounding_precision: int = 0,\n    mode: Literal[\n        \"half_to_even\", \"half_away_from_zero\"\n    ] = \"half_to_even\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Returns a copy of the timeseries with all the numerical values rounded.</p> <p>Parameters:</p> Name Type Description Default <code>rounding_precision</code> <code>int</code> <p>Number of decimals used to round numerical values.</p> <code>0</code> <code>mode</code> <code>Literal['half_to_even', 'half_away_from_zero']</code> <p>Rounding strategy.</p> <code>'half_to_even'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>Rounded Timeseries</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def round(\n    self,\n    rounding_precision: int = 0,\n    mode: Literal[\"half_to_even\", \"half_away_from_zero\"] = \"half_to_even\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"\n    Returns a copy of the timeseries with all the numerical values rounded.\n    :param rounding_precision: Number of decimals used to round numerical values.\n    :type rounding_precision: int\n    :param mode: Rounding strategy.\n    :type mode: str\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :return: Rounded Timeseries\n    :rtype: Self\n    \"\"\"\n    backend = self._get_data()\n    result = backend.with_columns(pl.col(\"value\").round(rounding_precision, mode))\n    return self._return(result, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.set_frequency","title":"set_frequency","text":"<pre><code>set_frequency(\n    frequency: str | Duration, inplace: bool = True\n) -&gt; Timeseries\n</code></pre> <p>Change the frequency (timestep) of the time series.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>The desired frequency. Can be a string (e.g., '1d', '15m') or a <code>pendulum.Duration</code>.</p> required <code>inplace</code> <code>bool</code> <p>If True, modifies the object in place. If False, returns a new modified object.</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>The resampled time series, either modified in place or as a new object.</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def set_frequency(self, frequency: str | pendulum.Duration, inplace: bool = True) -&gt; Timeseries:\n    \"\"\"\n    Change the frequency (timestep) of the time series.\n\n    :param frequency: The desired frequency. Can be a string (e.g., '1d', '15m') or a `pendulum.Duration`.\n    :type frequency: str or pendulum.Duration\n    :param inplace: If True, modifies the object in place. If False, returns a new modified object.\n    :type inplace: bool\n    :return: The resampled time series, either modified in place or as a new object.\n    :rtype: Timeseries\n    \"\"\"\n    new_timestep = get_duration(frequency)\n\n    if new_timestep &gt; self.frequency:\n        df = self.groupby(new_timestep, inplace=False)\n    elif new_timestep &lt; self.frequency:\n        df = self.upsample(new_timestep, inplace=False)\n    else:\n        df = self\n\n    return self._return(df.dataframe, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.set_timezone","title":"set_timezone","text":"<pre><code>set_timezone(timezone: str) -&gt; None\n</code></pre> <p>Convert the datetime column to a new timezone.</p> <p>Parameters:</p> Name Type Description Default <code>timezone</code> <code>str</code> <p>Timezone string</p> required Source code in <code>atlas/math/timeseries.py</code> <pre><code>def set_timezone(self, timezone: str) -&gt; None:\n    \"\"\"\n    Convert the datetime column to a new timezone.\n\n    :param timezone: Timezone string\n    :type timezone: str\n    \"\"\"\n    check_timezone(timezone)\n\n    self.timezone = timezone\n    self.timeseries = self.timeseries.with_columns(\n        pl.col(\"time\").dt.convert_time_zone(timezone),\n    )\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.set_value","title":"set_value","text":"<pre><code>set_value(\n    time: datetime | str,\n    value: float | None,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Update a value at a specific datetime.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to set</p> required <code>value</code> <code>float | None</code> <p>Value to set</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the set value</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the time is not in the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def set_value(\n    self,\n    time: datetime | str,\n    value: float | None,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Update a value at a specific datetime.\n\n    :param time: Datetime to set\n    :type time: datetime or str\n    :param value: Value to set\n    :type value: float or int\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If the time is not in the Timeseries\n    :return: Timeseries with the set value\n    :rtype: Timeseries\n    \"\"\"\n    dt: pendulum.DateTime = build_datetime(time, date_format).in_tz(self.timezone)\n\n    if dt not in self.dataframe[\"time\"]:\n        raise ValueError(f\"Could not set value at {dt} because timestamp is not in the Timeseries\")\n\n    df = self.timeseries.filter(pl.col(\"time\") != dt)\n    new_row = pl.DataFrame({\"time\": [dt], \"value\": [value]}).with_columns(\n        pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)),\n        pl.col(\"value\").cast(pl.Float64()),\n    )\n    df = pl.concat([df, new_row])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.set_values","title":"set_values","text":"<pre><code>set_values(\n    other: Timeseries\n    | DataFrame\n    | DataFrame\n    | TimeseriesDict,\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Set or update values. If the datetime exists, it is overwritten.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Timeseries | DataFrame | DataFrame | TimeseriesDict</code> <p>other timeseries with values to updated self</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the new values</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If frequency doesn't match or at least one time of the other Timeseries is not in the current one</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def set_values(\n    self,\n    other: Timeseries | pl.DataFrame | pd.DataFrame | TimeseriesDict,\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Set or update values. If the datetime exists, it is overwritten.\n\n    :param other: other timeseries with values to updated self\n    :type other: Timeseries\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If frequency doesn't match or at least one time of the other Timeseries is not in the\n    current one\n    :return: Timeseries with the new values\n    :rtype: Timeseries\n    \"\"\"\n    other = Timeseries(other)\n    if len(self.timeseries) == 0:\n        other_df = other.dataframe\n        return self._return(other_df, inplace)\n\n    other_ts = Timeseries(other)\n    if self.frequency != other_ts.frequency:\n        raise ValueError(\"Could not perform set values on Timeseries because frequencies don't match\")\n    if not other_ts.dataframe[\"time\"].is_in(self.dataframe[\"time\"]).all():\n        raise ValueError(\n            \"Could not set values on Timeseries because indexes to set are not all present in Timeseries\"\n        )\n\n    df = self.timeseries.filter(~pl.col(\"time\").is_in(other_ts.dataframe[\"time\"]))\n    df = pl.concat([df, other_ts.dataframe])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.slice","title":"slice","text":"<pre><code>slice(\n    start_bound: datetime | DateTime | str,\n    end_bound: datetime | DateTime | str,\n    closed: Literal[\n        \"left\", \"right\", \"both\", \"none\"\n    ] = \"both\",\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Get a slice of the Timeseries</p> <p>Parameters:</p> Name Type Description Default <code>start_bound</code> <code>datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code>end_bound</code> <code>datetime | DateTime | str</code> <p>Datetime to filter the Timeseries</p> required <code></code> <code>closed</code> <p>{'both', 'left', 'right', 'none'} Define which sides of the interval are closed (inclusive).</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The Timeseries object</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def slice(\n    self,\n    start_bound: datetime | pendulum.DateTime | str,\n    end_bound: datetime | pendulum.DateTime | str,\n    closed: Literal[\"left\", \"right\", \"both\", \"none\"] = \"both\",\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"Get a slice of the Timeseries\n\n    :param start_bound: Datetime to filter the Timeseries\n    :param end_bound: Datetime to filter the Timeseries\n    :param closed : {'both', 'left', 'right', 'none'}\n        Define which sides of the interval are closed (inclusive).\n    :param inplace: Whether to modify the current instance, defaults to True\n    :return: The Timeseries object\n    \"\"\"\n    backend = self._get_data()\n    date_start = build_datetime(start_bound).in_tz(self.timezone)\n    date_end = build_datetime(end_bound).in_tz(self.timezone)\n    sliced = backend.filter(pl.col(\"time\").is_between(date_start, date_end, closed))\n\n    return self._return(sliced, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.slice_with_offset","title":"slice_with_offset","text":"<pre><code>slice_with_offset(\n    offset: int,\n    length: int | None = None,\n    inplace: bool = True,\n) -&gt; Self\n</code></pre> <p>Get a slice of the Timeseries</p> <p>Parameters:</p> Name Type Description Default <code>offset</code> <code>int</code> <p>Start index. Negative indexing is supported.</p> required <code>length</code> <code>int | None</code> <p>Length of the slice. If set to <code>None</code>, all rows starting at the offset will be selected.</p> <code>None</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Self</code> <p>The Timeseries object</p> Source code in <code>atlas/math/abstract_timeseries.py</code> <pre><code>def slice_with_offset(\n    self,\n    offset: int,\n    length: int | None = None,\n    inplace: bool = True,\n) -&gt; Self:\n    \"\"\"Get a slice of the Timeseries\n\n    :param offset: Start index. Negative indexing is supported.\n    :param length: Length of the slice. If set to `None`, all rows starting at the offset will be selected.\n    :param inplace: Whether to modify the current instance, defaults to True\n    :return: The Timeseries object\n    \"\"\"\n    backend = self._get_data()\n    sliced = backend.slice(offset, length)\n    return self._return(sliced, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.sort","title":"sort","text":"<pre><code>sort(inplace: bool = True) -&gt; Timeseries\n</code></pre> <p>Sort the Timeseries by the given variable(s).</p> <p>Parameters:</p> Name Type Description Default <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <code>descending</code> <code>(bool, optional)</code> <p>Sort in descending order, defaults to False</p> required <p>Returns:</p> Type Description <code>Timeseries</code> <p>Sorted Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def sort(\n    self,\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Sort the Timeseries by the given variable(s).\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :param descending: Sort in descending order, defaults to False\n    :type descending: bool, optional\n    :return: Sorted Timeseries\n    :rtype: Timeseries\n    \"\"\"\n    return self._return(self.timeseries, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.sum","title":"sum","text":"<pre><code>sum() -&gt; float\n</code></pre> <p>Return the sum of the 'value' column.</p> <p>Returns:</p> Type Description <code>float | None</code> <p>The Timeseries sum value</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def sum(self) -&gt; float:  # type:ignore[return]\n    \"\"\"Return the sum of the 'value' column.\n\n    :return: The Timeseries sum value\n    :rtype: float or None\n    \"\"\"\n    if len(self.timeseries) &gt; 0:\n        return cast(\"float\", self.timeseries[\"value\"].sum())\n    else:\n        RuntimeError(\"Timeseries is empty, can't perform the sum\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.sum_value_at","title":"sum_value_at","text":"<pre><code>sum_value_at(\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Add to an existing value at a specific datetime.</p> <p>Parameters:</p> Name Type Description Default <code>time</code> <code>datetime | str</code> <p>Datetime to add to the value</p> required <code>value</code> <code>float</code> <p>Value to add to the precedent value</p> required <code>date_format</code> <code>str</code> <p>Date format string, defaults to \"YYYY-MM-DD HHss\"</p> <code>'YYYY-MM-DD HH:mm:ss'</code> <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Timeseries with the value added to the precedent value at a specific index</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If time is not present in the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def sum_value_at(\n    self,\n    time: datetime | str,\n    value: float,\n    date_format: str = \"YYYY-MM-DD HH:mm:ss\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Add to an existing value at a specific datetime.\n\n    :param time: Datetime to add to the value\n    :type time: datetime or str\n    :param value: Value to add to the precedent value\n    :type value: float\n    :param date_format: Date format string, defaults to \"YYYY-MM-DD HH:mm:ss\"\n    :type date_format: str, optional\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :raises ValueError: If time is not present in the Timeseries\n    :return: Timeseries with the value added to the precedent value at a specific index\n    :rtype: Timeseries\n    \"\"\"\n    dt: pendulum.DateTime = build_datetime(time, date_format).in_tz(self.timezone)\n\n    if dt not in self.dataframe[\"time\"]:\n        raise ValueError(f\"Could not add value at {dt} because timestamp is not in the Timeseries\")\n\n    old_value = self.get_value(dt)\n    df = self.timeseries.filter(pl.col(\"time\") != dt)\n    new_row = pl.DataFrame({\"time\": [dt], \"value\": [old_value + value]}).with_columns(\n        pl.col(\"time\").cast(pl.Datetime(\"us\", time_zone=self.timezone)),\n        pl.col(\"value\").cast(pl.Float64()),\n    )\n    df = pl.concat([df, new_row])\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.to_file","title":"to_file","text":"<pre><code>to_file(\n    path: str | Path,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n) -&gt; None\n</code></pre> <p>Export the Timeseries to a file.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def to_file(\n    self,\n    path: str | Path,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n) -&gt; None:\n    \"\"\"\n    Export the Timeseries to a file.\n\n    :param path: Destination file path\n    :type path: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path = str(path)\n    if not path.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    if file_format_lower == \"csv\":\n        self.timeseries.write_csv(path, separator=separator)\n    elif file_format_lower == \"parquet\":\n        self.timeseries.write_parquet(path)\n    elif file_format_lower == \"pickle\":\n        with open(path, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.to_file_with_attribute","title":"to_file_with_attribute","text":"<pre><code>to_file_with_attribute(\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\n        \"csv\", \"parquet\", \"pickle\"\n    ] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None\n</code></pre> <p>Export the Timeseries to a file with an attribute column.</p> <p>If the file already exists and concatenate is True, the new data will be appended to the existing data.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination file path</p> required <code>attribute</code> <code>str</code> <p>Attribute name to add as a column</p> required <code>file_format</code> <code>Literal['csv', 'parquet', 'pickle']</code> <p>Export file format, defaults to \"csv\"</p> <code>'csv'</code> <code>separator</code> <code>str</code> <p>Export column separator format, defaults to \";\"</p> <code>';'</code> <code>concatenate</code> <code>bool</code> <p>If True, concatenate with existing file data, defaults to True</p> <code>True</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file extension doesn't match format</p> <code>NotImplementedError</code> <p>If the file format is not supported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def to_file_with_attribute(\n    self,\n    path: str | Path,\n    attribute: str,\n    file_format: Literal[\"csv\", \"parquet\", \"pickle\"] = \"csv\",\n    separator: str = \";\",\n    concatenate: bool = True,\n) -&gt; None:\n    \"\"\"\n    Export the Timeseries to a file with an attribute column.\n\n    If the file already exists and concatenate is True, the new data will be\n    appended to the existing data.\n\n    :param path: Destination file path\n    :type path: str or Path\n    :param attribute: Attribute name to add as a column\n    :type attribute: str\n    :param file_format: Export file format, defaults to \"csv\"\n    :type file_format: Literal[\"csv\", \"parquet\", \"pickle\"], optional\n    :param separator: Export column separator format, defaults to \";\"\n    :type separator: str, optional\n    :param concatenate: If True, concatenate with existing file data, defaults to True\n    :type concatenate: bool, optional\n    :raises ValueError: If file extension doesn't match format\n    :raises NotImplementedError: If the file format is not supported\n    \"\"\"\n    file_format_lower = file_format.lower()\n\n    if isinstance(path, Path):\n        path_str = str(path)\n    else:\n        path_str = path\n\n    if not path_str.lower().endswith(file_format_lower):\n        raise ValueError(\"Format and file extension don't match.\")\n\n    df_to_write = self.timeseries.clone().insert_column(1, pl.lit(attribute).alias(\"attribute\"))\n\n    if concatenate:\n        path_obj = Path(path_str)\n        if path_obj.exists() and file_format_lower != \"pickle\":\n            try:\n                if file_format_lower == \"csv\":\n                    existing_df = pl.read_csv(path_str, separator=separator, try_parse_dates=True)\n                elif file_format_lower == \"parquet\":\n                    existing_df = pl.read_parquet(path_str)\n\n                # Concatenate with existing data\n                df_to_write = pl.concat([existing_df, df_to_write])\n            except Exception as e:\n                raise ValueError(f\"Could not read existing file for concatenation: {e}\") from e\n\n    # Write the file\n    if file_format_lower == \"csv\":\n        df_to_write.write_csv(path_str, separator=separator)\n    elif file_format_lower == \"parquet\":\n        df_to_write.write_parquet(path_str)\n    elif file_format_lower == \"pickle\":\n        with open(path_str, \"wb\") as f:\n            pickle.dump(self, f)\n    else:\n        raise NotImplementedError(\"Format not supported\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.to_frame","title":"to_frame","text":"<pre><code>to_frame(\n    engine: Literal[\"polars\", \"pandas\"] = \"polars\",\n) -&gt; DataFrame | DataFrame\n</code></pre> <p>Return the internal time series as a data frame.</p> <p>Parameters:</p> Name Type Description Default <code>engine</code> <code>Literal['polars', 'pandas']</code> <p>The engine to use for the output, defaults to \"pandas\"</p> <code>'polars'</code> <p>Returns:</p> Type Description <code>pl.DataFrame | pd.DataFrame</code> <p>The internal Timeseries data</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def to_frame(self, engine: Literal[\"polars\", \"pandas\"] = \"polars\") -&gt; pl.DataFrame | pd.DataFrame:\n    \"\"\"\n    Return the internal time series as a data frame.\n\n    :param engine: The engine to use for the output, defaults to \"pandas\"\n    :type engine: str, optional\n    :return: The internal Timeseries data\n    :rtype: pl.DataFrame or pd.DataFrame\n    \"\"\"\n    if engine == \"pandas\":\n        return self.timeseries.to_pandas()\n    if engine == \"polars\":\n        return self.timeseries\n    raise ValueError(\"Unsupported engine. Use 'polars' or 'pandas'.\")\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.to_lazy","title":"to_lazy","text":"<pre><code>to_lazy() -&gt; LazyFrame\n</code></pre> <p>Convert the internal Polars DataFrame to a LazyFrame.</p> <p>Returns:</p> Type Description <code>pl.LazyFrame</code> <p>A Polars LazyFrame representation of the Timeseries</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def to_lazy(self) -&gt; pl.LazyFrame:\n    \"\"\"\n    Convert the internal Polars DataFrame to a LazyFrame.\n\n    :return: A Polars LazyFrame representation of the Timeseries\n    :rtype: pl.LazyFrame\n    \"\"\"\n    return self.timeseries.lazy()\n</code></pre>"},{"location":"api/math/timeseries/#atlas.Timeseries.upsample","title":"upsample","text":"<pre><code>upsample(\n    frequency: str | Duration,\n    interpolation_method: Literal[\n        \"linear\", \"constant\"\n    ] = \"constant\",\n    inplace: bool = True,\n) -&gt; Timeseries\n</code></pre> <p>Upsample the Timeseries to a higher frequency.</p> <p>Fills in missing timestamps by interpolating or forward-filling values.</p> <p>Parameters:</p> Name Type Description Default <code>frequency</code> <code>str | Duration</code> <p>Frequency string (e.g., \"15m\", \"1h\") defining the new time resolution</p> required <code>inplace</code> <code>bool</code> <p>Whether to modify the current instance, defaults to True</p> <code>True</code> <code>interpolation_method</code> <code>Literal['linear', 'constant']</code> <p>Method to fill missing values: \"linear\" for interpolation, \"constant\" for forward fill</p> <code>'constant'</code> <p>Returns:</p> Type Description <code>Timeseries</code> <p>Upsampled Timeseries</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If the provided interpolation_method is not supported</p> Source code in <code>atlas/math/timeseries.py</code> <pre><code>def upsample(\n    self,\n    frequency: str | pendulum.Duration,\n    interpolation_method: Literal[\"linear\", \"constant\"] = \"constant\",\n    inplace: bool = True,\n) -&gt; Timeseries:\n    \"\"\"\n    Upsample the Timeseries to a higher frequency.\n\n    Fills in missing timestamps by interpolating or forward-filling values.\n\n    :param frequency: Frequency string (e.g., \"15m\", \"1h\") defining the new time resolution\n    :type frequency: str | pendulum.Duration\n    :param inplace: Whether to modify the current instance, defaults to True\n    :type inplace: bool, optional\n    :param interpolation_method: Method to fill missing values: \"linear\" for interpolation, \"constant\" for forward fill\n    :type interpolation_method: str, optional\n    :raises NotImplementedError: If the provided interpolation_method is not supported\n    :return: Upsampled Timeseries\n    :rtype: Timeseries\n    \"\"\"\n\n    if interpolation_method == \"linear\":\n        df = (\n            self.timeseries.upsample(time_column=\"time\", every=frequency)\n            .with_columns(pl.col(\"value\").interpolate_by(\"time\"))\n            .fill_null(strategy=\"forward\")\n        )\n    elif interpolation_method == \"constant\":\n        df = self.timeseries.upsample(time_column=\"time\", every=frequency).fill_null(\n            strategy=\"forward\",\n        )\n    else:\n        raise NotImplementedError(\"Unsupported interpolation method\")\n\n    return self._return(df, inplace)\n</code></pre>"},{"location":"api/models/equipment/","title":"Equipment Models","text":""},{"location":"api/models/equipment/#atlas.Equipment","title":"atlas.Equipment","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Node</code> <p>Associated Node</p> required <code>portfolio</code> <code>Portfolio</code> <p>Associated Portfolio</p> required <code>coe2_emission_factor</code> <code>float</code> <p>CO2 emissions per MWh</p> required <code>has_daily_energy_constraint</code> <code>bool</code> <p>True if equipment has a storage limit, False otherwise</p> required <code>maximum_afrr</code> <code>float</code> <p>Maximum volume that can be allocated to aFRR</p> required <code>maximum_fcr</code> <code>float</code> <p>Maximum volume that can be allocated to FCR</p> required <code>maximum_gradient</code> <code>float</code> <p>Maximum gradient possible with 0 representing an infinite gradient</p> required <code>setup_delay</code> <code>float</code> <p>Time between the transmission of an activation order and the respective production change of a unit. Currently used for balancing time frames.</p> required <code>unit_count</code> <code>float</code> <p>Number of units in the cluster</p> required <code>afrr_down_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted downward aFRR reserves</p> required <code>afrr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted upward aFRR reserves</p> required <code>fcr_down_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted downward FCR reserves</p> required <code>fcr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted upward FCR reserves</p> required <code>mfrr_down_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted downward mFRR reserves</p> required <code>mfrr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted upward mFRR reserves</p> required <code>rr_down_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted downward RR reserves</p> required <code>rr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of contracted upward RR reserves</p> required <code>co2_emission_factor</code> <code>float</code> <p>CO2 emissions per MWh</p> required <code>co2_emissions</code> <code>ForecastingMatrix</code> <p>Cumulated CO2 emission after Portfolio Optimization</p> required <code>id_buy_submitted_volume</code> <code>ForecastingMatrix</code> <p>Sum of volume of buy offers for each Intraday market</p> required <code>id_cleared_quantity</code> <code>ForecastingMatrix</code> <p>Total quantity accepted in each Intraday market. Represents the sum of accepted powers of all orders for this equipment</p> required <code>id_po_for_orders</code> <code>ForecastingMatrix</code> <p>Intermediate result of Portfolio optimization for Intraday market. Note that this value does not represent the production plan of the unit, but rather an intermediate result used for the creation of intraday orders.</p> required <code>id_sell_submitted_volume</code> <code>ForecastingMatrix</code> <p>Sum of volume of sell offers for each Intraday market</p> required <code>power</code> <code>ForecastingMatrix</code> <p>Production schedules for each hour and for each time horizon</p> required <code>specific_activated_power</code> <code>ForecastingMatrix</code> <p>Power activated by the Balancing Mechanism</p> required <code>storage_marginal_value</code> <code>ScenarioMatrix</code> <p>Storage use value. Ex: Water value for hydraulic equipment</p> required <code>afrr_activated</code> <code>Timeseries</code> <p>Volume of aFRR activated</p> required <code>afrr_submitted_volume</code> <code>Timeseries</code> <p>To be modified, in this state this property does not describe anything properly</p> required <code>mfrr_activated</code> <code>Timeseries</code> <p>Volume of mFRR activated</p> required <code>mfrr_submitted_volume</code> <code>Timeseries</code> <p>To be modified, in this state this property does not describe anything properly</p> required <code>fcr_activated</code> <code>Timeseries</code> <p>Volume of FCR activated</p> required <code>fcr_submitted_volume</code> <code>Timeseries</code> <p>To be modified, in this state this property does not describe anything properly</p> required <code>rr_activated</code> <code>Timeseries</code> <p>Volume of RR activated</p> required <code>rr_submitted_volume</code> <code>Timeseries</code> <p>To be modified, in this state this property does not describe anything properly</p> required <code>da_cleared_quantity</code> <code>AbstractTimeseries</code> <p>Sum of accepted power by day-ahead market</p> required <code>maximum_daily_energy</code> <code>AbstractTimeseries</code> <p>Maximum daily quantity of energy that can be produced</p> required <code>minimum_daily_energy</code> <code>Timeseries</code> <p>Minimum daily quantity of energy that can be produced</p> required <code>startup_cost</code> <code>Timeseries</code> <p>Startup cost. Currently only used for thermic equipment class. If the equipment represents a cluster of units, this value is the start-up cost per unit.</p> required <code>total_id_buy_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of buy offers for each Intraday market</p> required <code>total_id_cleared_quantity</code> <code>Timeseries</code> <p>Cumulative sum of all accepted power from all Intraday clearing</p> required <code>total_id_sell_submitted_volume</code> <code>Timeseries</code> <p>Cumulative sum of sell offers from all Intraday markets</p> required <code>variable_cost</code> <code>AbstractTimeseries</code> <p>Variable cost</p> required"},{"location":"api/models/equipment/#atlas.Equipment.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Load","title":"atlas.Load","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>load_type</code> <code>LoadType</code> <p>Load type</p> required <code>maximum_power_forecast</code> <code>ForecastingMatrix</code> <p>Forecast of the maximum production</p> required <code>da_buy_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of purchase offers submitted to the Day Ahead market</p> required <code>power_forecast_high</code> <code>Timeseries</code> <p>Year-long demand time series used to formulate the high price forecast scenario on Day Ahead</p> required <code>power_forecast_low</code> <code>Timeseries</code> <p>Year-long demand time series used to formulate the low price forecast scenario on Day Ahead</p> required"},{"location":"api/models/equipment/#atlas.Load.convert_hours_to_duration","title":"convert_hours_to_duration  <code>classmethod</code>","text":"<pre><code>convert_hours_to_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects (hours default).</p> Source code in <code>atlas/models/equipment/load.py</code> <pre><code>@field_validator(\"additional_hours\", mode=\"before\")\n@classmethod\ndef convert_hours_to_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects (hours default).\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Load.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.OtherNonDispatchable","title":"atlas.OtherNonDispatchable","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>maximum_power_forecast</code> <code>ForecastingMatrix</code> <p>Forecast of the maximum production</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers submitted to the Day Ahead market</p> required"},{"location":"api/models/equipment/#atlas.OtherNonDispatchable.convert_hours_to_duration","title":"convert_hours_to_duration  <code>classmethod</code>","text":"<pre><code>convert_hours_to_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects (hours default).</p> Source code in <code>atlas/models/equipment/other_non_dispatchable.py</code> <pre><code>@field_validator(\"additional_hours\", mode=\"before\")\n@classmethod\ndef convert_hours_to_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects (hours default).\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.OtherNonDispatchable.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Hydro","title":"atlas.Hydro","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>fragment_prices</code> <code>list[float]</code> <p>List of spreads applied to hydro equipment water values, to assign prices to fragments calculated with fragment_volumes</p> required <code>fragment_volumes</code> <code>list[float]</code> <p>List of percentages used to divide the interval between MaximumPower and MinimumPower into different fragments</p> required <code>stored_energy</code> <code>ForecastingMatrix</code> <p>Storage capacity of energy for different time horizons</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers submitted to the Day Ahead market</p> required <code>energy_target</code> <code>Timeseries</code> <p>Daily storage target. Used for countries where inflows are not provided in input data</p> required <code>inflows</code> <code>Timeseries</code> <p>Hydro daily inflows (in energy)</p> required <code>initial_level</code> <code>Timeseries</code> <p>Energy contained in the hydro reservoir prior to execution of any ATLAS module</p> required <code>maximum_energy</code> <code>AbstractTimeseries</code> <p>Maximum energy storage capacity</p> required <code>minimum_energy</code> <code>AbstractTimeseries</code> <p>Minimum energy storage capacity</p> required <code>maximum_power</code> <code>AbstractTimeseries</code> <p>Maximum power</p> required <code>minimum_power</code> <code>AbstractTimeseries</code> <p>Minimum power</p> required <code>inflow_frequency</code> <code>InflowFrequency</code> <p>Frequency of inflow data. Possible values: 'Monthly', 'Daily'</p> required <code>energy_target_frequency</code> <code>InflowFrequency</code> <p>Frequency of energy target data. Possible values: 'Monthly', 'Daily'</p> required"},{"location":"api/models/equipment/#atlas.Hydro.parse_duration","title":"parse_duration  <code>classmethod</code>","text":"<pre><code>parse_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects.</p> Source code in <code>atlas/models/equipment/hydro.py</code> <pre><code>@field_validator(\"additional_hours\", mode=\"before\")\n@classmethod\ndef parse_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects.\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Hydro.serialize_fragment_prices_and_volumes","title":"serialize_fragment_prices_and_volumes","text":"<pre><code>serialize_fragment_prices_and_volumes(\n    value: list[float] | None,\n) -&gt; str | None\n</code></pre> <p>Serialize fragment prices and volumes to a string.</p> Source code in <code>atlas/models/equipment/hydro.py</code> <pre><code>@field_serializer(\"fragment_prices\", \"fragment_volumes\", mode=\"plain\")\ndef serialize_fragment_prices_and_volumes(self, value: list[float] | None) -&gt; str | None:\n    \"\"\"Serialize fragment prices and volumes to a string.\"\"\"\n    return serializer_list_float(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Hydro.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Solar","title":"atlas.Solar","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>installed_capacity</code> <code>float</code> <p>Installed capacity</p> required <code>maximum_power_forecast</code> <p>Forecast of the maximum production</p> required <code>curtailment_cost</code> <code>Timeseries</code> <p>Equipment curtailment cost. Positive by convention</p> required <code>curtailed_power</code> <code>Timeseries</code> <p>Total curtailed power following each Portfolio Optimization</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers submitted to the Day Ahead market</p> required <code>maximum_curtailment_ratio</code> <code>Timeseries</code> <p>Ratio of maximum production power (indicated by MaximumPowerForecast) that can be curtailed</p> required"},{"location":"api/models/equipment/#atlas.Solar.convert_hours_to_duration","title":"convert_hours_to_duration  <code>classmethod</code>","text":"<pre><code>convert_hours_to_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects (hours default).</p> Source code in <code>atlas/models/equipment/solar.py</code> <pre><code>@field_validator(\"additional_hours\", mode=\"before\")\n@classmethod\ndef convert_hours_to_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects (hours default).\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Solar.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Wind","title":"atlas.Wind","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>installed_capacity</code> <code>float</code> <p>Installed capacity</p> required <code>maximum_power_forecast</code> <code>ForecastingMatrix</code> <p>Forecast of the maximum production</p> required <code>curtailed_power</code> <code>ForecastingMatrix</code> <p>Total curtailed power following each Portfolio Optimization</p> required <code>curtailment_cost</code> <code>Timeseries</code> <p>Equipment curtailment cost. Positive by convention</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers submitted to the Day Ahead market</p> required <code>maximum_curtailment_ratio</code> <code>Timeseries</code> <p>Ratio of maximum production power (indicated by MaximumPowerForecast) that can be curtailed</p> required"},{"location":"api/models/equipment/#atlas.Wind.convert_hours_to_duration","title":"convert_hours_to_duration  <code>classmethod</code>","text":"<pre><code>convert_hours_to_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects (hours default).</p> Source code in <code>atlas/models/equipment/wind.py</code> <pre><code>@field_validator(\"additional_hours\", mode=\"before\")\n@classmethod\ndef convert_hours_to_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects (hours default).\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Wind.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Storage","title":"atlas.Storage","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>charge_efficiency</code> <code>float</code> <p>Effiency during charging phase. Corresponds to the ratio of energy stored in the battery to energy withdrawn from the system</p> required <code>discharge_efficiency</code> <code>float</code> <p>Effiency during discharge phase. Corresponds to the ratio of energy injected to the system to energy withdrawn from the battery</p> required <code>is_v2g</code> <code>bool</code> <p>True if Equipment has vehicule to grid capabilities</p> required <code>storage_initial_level</code> <code>float</code> <p>Percentage of MaximumEnergy used as a reference to determine the unit's initial stock level, if StoredEnergy has not yet been filled by a previous market</p> required <code>storage_type</code> <code>StorageType</code> <p>Storage type, e.g. battery, pumped hydro, etc.</p> required <code>transition_duration</code> <code>Duration</code> <p>Duration of the transition phase between two states (e.g. charging to discharging).</p> required <code>stored_energy</code> <code>ForecastingMatrix</code> <p>Portfolio Optimization output containing anticipated storage levels after each clearing</p> required <code>da_buy_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of purchase offers submitted to the Day Ahead market</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers submitted to the Day Ahead market</p> required <code>displacement_energy</code> <code>Timeseries</code> <p>Energy used by the connected electric vehicles since their last charge</p> required <code>maximum_energy</code> <code>Timeseries</code> <p>Maximum energy that can be stored</p> required <code>maximum_power</code> <code>AbstractTimeseries</code> <p>Maximum power of the unit or cluster</p> required <code>minimum_power</code> <code>Timeseries</code> <p>Minimum power of the unit or cluster</p> required <code>minimum_state_of_charge</code> <code>Timeseries</code> <p>Coefficient applied to MaximumEnergy, to represent the minimum energy that can be stored</p> required"},{"location":"api/models/equipment/#atlas.Storage.additional_hours","title":"additional_hours  <code>property</code>","text":"<pre><code>additional_hours: Duration\n</code></pre> <p>Get additional hours for optimization period.</p>"},{"location":"api/models/equipment/#atlas.Storage.parse_duration","title":"parse_duration  <code>classmethod</code>","text":"<pre><code>parse_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects.</p> Source code in <code>atlas/models/equipment/storage.py</code> <pre><code>@field_validator(\n    \"transition_duration\",\n    \"additional_hours_\",\n    mode=\"before\",\n)\n@classmethod\ndef parse_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects.\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Storage.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Thermal","title":"atlas.Thermal","text":"<p>               Bases: <code>Equipment</code></p> <p>Parameters:</p> Name Type Description Default <code>installed_capacity</code> <code>float</code> <p>Installed capacity</p> required <code>minimum_stable_power_duration</code> <code>Duration | float</code> <p>Minimum stable power duration. Length of time that this unit must stay at a stable production level in between ramping periods. Provide as float (hours) or Duration object.</p> required <code>minimum_time_off</code> <code>Duration | float</code> <p>Minimum time that the unit must remain off after shutting down prior to a new start-up. Provide as float (hours) or Duration object.</p> required <code>minimum_time_on</code> <code>Duration | float</code> <p>Minimum time that the unit must remain on after starting up. Provide as float (hours) or Duration object.</p> required <code>outage_mean_duration</code> <code>Duration | float</code> <p>Mean duration of an outage, in hours.</p> required <code>outage_probability</code> <code>float</code> <p>Probability of outage</p> required <code>scheduled_shutdown_mean_duration</code> <code>float</code> <p>Mean duration of a scheduled shutdown, in hours.</p> required <code>scheduled_shutdown_probability</code> <code>float</code> <p>Probability of a scheduled shutdown</p> required <code>shutdown_duration</code> <code>Duration | float</code> <p>Time it takes for the unit to shut down. Provide as float (hours) or Duration object.</p> required <code>startup_delay_probability</code> <code>float</code> <p>Probability of a startup delay, which is the time it takes</p> required <code>startup_duration</code> <code>Duration | float</code> <p>Time it takes for the unit to start up. Provide as float (hours) or Duration object.</p> required <code>strategy</code> <code>ThermalStrategy</code> <p>Thermal strategy to be used for this unit. Possible values are \"thermal\", \"thermal_with_startup_delay\", \"thermal_with_shutdown\".</p> required <code>state_sequence</code> <code>AbstractScenarioMatrix</code> <p>Sequence of states for the thermal unit.</p> required <code>da_sell_submitted_volume</code> <code>Timeseries</code> <p>Sum of volume of sell offers on the Day Ahead market</p> required <code>maximum_power</code> <code>Timeseries</code> <p>Maximum power of the unit or cluster</p> required <code>minimum_power</code> <code>Timeseries</code> <p>Minimum power of the unit or cluster</p> required <code>co2_emission_factor</code> <code>float</code> <p>CO2 emissions per MWh</p> required"},{"location":"api/models/equipment/#atlas.Thermal.parse_duration","title":"parse_duration  <code>classmethod</code>","text":"<pre><code>parse_duration(v)\n</code></pre> <p>Convert various duration formats to Duration objects.</p> Source code in <code>atlas/models/equipment/thermal.py</code> <pre><code>@field_validator(\n    \"minimum_stable_power_duration\",\n    \"minimum_time_off\",\n    \"minimum_time_on\",\n    \"outage_mean_duration\",\n    \"shutdown_duration\",\n    \"startup_duration\",\n    \"additional_hours\",\n    mode=\"before\",\n)\n@classmethod\ndef parse_duration(cls, v):\n    \"\"\"Convert various duration formats to Duration objects.\"\"\"\n    return convert_to_duration(v)\n</code></pre>"},{"location":"api/models/equipment/#atlas.Thermal.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/equipment/equipment.py</code> <pre><code>@field_serializer(\"node\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/","title":"Market Models","text":""},{"location":"api/models/market/#atlas.MarketArea","title":"atlas.MarketArea","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>control_block</code> <code>ControlBlock</code> <p>Associated Control block</p> required <code>co2_emissions</code> <code>ForecastingMatrix</code> <p>CO2 emissions at the end of each Portfolio Optimization</p> required <code>id_balance</code> <code>ForecastingMatrix</code> <p>Net position of the zone on the various Intraday market clearings (sales - purchases)</p> required <code>id_price</code> <code>ForecastingMatrix</code> <p>Market area prices from each Intraday clearing market</p> required <code>id_price_forecast</code> <code>ForecastingMatrix</code> <p>Sum of volume of sell offers on the Day Ahead market</p> required <code>price_forecast_high</code> <code>ForecastingMatrix</code> <p>High price scenario of energy price forecasts for the various markets (ID, DA and Balancing) for different forecasting horizons over the different deadlines</p> required <code>price_forecast_low</code> <code>ForecastingMatrix</code> <p>Low price scenario of energy price forecasts for the various markets (ID, DA and Balancing) for different forecasting horizons over the different deadlines</p> required <code>price_forecast_medium</code> <code>ForecastingMatrix</code> <p>Medium price scenario of energy price forecasts for the various markets (ID, DA and Balancing) for different forecasting horizons (ID, DA and Balancing) over the different deadlines</p> required <code>afrr_activation_price</code> <code>Timeseries</code> <p>Activation price of aFRR (from MARI Clearing)</p> required <code>da_balance</code> <code>Timeseries</code> <p>Net position of the zone for the Day Ahead market clearing (sales - purchases)</p> required <code>da_price</code> <code>Timeseries</code> <p>Market area prices from the Day Ahead clearing market</p> required <code>fcr_activation_price</code> <code>Timeseries</code> <p>Activation price of FCR</p> required <code>maximum_price</code> <code>Timeseries</code> <p>Price cap for this market area</p> required <code>minimum_price</code> <code>Timeseries</code> <p>Minimum price allowed for this market area</p> required <code>mfrr_activation_balance</code> <code>Timeseries</code> <p>Net position of the zone after clearing of mFRR reserves</p> required <code>mfrr_activation_price</code> <code>Timeseries</code> <p>Activation price of mFRR</p> required <code>reference_balance</code> <code>Timeseries</code> <p>Zonal reference net position in the base case. Used for the flow-based constraints</p> required <code>rr_activation_balance</code> <code>Timeseries</code> <p>Net position of the zone after clearing of RR reserves</p> required <code>rr_activation_price</code> <code>Timeseries</code> <p>Activation price of RR (from TERRE Clearing)</p> required <code>total_id_balance</code> <code>Timeseries</code> <p>Cumulative sum of the net position from all Intraday market clearings</p> required"},{"location":"api/models/market/#atlas.MarketArea.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/market_area.py</code> <pre><code>@field_serializer(\"control_block\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.MarketBorder","title":"atlas.MarketBorder","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>downhill_control_block</code> <code>ControlBlock</code> <p>Downhill Control Block</p> required <code>uphill_control_block</code> <code>ControlBlock</code> <p>Uphill Control Block</p> required <code>downhill_market_area</code> <code>MarketArea</code> <p>Downhill Market Area</p> required <code>uphill_market_area</code> <code>MarketArea</code> <p>Uphill Market Area</p> required <code>coupling_type</code> <code>CouplingType</code> <p>Sum of volume of sell offers on the Day Ahead market</p> required <code>loss_factor</code> <code>float</code> <p>Network loss factor</p> required <code>time_resolution</code> <code>float</code> <p>Time resolution relative to the border</p> required <code>afrr_down_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of downward aFRR after the market clearing</p> required <code>afrr_up_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of upward aFRR after the market clearing</p> required <code>id_flow</code> <code>ForecastingMatrix</code> <p>Flows from the different Intraday Clearings</p> required <code>id_shadow_price</code> <code>float</code> <p>Shadow prices from the different Intraday Clearings</p> required <code>mfrr_down_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of downward mFRR after the market clearing</p> required <code>mfrr_up_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of upward mFRR after the market clearing</p> required <code>rr_down_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of downward RR after the market clearing</p> required <code>rr_up_procured</code> <code>ForecastingMatrix</code> <p>Cross-border contractualization of upward RR after the market clearing</p> required <code>afrr_activated</code> <code>Timeseries</code> <p>Activated aFRR flow across the border</p> required <code>da_flow</code> <code>Timeseries</code> <p>Cross-border flows from the Day Ahead market clearing</p> required <code>da_shadow_price</code> <code>Timeseries</code> <p>Shadow prices from the Day Ahead market clearing</p> required <code>fcr_activated</code> <code>Timeseries</code> <p>Activated FCR flow across the border</p> required <code>maximum_flow</code> <code>Timeseries</code> <p>Maximum flow from Uphill Market Area to Downhill Market Area</p> required <code>mfrr_activated</code> <code>Timeseries</code> <p>Activated mFRR flow across the border after MARI market</p> required <code>minimum_flow</code> <code>Timeseries</code> <p>Minimum flow from Uphill Market Area to Downhill Market Area</p> required <code>reference_flow</code> <code>Timeseries</code> <p>Cross-border reference flow in the base case</p> required <code>rr_activated</code> <code>Timeseries</code> <p>Activated RR flow across the border after TERRE market</p> required <code>total_id_flow</code> <code>Timeseries</code> <p>Cumulative sum of accepted cross-border quantities from all Intraday market clearings</p> required"},{"location":"api/models/market/#atlas.MarketBorder.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/market_border.py</code> <pre><code>@field_serializer(\n    \"downhill_control_block\", \"uphill_control_block\", \"downhill_market_area\", \"uphill_market_area\", mode=\"plain\"\n)\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.MarketAreaPtdf","title":"atlas.MarketAreaPtdf","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>market_area</code> <code>MarketArea</code> <p>Associated MarketArea</p> required <code>id_ptdf</code> <code>ForecastingMatrix</code> <p>Zonal PTDF (Power Transfer Distribution Factor) for Flow Based Intraday Market(s)</p> required <code>da_ptdf</code> <code>Timeseries</code> <p>Zonal PTDF (Power Transfer Distribution Factor) for Flow Based Day-Ahead Market</p> required"},{"location":"api/models/market/#atlas.MarketAreaPtdf.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/market_area_ptdf.py</code> <pre><code>@field_serializer(\"market_area\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.Order","title":"atlas.Order","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>equipment</code> <code>Equipment</code> <p>Associated Equipment</p> required <code>market_area</code> <code>Market Area</code> <p>Associated Market Area</p> required <code>portfolio</code> <code>Portfolio</code> <p>Associated Portfolio</p> required <code>accepted_power</code> <code>float</code> <p>Accepted power after Clearing</p> required <code>execution_date</code> <code>DateTime</code> <p>Date of offer</p> required <code>start_date</code> <code>DateTime</code> <p>Offer activation start DateTime</p> required <code>end_date</code> <code>DateTime</code> <p>Offer activation end DateTime</p> required <code>individual_spread</code> <code>float</code> <p>\"Profit (in the sense of social welfare) of the offer : _ spot price - offer price for a sell offer _ offer price - spot price for a purchase offer This difference is then multiplied by the power sold/purchased\"</p> required <code>is_agent_tso</code> <code>bool</code> <p>True if Agent is TSO, False otherwise</p> required <code>order_type</code> <code>OrderType</code> <p>Order type (Buy, Sell)</p> required <code>price</code> <code>float</code> <p>Price of the offer</p> required <code>price_group</code> <code>int</code> <p>Price group containing the offer, after Clearing</p> required <code>product</code> <code>Product</code> <p>Type of product</p> required <code>q_max</code> <code>float</code> <p>Maximum quantity that can be accepted in the clearing phase</p> required <code>q_min</code> <code>float</code> <p>Minimum quantity that can be accepted in the clearing phase</p> required"},{"location":"api/models/market/#atlas.Order.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/order.py</code> <pre><code>@field_serializer(\"equipment\", \"market_area\", \"portfolio\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.OrderCoupling","title":"atlas.OrderCoupling","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>orders</code> <code>list[Order]</code> <p>List of Order linked</p> required <code>complement_direction</code> <code>ComplementDirection</code> <p>Complement coupling constraint direction (EqualTo, LesserThan, GreaterThan)</p> required <code>complement_energy</code> <code>float</code> <p>Target energy for complement offers</p> required <code>coupling_type</code> <code>CouplingType</code> <p>Offer type (EXCLUSION, COMPLEMENT, IDENTICAL_VOLUME, PARENT_CHILDREN)</p> required"},{"location":"api/models/market/#atlas.NodePtdf","title":"atlas.NodePtdf","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Node</code> <p>Associated Node</p> required <code>id_ptdf</code> <code>ForecastingMatrix</code> <p>Nodal PTDF (Power Transfer Distribution Factor) for Flow Based Intraday Market(s)</p> required <code>da_ptdf</code> <code>ForecastingMatrix</code> <p>Nodal PTDF (Power Transfer Distribution Factor) for Flow Based Day-Ahead Market</p> required"},{"location":"api/models/market/#atlas.NodePtdf.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/node_ptdf.py</code> <pre><code>@field_serializer(\"node\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.CriticalBranch","title":"atlas.CriticalBranch","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>downhill_node</code> <code>Node</code> <p>Downhill node</p> required <code>uphill_node</code> <code>Node</code> <p>Uphill node</p> required <code>market_area_ptdf</code> <code>MarketAreaPtdf</code> <p>Associated Market Area PTDF</p> required <code>node_ptdf</code> <code>NodePtdf</code> <p>Associated Node PTDF</p> required <code>id_flow</code> <code>ForecastingMatrix</code> <p>Power transiting through this critical branch for each Intraday market</p> required <code>id_shadow_price</code> <code>ForecastingMatrix</code> <p>Critical branch shadow prices of the various Intraday clearings, for each execution_date</p> required <code>da_flow</code> <code>Timeseries</code> <p>Power transiting through this critical branch after the Day Ahead market clearing</p> required <code>da_shadow_price</code> <code>Timeseries</code> <p>Critical branch shadow prices for the Day Ahead market clearing</p> required <code>flow_reliability_margin</code> <code>AbstractTimeseries</code> <p>Safety margin to avoid forecast errors or uncertainties, in the Flow Based model</p> required <code>maximum_flow</code> <code>Timeseries</code> <p>Maximum power allowed on this branch</p> required <code>reference_flow</code> <code>Timeseries</code> <p>Reference flow in the base case, i.e. forecasted flow prior to an electricity market</p> required <code>total_id_flow</code> <code>Timeseries</code> <p>Cumulative sum of flows transiting the line for all Intraday clearing operations</p> required"},{"location":"api/models/market/#atlas.CriticalBranch.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/market/critical_branch.py</code> <pre><code>@field_serializer(\"downhill_node\", \"uphill_node\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.Portfolio","title":"atlas.Portfolio","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>control_block</code> <code>ControlBlock</code> <p>Installed capacity</p> required <code>market_area</code> <code>MarketArea</code> <p>Sum of volume of sell offers on the Day Ahead market</p> required <code>id_cleared_quantity</code> <code>ForecastingMatrix</code> <p>Sum of the cleared quantities for all equipment in this portfolio in each Intraday Market Clearing</p> required <code>imbalance</code> <code>ForecastingMatrix</code> <p>Portfolio imbalance at the end of each Portfolio optimization. Positive for excess production, and negative in the opposite case.</p> required <code>power</code> <code>ForecastingMatrix</code> <p>Sum of every power of the Portfolio equipment</p> required <code>afrr_activated</code> <code>Timeseries</code> <p>Volume of aFRR activated</p> required <code>afrr_down_procured</code> <code>float</code> <p>Volume of aFRR reserves contracted downward, summed for all equipment in this portfolio</p> required <code>afrr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of aFRR reserves contracted upward, summed for all equipment in this portfolio</p> required <code>da_cleared_quantity</code> <code>ForecastingMatrix</code> <p>Sum of the cleared quantities for all equipment in this portfolio in the Day Ahead Market Clearing</p> required <code>fcr_activated</code> <code>Timeseries</code> <p>Volume of FCR activated for this portfolio</p> required <code>imbalance_settlement_costs</code> <code>Timeseries</code> <p>Imbalance costs at the end of the Imbalance Settlement Process</p> required <code>mfrr_activated</code> <code>Timeseries</code> <p>Volume of mFRR activated after MARI Clearing</p> required <code>mfrr_down_procured</code> <code>ForecastingMatrix</code> <p>Volume of mFRR reserves contracted downward, summed for all equipment in this portfolio</p> required <code>mfrr_up_procured</code> <code>ForecastingMatrix</code> <p>Volume of mFRR reserves contracted upward, summed for all equipment in this portfolio</p> required <code>rr_activated</code> <code>Timeseries</code> <p>Volume of RR activated</p> required <code>rr_down_procured</code> <code>Timeseries</code> <p>Volume of RR reserves contracted downward, summed for all equipment in this portfolio</p> required <code>rr_up_procured</code> <code>Timeseries</code> <p>Volume of RR reserves contracted upward, summed for all equipment in this portfolio</p> required <code>total_id_cleared_quantity</code> <code>Timeseries</code> <p>Cumulative sum of all cleared quantities for equipment in this portfolio across all Intraday Market Clearings</p> required"},{"location":"api/models/market/#atlas.Portfolio.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/portfolio.py</code> <pre><code>@field_serializer(\"control_block\", \"market_area\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.Node","title":"atlas.Node","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>control_block</code> <code>ControlBlock</code> <p>Associated Control block</p> required <code>market_area</code> <code>MarketArea</code> <p>Associated Market Area</p> required <code>balance_forecast</code> <code>ForecastingMatrix | LazyForecastingMatrix</code> <p>Physical balance forecasts on the node for each execution date</p> required <code>id_power_injection</code> <code>ForecastingMatrix | LazyForecastingMatrix</code> <p>Injection (production - consumption) on the node after Intraday Clearing. May be positive, and thus represent demand May be negative, and thus represent withdrawal</p> required <code>da_power_injection</code> <code>Timeseries</code> <p>Injection (production - consumption) on the node after Day Ahead Clearing. May be negative, and thus represent demand</p> required <code>reference_balance</code> <code>Timeseries</code> <p>Nodal reference program for the base case</p> required"},{"location":"api/models/market/#atlas.Node.serializer_bmo","title":"serializer_bmo","text":"<pre><code>serializer_bmo(value: BusinessModel | None) -&gt; str | None\n</code></pre> <p>Serialize BusinessModel attributes to string.</p> Source code in <code>atlas/models/node.py</code> <pre><code>@field_serializer(\"control_block\", \"market_area\", mode=\"plain\")\ndef serializer_bmo(self, value: BusinessModel | None) -&gt; str | None:\n    \"\"\"Serialize BusinessModel attributes to string.\"\"\"\n    return serializer_business_model(value)\n</code></pre>"},{"location":"api/models/market/#atlas.ControlBlock","title":"atlas.ControlBlock","text":"<p>               Bases: <code>BusinessModel</code></p> <p>Parameters:</p> Name Type Description Default <code>alternative_type</code> <code>ReservesTypes</code> <p>Type of alternative considered for formulating a TSO offer on a balancing market</p> required <code>volume_uncertainty</code> <code>bool</code> <p>True if uncertainty about the volume of the TSO's balancing requirement must be taken into account when formulating bids on a balancing market</p> required <code>afrr_down_required</code> <code>ForecastingMatrix</code> <p>Procurement need of downward aFRR for this Control Block</p> required <code>afrr_up_required</code> <code>ForecastingMatrix</code> <p>Procurement Need of upward aFRR for this Control Block</p> required <code>balancing_mechanism_needs</code> <code>ForecastingMatrix</code> <p>Balancing needs for the Balancing Mechanism</p> required <code>mfrr_down_required</code> <code>ForecastingMatrix</code> <p>Procurement need of downward mFRR for this Control Block</p> required <code>mfrr_needs</code> <code>ForecastingMatrix</code> <p>Balancing needs for mFRR reserves</p> required <code>mfrr_up_required</code> <code>ForecastingMatrix</code> <p>Procurement need of upward mFRR for this Control Block</p> required <code>rr_down_required</code> <code>ForecastingMatrix</code> <p>Procurement need of downward RR for this Control Block</p> required <code>rr_needs</code> <code>ForecastingMatrix</code> <p>Balancing needs for RR reserves</p> required <code>rr_up_required</code> <code>ForecastingMatrix</code> <p>Procurement need of upward RR for this Control Block</p> required <code>spilled_energy</code> <code>ForecastingMatrix | LazyForecastingMatrix</code> <p>Energy lost (due to uncapped overproduction)</p> required <code>unsupplied_energy</code> <code>ForecastingMatrix</code> <p>Energy not distributed in real time, after balancing processes</p> required <code>afrr_activation_costs</code> <code>Timeseries</code> <p>Balancing costs for aFRR reserves</p> required <code>fcr_activation_costs</code> <code>Timeseries</code> <p>Balancing costs for FCR reserves</p> required <code>mfrr_activated</code> <code>Timeseries</code> <p>Volume of mFRR activated</p> required <code>mfrr_activation_costs</code> <code>Timeseries</code> <p>Balancing costs for mFRR reserves</p> required <code>negative_imbalance_price</code> <code>Timeseries</code> <p>Negative Imbalance Settlement Price (ISP-N)</p> required <code>positive_imbalance_price</code> <code>Timeseries</code> <p>Positive Imbalance Settlement Price (ISP-P)</p> required <code>rr_activated</code> <code>AbstractTimeseries</code> <p>Volume of RR activated</p> required <code>rr_activation_costs</code> <code>Timeseries</code> <p>Balancing costs for RR reserves</p> required <code>specific_activated_power</code> <code>Timeseries</code> <p>Power activated by the Balancing Mechanism</p> required <code>specific_activation_costs</code> <code>Timeseries</code> <p>Total balancing cost from the Balancing Mechanism</p> required <code>weighted_balance_price_down</code> <code>AbstractTimeseries</code> <p>Weighted average balancing energy activation price downward</p> required <code>weighted_balance_price_up</code> <code>AbstractTimeseries</code> <p>Weighted average balancing energy activation price upward</p> required"},{"location":"api/solver/interface/","title":"Solver Interface","text":""},{"location":"api/solver/interface/#atlas.OptimisationModel","title":"atlas.OptimisationModel","text":"<pre><code>OptimisationModel(\n    solver_name: SolverEnum | str,\n    name: str | None = None,\n    options: SolverOptions | None = None,\n)\n</code></pre> <p>Unified interface for OR-Tools optimization problems.</p> <p>This class provides a consistent API for different types of optimization problems including linear programming, mixed-integer programming, and constraint programming.</p> <p>Initialize the optimization model.</p> <p>Parameters:</p> Name Type Description Default <code>solver_name</code> <code>SolverEnum | str</code> <p>Specific solver name (e.g., 'GLOP', 'SCIP', 'GUROBI')</p> required <code>name</code> <code>str | None</code> <p>Optional name for the model</p> <code>None</code> <code>options</code> <code>SolverOptions | None</code> <p>Solver options (presolve, duality_gap, etc.)</p> <code>None</code> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def __init__(\n    self,\n    solver_name: SolverEnum | str,\n    name: str | None = None,\n    options: SolverOptions | None = None,\n):\n    \"\"\"\n    Initialize the optimization model.\n\n    :param solver_name: Specific solver name (e.g., 'GLOP', 'SCIP', 'GUROBI')\n    :type solver_name: Optional[str]\n    :param name: Optional name for the model\n    :type name: Optional[str]\n    :param options: Solver options (presolve, duality_gap, etc.)\n    :type options: Optional[SolverOptions]\n    \"\"\"\n    self.name = name\n    self._solver = None\n    self._variables_name: set[str] = set()\n    self._constraints_name: set[str] = set()\n    self._objective: Any | None = None\n    self._objective_direction: Literal[\"maximize\", \"minimize\"] | None = None\n    self._solution_info: SolutionInfo | None = None\n    self._options: SolverOptions = options if options is not None else SolverOptions()\n\n    self._initialize_solver(solver_name)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.constraints","title":"constraints  <code>property</code>","text":"<pre><code>constraints: set[str]\n</code></pre> <p>Return the set of constraints.</p>"},{"location":"api/solver/interface/#atlas.OptimisationModel.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name = name\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.options","title":"options  <code>property</code>","text":"<pre><code>options: SolverOptions\n</code></pre> <p>Return the current solver options.</p>"},{"location":"api/solver/interface/#atlas.OptimisationModel.solution_info","title":"solution_info  <code>property</code>","text":"<pre><code>solution_info: SolutionInfo | None\n</code></pre> <p>Return the last computed solution info.</p>"},{"location":"api/solver/interface/#atlas.OptimisationModel.solver","title":"solver  <code>property</code>","text":"<pre><code>solver: Solver\n</code></pre> <p>Return the underlying OR-Tools solver instance.</p>"},{"location":"api/solver/interface/#atlas.OptimisationModel.variables","title":"variables  <code>property</code>","text":"<pre><code>variables: set[str]\n</code></pre> <p>Return the set of decision variables.</p>"},{"location":"api/solver/interface/#atlas.OptimisationModel.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>String representation of the model.</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"String representation of the model.\"\"\"\n    return f\"OptimisationModel(name={self.name},solver={self.solver_name.value})\"\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.add_boolean_variable","title":"add_boolean_variable","text":"<pre><code>add_boolean_variable(name: str) -&gt; Any\n</code></pre> <p>Add a boolean variable to the model.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Variable name</p> required <p>Returns:</p> Type Description <code>pywraplp.Variable</code> <p>OR-Tools variable object that can be used in expressions</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def add_boolean_variable(self, name: str) -&gt; Any:\n    \"\"\"\n    Add a boolean variable to the model.\n\n    :param name: Variable name\n    :type name: str\n    :return: OR-Tools variable object that can be used in expressions\n    :rtype: pywraplp.Variable\n    \"\"\"\n    logger.debug(f\"Adding boolean variable '{name}'\")\n    if name in self._variables_name:\n        raise ValueError(f\"Variable '{name}' already exists\")\n\n    var = self._solver.BoolVar(name)\n    self._variables_name.add(name)\n    return var\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.add_constraint","title":"add_constraint","text":"<pre><code>add_constraint(\n    constraint_expr: Any, name: str | None = None\n) -&gt; None\n</code></pre> <p>Add a constraint using OR-Tools expression syntax.</p> <p>This method allows you to pass constraints directly like: model.add_constraint(x + y &lt;= 10, \"sum_constraint\") model.add_constraint(2 * x + 3 * y &gt;= 5, \"min_constraint\") model.add_constraint(x == y, \"equality_constraint\")</p> <p>Parameters:</p> Name Type Description Default <code>constraint_expr</code> <code>Any</code> <p>OR-Tools constraint expression</p> required <code>name</code> <code>str | None</code> <p>Optional constraint name</p> <code>None</code> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def add_constraint(self, constraint_expr: Any, name: str | None = None) -&gt; None:\n    \"\"\"\n    Add a constraint using OR-Tools expression syntax.\n\n    This method allows you to pass constraints directly like:\n    model.add_constraint(x + y &lt;= 10, \"sum_constraint\")\n    model.add_constraint(2 * x + 3 * y &gt;= 5, \"min_constraint\")\n    model.add_constraint(x == y, \"equality_constraint\")\n\n    :param constraint_expr: OR-Tools constraint expression\n    :type constraint_expr: Any (OR-Tools constraint object)\n    :param name: Optional constraint name\n    :type name: Optional[str]\n    \"\"\"\n    if name is None:\n        name = f\"constraint_{len(self._constraints_name)}\"\n\n    if name in self._constraints_name:\n        raise ValueError(f\"Constraint '{name}' already exists\")\n\n    logger.debug(f\"Adding constraint: {name}\")\n\n    self._solver.Add(constraint_expr, name)\n    self._constraints_name.add(name)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.add_continuous_variable","title":"add_continuous_variable","text":"<pre><code>add_continuous_variable(\n    name: str,\n    lower_bound: float = float(\"-inf\"),\n    upper_bound: float = float(\"inf\"),\n) -&gt; Any\n</code></pre> <p>Add a continuous variable to the model.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Variable name</p> required <code>lower_bound</code> <code>float</code> <p>Lower bound for the variable</p> <code>float('-inf')</code> <code>upper_bound</code> <code>float</code> <p>Upper bound for the variable</p> <code>float('inf')</code> <p>Returns:</p> Type Description <code>pywraplp.Variable</code> <p>OR-Tools variable object that can be used in expressions</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def add_continuous_variable(\n    self,\n    name: str,\n    lower_bound: float = float(\"-inf\"),\n    upper_bound: float = float(\"inf\"),\n) -&gt; Any:\n    \"\"\"\n    Add a continuous variable to the model.\n\n    :param name: Variable name\n    :type name: str\n    :param lower_bound: Lower bound for the variable\n    :type lower_bound: float\n    :param upper_bound: Upper bound for the variable\n    :type upper_bound: float\n    :return: OR-Tools variable object that can be used in expressions\n    :rtype: pywraplp.Variable\n    \"\"\"\n    logger.debug(f\"Adding continuous variable '{name}' with bounds [{lower_bound}, {upper_bound}]\")\n    if name in self._variables_name:\n        raise ValueError(f\"Variable '{name}' already exists\")\n\n    var = self._solver.NumVar(lower_bound, upper_bound, name)\n    self._variables_name.add(name)\n    return var\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.add_integer_variable","title":"add_integer_variable","text":"<pre><code>add_integer_variable(\n    name: str,\n    lower_bound: float = 0.0,\n    upper_bound: float = float(\"inf\"),\n) -&gt; Any\n</code></pre> <p>Add a integer variable to the model.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Variable name</p> required <code>lower_bound</code> <code>float</code> <p>Lower bound for the variable</p> <code>0.0</code> <code>upper_bound</code> <code>float</code> <p>Upper bound for the variable</p> <code>float('inf')</code> <p>Returns:</p> Type Description <code>pywraplp.Variable</code> <p>OR-Tools variable object that can be used in expressions</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def add_integer_variable(\n    self,\n    name: str,\n    lower_bound: float = 0.0,\n    upper_bound: float = float(\"inf\"),\n) -&gt; Any:\n    \"\"\"\n    Add a integer variable to the model.\n\n    :param name: Variable name\n    :type name: str\n    :param lower_bound: Lower bound for the variable\n    :type lower_bound: float\n    :param upper_bound: Upper bound for the variable\n    :type upper_bound: float\n    :return: OR-Tools variable object that can be used in expressions\n    :rtype: pywraplp.Variable\n    \"\"\"\n    logger.debug(f\"Adding integer variable '{name}' with bounds [{lower_bound}, {upper_bound}]\")\n    if name in self._variables_name:\n        raise ValueError(f\"Variable '{name}' already exists\")\n\n    var = self._solver.IntVar(lower_bound, upper_bound, name)\n    self._variables_name.add(name)\n    return var\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.add_objective","title":"add_objective","text":"<pre><code>add_objective(objective_expr: Any) -&gt; None\n</code></pre> <p>Add terms to the objective function.</p> <p>This method allows you to incrementally build the objective function by adding terms one at a time. The optimization direction must be set using set_direction() before calling this method.</p> <p>Example</p> <pre><code>model.set_direction(\"maximize\")\nmodel.add_objective(x + 2 * y)\nmodel.add_objective(3 * z)  # Adds to existing objective\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>objective_expr</code> <code>Any</code> <p>OR-Tools expression to add to the objective</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction has not been set via set_direction()</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def add_objective(self, objective_expr: Any) -&gt; None:\n    \"\"\"\n    Add terms to the objective function.\n\n    This method allows you to incrementally build the objective function by adding\n    terms one at a time. The optimization direction must be set using set_direction()\n    before calling this method.\n\n    **Example**\n\n        model.set_direction(\"maximize\")\n        model.add_objective(x + 2 * y)\n        model.add_objective(3 * z)  # Adds to existing objective\n\n    :param objective_expr: OR-Tools expression to add to the objective\n    :type objective_expr: Any (OR-Tools expression object)\n    :raises ValueError: If direction has not been set via set_direction()\n    \"\"\"\n    if self._objective_direction is None:\n        raise ValueError(\n            \"Optimization direction must be set before adding objective terms. \"\n            \"Call set_direction('maximize') or set_direction('minimize') first.\"\n        )\n\n    if self._objective is None:\n        self._objective = objective_expr\n    else:\n        self._objective = self._objective + objective_expr\n\n    self._update_solver_objective()\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Clear the model and reset all variables and constraints.</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def clear(self) -&gt; None:\n    \"\"\"Clear the model and reset all variables and constraints.\"\"\"\n    self._variables_name.clear()\n    self._constraints_name.clear()\n    self._solution_info = None\n    self._objective = None\n    self._objective_direction = None\n    self._initialize_solver(self.solver_name)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.export_model","title":"export_model","text":"<pre><code>export_model(filename: str | Path) -&gt; None\n</code></pre> <p>Export the model to a file.</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str | Path</code> <p>Output filename</p> required <code>format_type</code> <code>str</code> <p>Export format ('lp', 'mps')</p> required Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def export_model(self, filename: str | Path) -&gt; None:\n    \"\"\"\n    Export the model to a file.\n\n    :param filename: Output filename\n    :type filename: str\n    :param format_type: Export format ('lp', 'mps')\n    :type format_type: str\n    \"\"\"\n    logger.debug(f\"Exporting model to '{filename}'\")\n\n    lp = self._solver.ExportModelAsLpFormat(False)\n\n    with open(filename, \"w\") as f:\n        f.write(lp)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.get_constraint","title":"get_constraint","text":"<pre><code>get_constraint(name: str) -&gt; Any\n</code></pre> <p>Get a constraint object by name for use in expressions.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Constraint name</p> required <p>Returns:</p> Type Description <code>pywraplp.Constraint</code> <p>OR-Tools constraint object</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If constraint doesn't exist</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def get_constraint(self, name: str) -&gt; Any:\n    \"\"\"\n    Get a constraint object by name for use in expressions.\n\n    :param name: Constraint name\n    :type name: str\n    :return: OR-Tools constraint object\n    :rtype: pywraplp.Constraint\n    :raises ValueError: If constraint doesn't exist\n    \"\"\"\n    if name not in self._constraints_name:\n        raise ValueError(f\"Constraint '{name}' not found\")\n    return self._solver.LookupConstraint(name)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.get_constraint_bounds","title":"get_constraint_bounds","text":"<pre><code>get_constraint_bounds(name: str) -&gt; ConstraintBounds\n</code></pre> <p>Get the bounds of a constraint by name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Constraint name</p> required <p>Returns:</p> Type Description <code>ConstraintBounds</code> <p>Tuple containing (lower_bound, upper_bound)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If constraint doesn't exist</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def get_constraint_bounds(self, name: str) -&gt; ConstraintBounds:\n    \"\"\"\n    Get the bounds of a constraint by name.\n\n    :param name: Constraint name\n    :type name: str\n    :return: Tuple containing (lower_bound, upper_bound)\n    :rtype: ConstraintBounds\n    :raises ValueError: If constraint doesn't exist\n    \"\"\"\n    constraint = self.get_constraint(name)\n    return ConstraintBounds(\n        lower_bound=constraint.lb(),\n        upper_bound=constraint.ub(),\n    )\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.get_constraint_slack_value","title":"get_constraint_slack_value","text":"<pre><code>get_constraint_slack_value(name: str) -&gt; float\n</code></pre> <p>Get the slack value of a constraint.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>constraint name</p> required <p>Returns:</p> Type Description <code>float</code> <p>Slack value of the constraint</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If model hasn't been solved</p> <code>ValueError</code> <p>If constraint hasn't been added</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def get_constraint_slack_value(self, name: str) -&gt; float:\n    \"\"\"\n    Get the slack value of a constraint.\n\n    :param name: constraint name\n    :type name: str\n    :return: Slack value of the constraint\n    :rtype: float\n    :raises RuntimeError: If model hasn't been solved\n    :raises ValueError: If constraint hasn't been added\n    \"\"\"\n    if not self._solution_info:\n        raise RuntimeError(\"Optimisation model has not been solved yet\")\n\n    if name not in self._constraints_name:\n        raise ValueError(f\"Constraint '{name}' not found in model\")\n\n    constraint = self._solver.LookupConstraint(name)\n    sum_coeff = sum([constraint.GetCoefficient(var) * var.solution_value() for var in self._solver.variables()])\n    slack_value = constraint.ub() - sum_coeff if constraint.ub() != float(\"inf\") else constraint.lb() - sum_coeff\n    return slack_value\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.get_variable","title":"get_variable","text":"<pre><code>get_variable(name: str) -&gt; Any\n</code></pre> <p>Get a variable object by name for use in expressions.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Variable name</p> required <p>Returns:</p> Type Description <code>pywraplp.Variable</code> <p>OR-Tools variable object</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If variable doesn't exist</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def get_variable(self, name: str) -&gt; Any:\n    \"\"\"\n    Get a variable object by name for use in expressions.\n\n    :param name: Variable name\n    :type name: str\n    :return: OR-Tools variable object\n    :rtype: pywraplp.Variable\n    :raises ValueError: If variable doesn't exist\n    \"\"\"\n    if name not in self._variables_name:\n        raise ValueError(f\"Variable '{name}' not found\")\n    return self._solver.LookupVariable(name)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.get_variable_value","title":"get_variable_value","text":"<pre><code>get_variable_value(name: str) -&gt; float\n</code></pre> <p>Get the optimal value of a variable.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Variable name</p> required <p>Returns:</p> Type Description <code>float</code> <p>Variable value</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If model hasn't been solved</p> <code>ValueError</code> <p>If variable hasn't been added</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def get_variable_value(self, name: str) -&gt; float:\n    \"\"\"\n    Get the optimal value of a variable.\n\n    :param name: Variable name\n    :type name: str\n    :return: Variable value\n    :rtype: float\n    :raises RuntimeError: If model hasn't been solved\n    :raises ValueError: If variable hasn't been added\n    \"\"\"\n    if not self._solution_info:\n        raise RuntimeError(\"Optimisation model has not been solved yet\")\n\n    if name not in self._variables_name:\n        raise ValueError(f\"Variable '{name}' not found in solution\")\n\n    return self._solver.LookupVariable(name).solution_value()\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.set_direction","title":"set_direction","text":"<pre><code>set_direction(\n    direction: Literal[\"maximize\", \"minimize\"],\n) -&gt; None\n</code></pre> <p>Set the optimization direction for the model.</p> <p>This method can only be called once. Once set, the direction is immutable and cannot be changed. This ensures consistency when building the objective function incrementally.</p> <p>Parameters:</p> Name Type Description Default <code>direction</code> <code>Literal['maximize', 'minimize']</code> <p>Optimization direction (\"maximize\" or \"minimize\")</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction has already been set</p> <code>ValueError</code> <p>If direction is not \"maximize\" or \"minimize\"</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def set_direction(self, direction: Literal[\"maximize\", \"minimize\"]) -&gt; None:\n    \"\"\"\n    Set the optimization direction for the model.\n\n    This method can only be called once. Once set, the direction is immutable\n    and cannot be changed. This ensures consistency when building the objective\n    function incrementally.\n\n    :param direction: Optimization direction (\"maximize\" or \"minimize\")\n    :type direction: Literal[\"maximize\", \"minimize\"]\n    :raises ValueError: If direction has already been set\n    :raises ValueError: If direction is not \"maximize\" or \"minimize\"\n    \"\"\"\n    if self._objective_direction is not None:\n        raise ValueError(\n            f\"Optimization direction is already set to '{self._objective_direction}' and cannot be changed. \"\n            f\"Direction must be set only once.\"\n        )\n\n    if direction not in [\"maximize\", \"minimize\"]:\n        raise ValueError(f\"Direction must be 'maximize' or 'minimize', got '{direction}'\")\n\n    logger.debug(f\"Setting optimization direction to '{direction}'\")\n    self._objective_direction = direction\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.set_objective","title":"set_objective","text":"<pre><code>set_objective(objective_expr: Any) -&gt; None\n</code></pre> <p>Set the objective function using OR-Tools expression syntax.</p> <p>This method replaces any existing objective function. Use add_objective() if you want to incrementally build the objective. The optimization direction must be set using set_direction() before calling this method.</p> <p>This method allows you to set objectives directly like:</p> <p>Example</p> <pre><code>model.set_direction(\"maximize\")\nmodel.set_objective(x + 2 * y)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>objective_expr</code> <code>Any</code> <p>OR-Tools expression for the objective</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If direction has not been set via set_direction()</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def set_objective(self, objective_expr: Any) -&gt; None:\n    \"\"\"\n    Set the objective function using OR-Tools expression syntax.\n\n    This method replaces any existing objective function. Use add_objective()\n    if you want to incrementally build the objective. The optimization direction\n    must be set using set_direction() before calling this method.\n\n    This method allows you to set objectives directly like:\n\n    **Example**\n\n        model.set_direction(\"maximize\")\n        model.set_objective(x + 2 * y)\n\n    :param objective_expr: OR-Tools expression for the objective\n    :type objective_expr: Any (OR-Tools expression object)\n    :raises ValueError: If direction has not been set via set_direction()\n    \"\"\"\n    if self._objective_direction is None:\n        raise ValueError(\n            \"Optimization direction must be set before setting objective. \"\n            \"Call set_direction('maximize') or set_direction('minimize') first.\"\n        )\n\n    logger.debug(\"Setting objective expression\")\n    self._objective = objective_expr\n    self._update_solver_objective()\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.set_solver_options","title":"set_solver_options","text":"<pre><code>set_solver_options(options: SolverOptions) -&gt; None\n</code></pre> <p>Update solver options.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>SolverOptions</code> <p>New solver options</p> required Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def set_solver_options(self, options: SolverOptions) -&gt; None:\n    \"\"\"\n    Update solver options.\n\n    :param options: New solver options\n    :type options: SolverOptions\n    \"\"\"\n    logger.debug(f\"Updating solver options to: {options}\")\n    self._options = options\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.set_solver_specific_parameters_as_string","title":"set_solver_specific_parameters_as_string","text":"<pre><code>set_solver_specific_parameters_as_string(\n    params: str,\n) -&gt; bool\n</code></pre> <p>Pass solver specific parameters in text format.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>str</code> <p>Specific parameters</p> required <p>Returns:</p> Type Description <code>bool</code> <p>Returns true if the operation was successful.</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def set_solver_specific_parameters_as_string(self, params: str) -&gt; bool:\n    \"\"\"\n    Pass solver specific parameters in text format.\n\n    :param params: Specific parameters\n    :return: Returns true if the operation was successful.\n    \"\"\"\n    return self._solver.SetSolverSpecificParametersAsString(params)\n</code></pre>"},{"location":"api/solver/interface/#atlas.OptimisationModel.solve","title":"solve","text":"<pre><code>solve() -&gt; SolutionInfo\n</code></pre> <p>Solve the optimization problem.</p> <p>Returns:</p> Type Description <code>SolutionInfo</code> <p>Solution information</p> Source code in <code>atlas/solver/solver_interface.py</code> <pre><code>def solve(self) -&gt; SolutionInfo:\n    \"\"\"\n    Solve the optimization problem.\n\n    :return: Solution information\n    :rtype: SolutionInfo\n    \"\"\"\n    self._apply_solver_options()\n\n    with timer() as t:\n        logger.info(f\"Solving the optimisation model {self.name}...\")\n        status = self._solver.Solve()\n    solve_time = t()\n\n    status_map = {\n        pywraplp.Solver.OPTIMAL: SolverStatus.OPTIMAL,\n        pywraplp.Solver.FEASIBLE: SolverStatus.FEASIBLE,\n        pywraplp.Solver.INFEASIBLE: SolverStatus.INFEASIBLE,\n        pywraplp.Solver.UNBOUNDED: SolverStatus.UNBOUNDED,\n        pywraplp.Solver.ABNORMAL: SolverStatus.ABNORMAL,\n        pywraplp.Solver.NOT_SOLVED: SolverStatus.NOT_SOLVED,\n        pywraplp.Solver.MODEL_INVALID: SolverStatus.MODEL_INVALID,\n    }\n\n    mapped_status = status_map.get(status, SolverStatus.NOT_SOLVED)\n    logger.info(f\"Optimisation finished in {solve_time} with status: {mapped_status.name}\")\n\n    objective_value = None\n\n    if status in [pywraplp.Solver.OPTIMAL, pywraplp.Solver.FEASIBLE]:\n        objective_value = self._solver.Objective().Value()\n\n        if objective_value is not None:\n            logger.debug(f\"Objective value: {objective_value}\")\n\n    self._solution_info = SolutionInfo(\n        status=mapped_status,\n        objective_value=objective_value,\n        solve_time=solve_time,\n        num_iterations=self._solver.iterations(),\n    )\n\n    return self._solution_info\n</code></pre>"},{"location":"api/solver/models/","title":"Optimisation Data Models","text":""},{"location":"api/solver/models/#atlas.SolverOptions","title":"atlas.SolverOptions","text":"<p>               Bases: <code>BaseModel</code></p> <p>Container for solver options.</p> <p>Parameters:</p> Name Type Description Default <code>presolve</code> <code>bool</code> <p>Enable/disable presolve</p> required <code>duality_gap</code> <code>float | None</code> <p>Relative MIP gap tolerance (e.g., 0.01 for 1%)</p> required <code>time_limit</code> <code>float | None</code> <p>Time limit in seconds for the solver</p> required <code>num_threads</code> <code>int | None</code> <p>Number of threads to use for parallel solving</p> required"},{"location":"api/solver/models/#atlas.SolverOptions.duality_gap","title":"duality_gap  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>duality_gap: float | None = Field(\n    default=None,\n    ge=0.0,\n    le=1.0,\n    description=\"Relative MIP gap tolerance (e.g., 0.01 for 1%)\",\n)\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverOptions.model_config","title":"model_config  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>model_config = ConfigDict(arbitrary_types_allowed=True)\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverOptions.presolve","title":"presolve  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>presolve: bool = Field(\n    default=True, description=\"Enable/disable presolve\"\n)\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverOptions.time_limit","title":"time_limit  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>time_limit: Duration | None = Field(\n    default=None,\n    description=\"Time limit in seconds for the solver\",\n)\n</code></pre>"},{"location":"api/solver/models/#atlas.SolutionInfo","title":"atlas.SolutionInfo","text":"<p>               Bases: <code>BaseModel</code></p> <p>Container for optimization solution information.</p> <p>Parameters:</p> Name Type Description Default <code>status</code> <code>SolverStatus</code> <p>The solver status</p> required <code>objective_value</code> <code>Optional[float]</code> <p>The optimal objective value if found</p> required <code>solve_time</code> <code>float</code> <p>Time taken to solve in seconds</p> required <code>num_iterations</code> <code>Optional[int]</code> <p>Number of iterations performed</p> required"},{"location":"api/solver/models/#atlas.SolutionInfo.num_iterations","title":"num_iterations  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>num_iterations: int | None = None\n</code></pre>"},{"location":"api/solver/models/#atlas.SolutionInfo.objective_value","title":"objective_value  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>objective_value: float | None = None\n</code></pre>"},{"location":"api/solver/models/#atlas.SolutionInfo.solve_time","title":"solve_time  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>solve_time: str | None = None\n</code></pre>"},{"location":"api/solver/models/#atlas.SolutionInfo.status","title":"status  <code>instance-attribute</code>","text":"<pre><code>status: SolverStatus\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus","title":"atlas.SolverStatus","text":"<p>               Bases: <code>Enum</code></p> <p>Enumeration of solver status codes.</p>"},{"location":"api/solver/models/#atlas.SolverStatus.ABNORMAL","title":"ABNORMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ABNORMAL = 'abnormal'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.FEASIBLE","title":"FEASIBLE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FEASIBLE = 'feasible'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.INFEASIBLE","title":"INFEASIBLE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INFEASIBLE = 'infeasible'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.MODEL_INVALID","title":"MODEL_INVALID  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MODEL_INVALID = 'model_invalid'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.NOT_SOLVED","title":"NOT_SOLVED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NOT_SOLVED = 'not_solved'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.OPTIMAL","title":"OPTIMAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>OPTIMAL = 'optimal'\n</code></pre>"},{"location":"api/solver/models/#atlas.SolverStatus.UNBOUNDED","title":"UNBOUNDED  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>UNBOUNDED = 'unbounded'\n</code></pre>"},{"location":"api/solver/models/#atlas.ConstraintBounds","title":"atlas.ConstraintBounds","text":"<p>               Bases: <code>BaseModel</code></p> <p>Container for constraint bounds.</p> <p>Parameters:</p> Name Type Description Default <code>lower_bound</code> <code>float</code> <p>The lower bound of the constraint</p> required <code>upper_bound</code> <code>float</code> <p>The upper bound of the constraint</p> required"},{"location":"api/solver/models/#atlas.ConstraintBounds.lower_bound","title":"lower_bound  <code>instance-attribute</code>","text":"<pre><code>lower_bound: float\n</code></pre>"},{"location":"api/solver/models/#atlas.ConstraintBounds.upper_bound","title":"upper_bound  <code>instance-attribute</code>","text":"<pre><code>upper_bound: float\n</code></pre>"},{"location":"api/workflow/workflow/","title":"Workflows","text":""},{"location":"api/workflow/workflow/#atlas.Workflow","title":"atlas.Workflow","text":"<pre><code>Workflow(\n    name: str,\n    dataset: AbstractDataset,\n    steps: list[WorkflowStep] | None,\n)\n</code></pre> <p>A structure for managing the sequential execution of multiple modules through a list of workflow steps.</p> <p>Each step processes the output of the previous one, starting from the input dataset.</p> <p>Initialize a Workflow instance.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the workflow.</p> required <code>dataset</code> <code>AbstractDataset</code> <p>The initial input dataset for the workflow.</p> required <code>steps</code> <code>list[WorkflowStep] | None</code> <p>Optional list of workflow steps to execute.</p> required Source code in <code>atlas/workflow/workflow.py</code> <pre><code>def __init__(self, name: str, dataset: AbstractDataset, steps: list[WorkflowStep] | None):\n    \"\"\"Initialize a Workflow instance.\n\n    :param name: Name of the workflow.\n    :type name: str\n    :param dataset: The initial input dataset for the workflow.\n    :type dataset: AbstractDataset\n    :param steps: Optional list of workflow steps to execute.\n    :type steps: list[WorkflowStep] | None\n    \"\"\"\n    self.name = name\n    self.dataset = dataset\n    self._steps: list[WorkflowStep] = steps if steps is not None else []\n</code></pre>"},{"location":"api/workflow/workflow/#atlas.Workflow.steps","title":"steps  <code>property</code>","text":"<pre><code>steps: list[WorkflowStep]\n</code></pre> <p>Access the workflow steps.</p> <p>Returns:</p> Type Description <code>list[WorkflowStep]</code> <p>The list of WorkflowStep instances.</p>"},{"location":"api/workflow/workflow/#atlas.Workflow.add_step","title":"add_step","text":"<pre><code>add_step(step: WorkflowStep) -&gt; None\n</code></pre> <p>Add a single step to the end of the workflow.</p> Source code in <code>atlas/workflow/workflow.py</code> <pre><code>def add_step(self, step: WorkflowStep) -&gt; None:\n    \"\"\"Add a single step to the end of the workflow.\"\"\"\n    self._steps.append(step)\n</code></pre>"},{"location":"api/workflow/workflow/#atlas.Workflow.add_steps","title":"add_steps","text":"<pre><code>add_steps(steps: list[WorkflowStep]) -&gt; None\n</code></pre> <p>Add multiple steps to the end of the workflow.</p> Source code in <code>atlas/workflow/workflow.py</code> <pre><code>def add_steps(self, steps: list[WorkflowStep]) -&gt; None:\n    \"\"\"Add multiple steps to the end of the workflow.\"\"\"\n    self._steps.extend(steps)\n</code></pre>"},{"location":"api/workflow/workflow/#atlas.Workflow.execute","title":"execute","text":"<pre><code>execute() -&gt; None\n</code></pre> <p>Execute the workflow</p> <p>Returns:</p> Type Description <code>None</code> Source code in <code>atlas/workflow/workflow.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"\n    Execute the workflow\n    :return:\n    \"\"\"\n    \"\"\"\n    Execute all workflow steps sequentially.\n\n    Each step receives as input the output of the previous step.\n    The first step receives the workflow's initial dataset.\n    \"\"\"\n    output_dataset: AbstractDataset | None = None\n    for i, step in enumerate(self.steps):\n        step.input_dataset = self.dataset if i == 0 else output_dataset\n        step.execute()\n        output_dataset = step.output_dataset\n</code></pre>"},{"location":"api/workflow/workflow/#atlas.Workflow.get_output_dataset","title":"get_output_dataset","text":"<pre><code>get_output_dataset() -&gt; AbstractDataset | None\n</code></pre> <p>Returns the final dataset of the workflow</p> Source code in <code>atlas/workflow/workflow.py</code> <pre><code>def get_output_dataset(self) -&gt; AbstractDataset | None:\n    \"\"\"Returns the final dataset of the workflow\"\"\"\n    return self.steps[-1].get_output_dataset()\n</code></pre>"},{"location":"api/workflow/workflow_parameters/","title":"Workflow Parameters","text":""},{"location":"api/workflow/workflow_parameters/#atlas.WorkflowParametersParser","title":"atlas.WorkflowParametersParser","text":"<p>A class used to parse the parameters file of a workflow</p>"},{"location":"api/workflow/workflow_parameters/#atlas.WorkflowParametersParser.from_file","title":"from_file  <code>classmethod</code>","text":"<pre><code>from_file(file_path: str | Path) -&gt; WorkflowParameters\n</code></pre> <p>Load parameters from a YAML file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str | Path</code> <p>Path to the parameters file.</p> required <p>Returns:</p> Type Description <code>WorkflowParameters</code> <p>A WorkflowParameters object containing the parsed and validated parameters.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the file extension is not supported.</p> Source code in <code>atlas/workflow/workflow_parameters_parser.py</code> <pre><code>@classmethod\ndef from_file(cls, file_path: str | Path) -&gt; WorkflowParameters:\n    \"\"\"Load parameters from a YAML file.\n\n    :param file_path: Path to the parameters file.\n    :type file_path: str or pathlib.Path\n    :return: A WorkflowParameters object containing the parsed and validated parameters.\n    :rtype: WorkflowParameters\n    :raises ValueError: If the file extension is not supported.\n    \"\"\"\n    file_extension = Path(file_path).suffix\n\n    if file_extension in (\".yaml\", \".yml\"):\n        parameters = cls._parse_yaml(file_path)\n    else:\n        raise ValueError(f\"Unsupported file extension: {file_extension}\")\n\n    return WorkflowParameters.model_validate(parameters)\n</code></pre>"},{"location":"api/workflow/workflow_step/","title":"Workflow Step","text":""},{"location":"api/workflow/workflow_step/#atlas.WorkflowStep","title":"atlas.WorkflowStep","text":"<pre><code>WorkflowStep(\n    name: str,\n    module_parameters: AbstractParameters,\n    module: AbstractModule,\n    input_dataset: AbstractDataset | None = None,\n)\n</code></pre> <p>A step in a workflow, responsible for executing a module using provided parameters and producing an output dataset from an input dataset.</p> <p>Initialize a WorkflowStep.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the workflow step.</p> required <code>module_parameters</code> <code>AbstractParameters</code> <p>Parameters for the module execution.</p> required <code>module</code> <code>AbstractModule</code> <p>Module to be executed in this step.</p> required <code>input_dataset</code> <code>AbstractDataset | None</code> <p>Input dataset for the module, optional.</p> <code>None</code> Source code in <code>atlas/workflow/workflow_step.py</code> <pre><code>def __init__(\n    self,\n    name: str,\n    module_parameters: AbstractParameters,\n    module: AbstractModule,\n    input_dataset: AbstractDataset | None = None,\n):\n    \"\"\"\n    Initialize a WorkflowStep.\n\n    :param name: Name of the workflow step.\n    :param module_parameters: Parameters for the module execution.\n    :param module: Module to be executed in this step.\n    :param input_dataset: Input dataset for the module, optional.\n    \"\"\"\n    self.name: str = name\n    self.module: AbstractModule = module\n    self.module_parameters: AbstractParameters = module_parameters\n    self.input_dataset: AbstractDataset | None = input_dataset\n    self._output_dataset: AbstractDataset | None = None\n</code></pre>"},{"location":"api/workflow/workflow_step/#atlas.WorkflowStep.output_dataset","title":"output_dataset  <code>property</code>","text":"<pre><code>output_dataset: AbstractDataset | None\n</code></pre> <p>Output dataset produced after executing the step.</p> <p>Returns:</p> Type Description <code>AbstractDataset | None</code> <p>An AbstractDataset or None if not yet executed.</p>"},{"location":"api/workflow/workflow_step/#atlas.WorkflowStep.execute","title":"execute","text":"<pre><code>execute() -&gt; None\n</code></pre> <p>Execute the step's module with the given parameters and input dataset. Stores the resulting dataset as output.</p> Source code in <code>atlas/workflow/workflow_step.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"\n    Execute the step's module with the given parameters and input dataset.\n    Stores the resulting dataset as output.\n    \"\"\"\n    self._output_dataset = self.module.execute(self.module_parameters, self.input_dataset)\n</code></pre>"},{"location":"api/workflow/workflow_step/#atlas.WorkflowStep.get_output_dataset","title":"get_output_dataset","text":"<pre><code>get_output_dataset() -&gt; AbstractDataset | None\n</code></pre> <p>Get the output dataset produced by this workflow step.</p> <p>Returns:</p> Type Description <code>AbstractDataset | None</code> <p>An AbstractDataset or None if not yet executed.</p> Source code in <code>atlas/workflow/workflow_step.py</code> <pre><code>def get_output_dataset(self) -&gt; AbstractDataset | None:\n    \"\"\"\n    Get the output dataset produced by this workflow step.\n\n    :return: An AbstractDataset or None if not yet executed.\n    \"\"\"\n    return self._output_dataset\n</code></pre>"},{"location":"concepts/overview/","title":"Overview","text":""},{"location":"modules/market-clearing/","title":"Market Clearing Module","text":""},{"location":"modules/market-clearing/#overview","title":"Overview","text":"<p>The Market Clearing module is responsible for determining the market equilibrium by matching supply and demand across multiple market areas while respecting economic and network constraints.</p> <p>Its main objective is to compute:</p> <ul> <li>Accepted quantities for each market order</li> <li>Market clearing prices per area</li> <li>Cross-border exchanges between areas</li> </ul>"},{"location":"modules/market-clearing/#documentation","title":"Documentation","text":""},{"location":"modules/market-clearing/#user-guide","title":"User Guide","text":"<p>How to use the module:</p> <ul> <li>Overview: Introduction and basic usage</li> <li>Parameters: Configuration reference</li> <li>Running: Execution details</li> </ul>"},{"location":"modules/market-clearing/#developer-reference","title":"Developer Reference","text":"<p>Technical documentation for contributors:</p> <ul> <li>Architecture: Module design</li> <li>API Reference: Class and function documentation</li> <li>Contributing: Development guidelines</li> <li>Examples: Development examples</li> </ul>"},{"location":"modules/market-clearing/developer/architecture/","title":"Module Architecture","text":""},{"location":"modules/market-clearing/developer/architecture/#overview","title":"Overview","text":"<p>The Market Clearing module follows ATLAS's <code>AbstractModule</code> pattern, implementing standard lifecycle methods for data import, validation, execution, and export.</p>"},{"location":"modules/market-clearing/developer/architecture/#module-structure","title":"Module Structure","text":"<pre><code>market_clearing/\n\u251c\u2500\u2500 main.py                              # Execution entry point\n\u251c\u2500\u2500 market_clearing_constants.py         # Global constants\n\u251c\u2500\u2500 market_clearing_parameters.py        # Configuration parameters\n\u251c\u2500\u2500 market_clearing_input_dataset.py     # Input data aggregation\n\u251c\u2500\u2500 market_clearing_output_dataset.py    # Output data aggregation\n\u251c\u2500\u2500 marker_clearing_module.py            # Core clearing module\n\u251c\u2500\u2500 price_group.py                       # group of market area with the same price for every timestep\n\u2514\u2500\u2500 models/                              # Market Clearing models\n\u2502   \u251c\u2500\u2500 control_block_mc.py\n\u2502   \u251c\u2500\u2500 critical_branch_mc.py\n\u2502   \u251c\u2500\u2500 market_area_mc.py\n\u2502   \u251c\u2500\u2500 market_area_ptdf_mc.py\n\u2502   \u251c\u2500\u2500 market_border_mc.py\n\u2502   \u251c\u2500\u2500 order_mc.py\n\u2502   \u2514\u2500\u2500 order_coupling_mc.py\n\u2514\u2500\u2500 phases/                              # Market Clearing phases\n\u2502   \u251c\u2500\u2500 clearing.py                      # First phase of market clearing\n\u2502   \u251c\u2500\u2500 exchange_fixing.py               # second phase of market clearing\n\u2502   \u251c\u2500\u2500 pricing.py                       # Third phase of market clearing\n\u2502   \u251c\u2500\u2500 marginal_fixing.py               # Fourth phase of market clearing\n\u2502   \u251c\u2500\u2500 market_clearing_results.py       # Export of files\n</code></pre>"},{"location":"modules/market-clearing/developer/architecture/#core-classes","title":"Core Classes","text":""},{"location":"modules/market-clearing/developer/architecture/#marketclearingmodule","title":"MarketClearingModule","text":"<p>Implements <code>AbstractModule</code> with methods: - <code>get_parameters_class()</code>: Returns <code>MarketClearingParameters</code> - <code>import_data()</code>: Creates <code>MarketClearingInputDataset</code> - <code>validate_data()</code>: Nothing is done - <code>execute()</code>: Run every step of the Market Clearing. Returns <code>MarketClearingOutputDataset</code> - <code>validates_results()</code>: Nothing is done - <code>export_results()</code>: Run <code>MarketClearingResults</code></p>"},{"location":"modules/market-clearing/developer/architecture/#marketclearingparameters","title":"MarketClearingParameters","text":"<p>Pydantic model inheriting from <code>AbstractParameters</code>. Defines all configuration parameters (see Parameters).</p>"},{"location":"modules/market-clearing/developer/architecture/#marketclearinginputdataset","title":"MarketClearingInputDataset","text":"<ul> <li>Converts business models to market-clearing-specific models (more information below on the section about them)</li> <li>Filter to input to keep only useful data</li> </ul>"},{"location":"modules/market-clearing/developer/architecture/#marketclearingoutputdataset","title":"MarketClearingOutputDataset","text":"<p>Update dataset with the result of MarketClearing : All modification of attributes are write in docstring of MarketClearingOutput</p>"},{"location":"modules/market-clearing/developer/architecture/#models","title":"Models","text":""},{"location":"modules/market-clearing/developer/architecture/#ordermc","title":"OrderMC","text":"<ul> <li>Test if the order is feasible</li> <li>Add timestep of use</li> <li>Add information about associated OrderCoupling</li> </ul>"},{"location":"modules/market-clearing/developer/architecture/#ordercouplingmc","title":"OrderCouplingMC","text":""},{"location":"modules/market-clearing/developer/architecture/#-test-if-the-ordercoupling-is-feasible","title":"- Test if the OrderCoupling is feasible","text":""},{"location":"modules/market-clearing/developer/architecture/#pricegroup","title":"PriceGroup","text":"<p>Keep information of price for every timestep : - Min/Max price - List of MarketArea with the same price</p>"},{"location":"modules/market-clearing/developer/architecture/#data-flow","title":"Data Flow","text":"<pre><code>run(raw_data, raw_params)\n  \u2193\nimport_parameters() \u2192 MarketClearingParameters\n  \u2193\nimport_data() \u2192 MarketClearingInputDataset\n  \u2193\nvalidate_data()\n  \u2193\nexecute()\n  \u251c\u2192 Clearing.run()\n  \u251c\u2192 ExchangeFixing.run()\n  \u251c\u2192 Pricing.run()\n  \u251c\u2192 MarginalFIxing.run()\n  \u2514\u2192 MarketClearingResult.runt()\n  \u2193\nvalidates_results()\n  \u2193\nexport_results() \u2192 update price value of equipments/portfolios and flow value\n</code></pre>"},{"location":"modules/market-clearing/developer/architecture/#key-design-patterns","title":"Key Design Patterns","text":"<p>Module Pattern: Follows <code>AbstractModule</code> for consistent lifecycle</p> <p>Pydantic Models: Parameters and data validated via Pydantic</p> <p>Solver Interface: Uses ATLAS <code>OptimisationModel</code> for solver abstraction</p>"},{"location":"modules/market-clearing/user-guide/input-data/","title":"Parameters","text":""},{"location":"modules/market-clearing/user-guide/input-data/#overview","title":"Overview","text":"<p>The Market Clearing module is configured through <code>MarketClearingParameters</code>. Parameters can be provided as a dictionary or loaded from a JSON/YAML file.</p>"},{"location":"modules/market-clearing/user-guide/input-data/#required-parameters","title":"Required Parameters","text":"<p>These parameters are inherited from <code>AbstractParameters</code>:</p> <ul> <li><code>start_date</code> (DateTime): Start of the optimization period</li> <li><code>end_date</code> (DateTime): End of the optimization period</li> <li><code>execution_date</code> (DateTime): Date when the optimization is executed</li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#market-clearing-parameters","title":"Market Clearing \u2013 Parameters","text":"<p>This document describes all configuration parameters available for the Market Clearing module. The structure, level of detail, and wording are aligned with the parameter documentation of the Market Clearing module to ensure consistency across the project.</p>"},{"location":"modules/market-clearing/user-guide/input-data/#solver-configuration","title":"Solver Configuration","text":"<ul> <li> <p><code>solver_name</code> (<code>SolverEnum</code>, default: <code>XPRESS</code>): Optimization solver used for the market clearing problem.</p> </li> <li> <p>Options: <code>\"XPRESS\"</code>, <code>\"PNE\"</code>, <code>\"GLOP\"</code>, <code>\"SCIP\"</code>, <code>\"CP-SAT\"</code>, <code>\"CBC\"</code></p> </li> <li> <p><code>use_presolve</code> (<code>bool</code>, default: <code>True</code>): Enable a presolve step before executing all optimization in the market clearing.</p> </li> <li> <p><code>True</code>: Presolve is applied (recommended for performance)</p> </li> <li> <p><code>False</code>: Presolve is skipped</p> </li> <li> <p><code>export_lp</code> (<code>bool</code>, default: <code>False</code>): Export solver LP files for debugging and analysis.</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#market-definition","title":"Market Definition","text":"<ul> <li> <p><code>market</code> (<code>Product</code>, default: <code>DayAhead</code>): What type of clearing is execute.</p> </li> <li> <p>Only orders matching this market name are considered</p> </li> <li> <p>Not case-sensitive</p> </li> <li> <p><code>time_step</code> (<code>Duration</code>, default: <code>1 hour</code>): Time resolution of the studied market.</p> </li> <li> <p>Must be strictly greater than 0</p> </li> <li> <p><code>execution_datetime_tolerance</code> (<code>int</code>, default: <code>5 minutes</code>): Tolerance window used to handle overlapping markets (Intraday or Balancing).</p> </li> <li> <p>Must be greater than the execution date difference between consecutive order formulation and clearing</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#market-scope","title":"Market Scope","text":"<ul> <li> <p><code>market_area_names</code> (<code>str | list[str]</code>, default: <code>\"All\"</code>): Market areas included in the Market Clearing.</p> </li> <li> <p><code>\"All\"</code>: All market areas are included</p> </li> <li> <p>List of strings: Explicit selection (e.g. <code>[\"FR\", \"DE\"]</code>)</p> </li> <li> <p><code>control_block_names</code> (<code>str | list[str]</code>, default: <code>\"All\"</code>): Control blocks included in the Market Clearing.</p> </li> <li> <p><code>\"All\"</code>: All control blocks are included</p> </li> <li>List of strings: Explicit selection (e.g. <code>[\"CB_FR\", \"CB_DE\"]</code>)</li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#exchange-network-constraints","title":"Exchange &amp; Network Constraints","text":"<ul> <li> <p><code>exchange_constraints_type</code> (<code>ExchangeConstraintsType</code>, default: <code>ATC</code>): Type of constraints applied to exchanges between market areas.</p> </li> <li> <p><code>ATC</code>: Available Transfer Capacity</p> </li> <li> <p><code>FB</code>: Flow-Based constraints</p> </li> <li> <p><code>prevent_adverse_flows</code> (<code>bool</code>, default: <code>False</code>): Prevent adverse flows during the pricing phase.</p> </li> <li> <p>Not used in Flow-Based configurations</p> </li> <li> <p><code>activate_constrained_tso_quantity</code> (<code>bool</code>, default: <code>False</code>): Used in Balancing markets only.</p> </li> <li> <p>Enforces constraint on TSO sold/bought quantities</p> </li> <li>TSO quantities can only be accepted if sufficient opposite offers exist</li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#objective-function-modifiers","title":"Objective Function Modifiers","text":"<ul> <li> <p><code>price_modifier_lambda_1</code> (<code>float</code>, default: <code>0</code>): Price modifier allowing artificial price adjustments to improve convergence or stability.</p> </li> <li> <p><code>flow_penalty_lambda_2</code> (<code>float</code>, default: <code>0</code>): Penalizes total exchanges through borders in the clearing objective.</p> </li> <li> <p><code>flow_penalty_lambda_3</code> (<code>float</code>, default: <code>0</code>): Penalizes non-maximal exchanges through borders.</p> </li> <li> <p><code>flow_penalty_lambda_4</code> (<code>float</code>, default: <code>0</code>): Penalizes non-minimal exchanges through borders.</p> </li> <li> <p><code>market_price_penalty_alpha</code> (<code>float</code>, default: <code>10</code>): Penalty coefficient favoring the minimization of individual market prices.</p> </li> <li> <p><code>market_price_penalty_beta</code> (<code>float</code>, default: <code>20</code>): Penalty coefficient favoring the minimization of absolute market prices.</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#constraints","title":"Constraints","text":"<ul> <li> <p><code>fb_branch_load_slack_penalty</code> (<code>float</code>, default: <code>200</code>): Penalty coefficient encouraging minimization of slack variables on flow-based branch constraints during pricing.</p> </li> <li> <p><code>paradoxically_accepted_penalty_M</code> (<code>float</code>, default: <code>10000</code>): Large penalty used to minimize paradoxically accepted bids during price fixing.</p> </li> <li> <p><code>paradoxically_rejected_penalty_N</code> (<code>float</code>, default: <code>1000</code>): Large penalty used to minimize paradoxically rejected bids during price fixing.</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#numerical-stability","title":"Numerical Stability","text":"<ul> <li> <p><code>allowed_round_off_error</code> (<code>float</code>, default: <code>0.001 MW</code>): Threshold below which accepted power values are considered equal to zero.</p> </li> <li> <p>Typical values: <code>0.001</code>, <code>0.0001</code>, <code>0.00001</code></p> </li> <li> <p><code>initial_max_price</code> (<code>int</code>, default: <code>100000000</code>): Initial upper bound for market prices.</p> </li> <li> <p><code>initial_min_price</code> (<code>int</code>, default: <code>-100000000</code>): Initial lower bound for market prices.</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#output-configuration","title":"Output Configuration","text":"<ul> <li> <p><code>output_dataset_path</code> (<code>str | None</code>, default: <code>None</code>): Path where the market clearing output dataset is exported.</p> </li> <li> <p><code>output_path</code> (<code>str</code>, default: <code>\"\"</code>): Path where the market clearing outputs are exported (csv and lp).</p> </li> <li> <p><code>export_csv</code> (<code>boolean</code>, default: <code>False</code>): True if output csv files are exported else False .</p> </li> </ul>"},{"location":"modules/market-clearing/user-guide/input-data/#includes-offers-market-areas-order-couplings-and-market-borders","title":"* Includes offers, market areas, order couplings, and market borders","text":""},{"location":"modules/market-clearing/user-guide/input-data/#example-configuration","title":"Example Configuration","text":"<pre><code>time_step: \"60m\"\nstart_date:  \"2028-09-27 00:00:00\"\nend_date: \"2028-09-28 00:00:00\"\nexecution_date: \"2028-09-26 12:00:00\"\nactivate_constrained_tso_quantity: False\nprevent_adverse_flows: False\nfb_branch_load_slack_penalty: 200\ncontrol_block_names: \"All\"\nmarket_area_names: \"All\"\nexchange_constraints_type: \"ATC\"\nprice_modifier_lambda_1: 0\nflow_penalty_lambda_2: 0\nflow_penalty_lambda_3: 0\nflow_penalty_lambda_4: 0\nmarket_price_penalty_alpha: 10\nmarket_price_penalty_beta: 20\nparadoxically_accepted_penalty_M: 1_000\nparadoxically_rejected_penalty_N: 10_000\nsolver: \"XPRESS\"\nuse_presolve: True\nlog_level: \"DEBUG\"\nproduct: \"DayAhead\"\nexport_lp: True\nexport_csv: True\nallowed_round_off_error: 1e-3\nexecution_datetime_tolerance: 5\noutput_path: \"\"\n</code></pre>"},{"location":"modules/market-clearing/user-guide/input-data/#next-steps","title":"Next Steps","text":"<ul> <li>Running: How to execute the module</li> </ul>"},{"location":"modules/market-clearing/user-guide/overview/","title":"User Guide Overview","text":""},{"location":"modules/market-clearing/user-guide/overview/#introduction","title":"Introduction","text":"<p>The Market Clearing module is responsible for determining the market equilibrium by matching supply and demand across multiple market areas while respecting economic and network constraints.</p>"},{"location":"modules/market-clearing/user-guide/overview/#how-to-use","title":"How to Use","text":"<p>The module follows ATLAS's standard <code>AbstractModule</code> pattern:</p> <pre><code>from pathlib import Path\n\nfrom atlas.io_utils.input_loader import InputLoader\nfrom atlas.modules.market_clearing import MarketClearingModule\n\nraw_data_path = Path(\"path/to/dataset\")\nraw_params_path = Path(\"path/to/parameters.yml\")\n\nmc_module = MarketClearingModule()\nraw_data = InputLoader.from_directory(raw_data_path)\nmc_module.run(raw_data, raw_params_path)\n</code></pre> <p>Where: - <code>raw_data_path</code>: Path to the dataset to use - <code>raw_data</code>: Dictionary of business model objects (portfolios, equipment, market areas) - <code>raw_params_path</code>: Parameter dictionary or path to JSON/YAML file</p>"},{"location":"modules/market-clearing/user-guide/overview/#module-workflow","title":"Module Workflow","text":"<p>The <code>run()</code> method executes:</p> <ol> <li>Import Parameters: Load <code>MarketClearingParameters</code></li> <li>Import Data: Convert to <code>MarketClearingInputDataset</code></li> <li>Validate: Check timestep consistency</li> <li>Execute: Run all phases and result</li> <li>Export: Update prices of equipments/portfolios and flow</li> </ol> <p>Results are stored directly in the business model objects.</p>"},{"location":"modules/market-clearing/user-guide/overview/#next-steps","title":"Next Steps","text":"<ul> <li>Parameters: Configuration options</li> <li>Running: Execution details</li> </ul>"},{"location":"modules/market-clearing/user-guide/running/","title":"Running the Module","text":""},{"location":"modules/market-clearing/user-guide/running/#basic-usage","title":"Basic Usage","text":"<pre><code>from pathlib import Path\n\nfrom atlas.io_utils.input_loader import InputLoader\nfrom atlas.modules.market_clearing import MarketClearingModule\n\nraw_data_path = Path(\"path/to/dataset\")\nraw_params_path = Path(\"path/to/parameters.yml\")\n\nmc_module = MarketClearingModule()\nraw_data = InputLoader.from_directory(raw_data_path)\nmc_module.run(raw_data, raw_params_path)\n</code></pre>"},{"location":"modules/market-clearing/user-guide/running/#input-data-structure","title":"Input Data Structure","text":"<p>Input data is basically a dictionary of business model objects by type, obtained by a call to <code>InputLoader.from_directory</code>:</p> <pre><code>raw_data = {\n    \"market_area\": [...],\n    \"control_block\": [...],\n    \"order\": [...],\n    \"order_coupling\": [...],\n}\n</code></pre>"},{"location":"modules/market-clearing/user-guide/running/#parameters","title":"Parameters","text":"<p>Provide as dictionary or file path:</p> <pre><code># Dictionary\nparams = {\n    \"start_date\": \"2024-01-01T00:00:00\",\n    \"end_date\": \"2024-01-02T00:00:00\",\n    \"execution_date\": \"2023-12-31T12:00:00\",\n    \"export_result\": true,\n    \"solver\": \"XPRESS\",\n    \"timestep\": \"PT1H\"\n}\n\n# Or JSON file\nmodule.run(raw_data, \"config/parameters.json\")\n</code></pre> <p>See Parameters for full list.</p>"},{"location":"modules/market-clearing/user-guide/running/#key-options","title":"Key Options","text":"<p>How to change the type of Market Clearing ? Set <code>market</code> to DayAhead / IntraDay / ...</p> <p>Solver: Choose solver with <code>solver</code> parameter and configure <code>use_presolve</code></p>"},{"location":"modules/portfolio-optimisation/","title":"Portfolio Optimisation Module","text":""},{"location":"modules/portfolio-optimisation/#overview","title":"Overview","text":"<p>Optimizes energy portfolios including thermal, hydro, storage, solar, wind, and load assets for different market conditions.</p>"},{"location":"modules/portfolio-optimisation/#documentation","title":"Documentation","text":""},{"location":"modules/portfolio-optimisation/#user-guide","title":"User Guide","text":"<p>How to use the module:</p> <ul> <li>Overview: Introduction and basic usage</li> <li>Parameters: Configuration reference</li> <li>Running: Execution details</li> <li>Results: Accessing outputs</li> <li>Examples: Usage examples</li> </ul>"},{"location":"modules/portfolio-optimisation/#developer-reference","title":"Developer Reference","text":"<p>Technical documentation for contributors:</p> <ul> <li>Architecture: Module design</li> <li>API Reference: Class and function documentation</li> <li>Contributing: Development guidelines</li> <li>Examples: Development examples</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/","title":"Module Architecture","text":""},{"location":"modules/portfolio-optimisation/developer/architecture/#overview","title":"Overview","text":"<p>The Portfolio Optimisation module follows ATLAS's <code>AbstractModule</code> pattern, implementing standard lifecycle methods for data import, validation, execution, and export.</p>"},{"location":"modules/portfolio-optimisation/developer/architecture/#module-structure","title":"Module Structure","text":"<pre><code>atlas/modules/portfolio_optimisation/\n\u251c\u2500\u2500 module.py                            # PortfolioOptimisationModule (AbstractModule)\n\u251c\u2500\u2500 parameters.py                        # PortfolioOptimisationParameters\n\u251c\u2500\u2500 input_dataset.py                     # PortfolioOptimisationInputDataset\n\u251c\u2500\u2500 output_dataset.py                    # PortfolioOptimisationOutputDataset\n\u251c\u2500\u2500 portfolio_orchestrator.py            # Orchestrates portfolio optimization\n\u251c\u2500\u2500 portfolio_optimisation_model.py      # OptimisationModel for single portfolio\n\u251c\u2500\u2500 models/                              # Equipment-specific models\n\u2502   \u251c\u2500\u2500 portfolio.py                     # PortfolioPO\n\u2502   \u251c\u2500\u2500 portfolio_equipments.py          # Equipment container\n\u2502   \u251c\u2500\u2500 thermal/                         # Thermal models\n\u2502   \u251c\u2500\u2500 hydro.py, storage.py, etc.       # Other equipment models\n\u2514\u2500\u2500 utils/                               # Utility functions\n</code></pre>"},{"location":"modules/portfolio-optimisation/developer/architecture/#core-classes","title":"Core Classes","text":""},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationmodule","title":"<code>PortfolioOptimisationModule</code>","text":"<p>Implements <code>AbstractModule</code> with methods:</p> <ul> <li><code>get_parameters_class()</code>: Returns <code>PortfolioOptimisationParameters</code></li> <li><code>import_data()</code>: Creates <code>PortfolioOptimisationInputDataset</code></li> <li><code>validate_data()</code>: Validates timestep consistency</li> <li><code>execute()</code>: Runs optimization via <code>PortfolioOptimisationOrchestrator</code></li> <li><code>validates_results()</code>: Validates outputs</li> <li><code>export_results()</code>: Updates equipment and portfolio forecasts</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationparameters","title":"<code>PortfolioOptimisationParameters</code>","text":"<p>Pydantic model inheriting from <code>AbstractParameters</code>. Defines all configuration parameters (see Parameters).</p>"},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationinputdataset","title":"<code>PortfolioOptimisationInputDataset</code>","text":"<p>Converts business models to portfolio-optimisation-specific models:</p> <ul> <li>Groups equipment by portfolio</li> <li>Applies manual activation rules</li> <li>Creates PO-specific models (ThermalPO, HydroPO, StoragePO, etc.)</li> <li>Calculates optimization time windows</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationorchestrator","title":"<code>PortfolioOptimisationOrchestrator</code>","text":"<p>Coordinates optimization for multiple portfolios:</p> <ul> <li>Creates <code>PortfolioOptimisationModel</code> for each portfolio</li> <li>Handles multiprocessing if enabled</li> <li>Returns optimization results</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationmodel","title":"<code>PortfolioOptimisationModel</code>","text":"<p>Extends <code>OptimisationModel</code> (solver interface):</p> <ul> <li>Builds optimization variables, constraints, and objectives</li> <li>Calls solver</li> <li>Returns solution</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#portfoliooptimisationoutputdataset","title":"<code>PortfolioOptimisationOutputDataset</code>","text":"<p>Processes optimization results:</p> <ul> <li>Extracts variable values</li> <li>Updates equipment <code>power</code> forecasts</li> <li>Updates portfolio <code>imbalance</code></li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#equipment-models","title":"Equipment Models","text":"<p>Each equipment type has a PO-specific model (e.g., <code>ThermalPO</code>, <code>HydroPO</code>) that implements:</p> <ul> <li><code>add_variables()</code>: Create decision variables</li> <li><code>add_constraints()</code>: Add equipment constraints</li> <li><code>add_objective()</code>: Add cost/revenue terms</li> <li><code>prefetch_forecasts()</code>: Load forecast data</li> </ul>"},{"location":"modules/portfolio-optimisation/developer/architecture/#data-flow","title":"Data Flow","text":"<pre><code>run(raw_data, raw_params)\n  \u2193\nimport_parameters() \u2192 PortfolioOptimisationParameters\n  \u2193\nimport_data() \u2192 PortfolioOptimisationInputDataset\n  \u2193\nvalidate_data() \u2192 timestep consistency check\n  \u2193\nexecute() \u2192 PortfolioOptimisationOrchestrator\n  \u251c\u2192 PortfolioOptimisationModel.build()\n  \u251c\u2192 PortfolioOptimisationModel.solve()\n  \u2514\u2192 PortfolioOptimisationResult\n  \u2193\nvalidates_results()\n  \u2193\nexport_results() \u2192 update equipment.power, portfolio.imbalance\n</code></pre>"},{"location":"modules/portfolio-optimisation/developer/architecture/#key-design-patterns","title":"Key Design Patterns","text":"<p>Module Pattern: Follows <code>AbstractModule</code> for consistent lifecycle</p> <p>Pydantic Models: Parameters and data validated via Pydantic</p> <p>Multiprocessing: Portfolios optimized in parallel when enabled</p> <p>Solver Interface: Uses ATLAS <code>OptimisationModel</code> for solver abstraction</p>"},{"location":"modules/portfolio-optimisation/user-guide/examples/","title":"Examples","text":""},{"location":"modules/portfolio-optimisation/user-guide/examples/#basic-usage","title":"Basic Usage","text":"<pre><code>from atlas.modules.portfolio_optimisation import PortfolioOptimisationModule\n\nmodule = PortfolioOptimisationModule()\n\ninput_data = {\n    \"portfolio\": [portfolio1],\n    \"thermal\": [thermal1],\n    \"hydro\": [hydro1],\n    \"storage\": [storage1],\n}\n\nparams = {\n    \"start_date\": \"2024-01-01T00:00:00\",\n    \"end_date\": \"2024-01-02T00:00:00\",\n    \"execution_date\": \"2023-12-31T12:00:00\",\n    \"export_result\": True,\n    \"solver_name\": \"XPRESS\",\n    \"timestep\": \"PT1H\"\n}\n\nmodule.run(input_data, params)\n\n# Access results\noptimized_power = thermal1.power.get_forecast(\n    params[\"execution_date\"], params[\"start_date\"], params[\"end_date\"]\n)\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/examples/#using-config-file","title":"Using Config File","text":"<pre><code>{\n  \"start_date\": \"2024-01-01T00:00:00\",\n  \"end_date\": \"2024-01-02T00:00:00\",\n  \"execution_date\": \"2023-12-31T12:00:00\",\n  \"export_result\": true,\n  \"solver_name\": \"XPRESS\",\n  \"timestep\": \"PT1H\",\n}\n</code></pre> <pre><code>module.run(input_data, \"config/parameters.json\")\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/examples/#configuration-examples","title":"Configuration Examples","text":"<p>Individual unit optimization: <pre><code>params = {\"is_portfolio_bidding\": False, ...}\n</code></pre></p> <p>Exclude equipment: <pre><code>params = {\n    \"excluded_technologies\": [\"wind\", \"solar\"],\n    \"excluded_thermal_strategy\": [\"Peak\"],\n    \"excluded_market_areas\": [\"FR\"],\n    ...\n}\n</code></pre></p> <p>Multiprocessing: <pre><code>params = {\"use_multiprocessing\": True, \"max_workers\": 4, ...}\n</code></pre></p> <p>Solver options: <pre><code>params = {\n    \"solver_name\": \"SCIP\",\n    \"solver_timeout\": \"PT300S\",\n    \"solver_duality_gap\": 0.001,\n    \"use_presolve\": True,\n    ...\n}\n</code></pre></p> <p>Adjust penalties: <pre><code>params = {\n    \"imbalance_penalty_offset\": 15,\n    \"small_imbalance_penalty\": 0.15,\n    \"large_imbalance_penalty\": 0.25,\n    \"automated_unprocured_reserves_penalty\": 50000,\n    ...\n}\n</code></pre></p>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/","title":"Parameters","text":""},{"location":"modules/portfolio-optimisation/user-guide/input-data/#overview","title":"Overview","text":"<p>The Portfolio Optimisation module is configured through <code>PortfolioOptimisationParameters</code>. Parameters can be provided as a dictionary or loaded from a JSON/YAML file.</p>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#required-parameters","title":"Required Parameters","text":"<p>These parameters are inherited from <code>AbstractParameters</code>:</p> <ul> <li><code>start_date</code> (DateTime): Start of the optimization period</li> <li><code>end_date</code> (DateTime): End of the optimization period</li> <li><code>execution_date</code> (DateTime): Date when the optimization is executed</li> <li><code>export_result</code> (bool): Whether to export results</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#optimization-parameters","title":"Optimization Parameters","text":""},{"location":"modules/portfolio-optimisation/user-guide/input-data/#market-solver","title":"Market &amp; Solver","text":"<ul> <li> <p><code>timestep</code> (Duration, default: 1 hour): Time step of the market</p> </li> <li> <p><code>market</code> (MarketType, default: <code>dayahead</code>): Market type for optimization</p> <ul> <li>Options: <code>\"DayAhead\"</code>, <code>\"Intraday\"</code>, <code>\"RRActivation\"</code>, <code>\"MFRRActivation\"</code></li> </ul> </li> <li> <p><code>solver_name</code> (SolverEnum, default: <code>XPRESS</code>): Optimization solver to use</p> <ul> <li>Options: <code>\"XPRESS\"</code>, <code>\"PNE\"</code>, <code>\"GLOP\"</code>, <code>\"SCIP\"</code>, <code>\"CP-SAT\"</code></li> </ul> </li> <li> <p><code>solver_timeout</code> (Duration, default: 60 seconds): Maximum solve time</p> </li> <li> <p><code>solver_duality_gap</code> (float, default: 0.0001): Duality gap for optimization</p> </li> <li> <p><code>use_presolve</code> (bool, default: False): Enable solver presolve mode</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#optimization-scope","title":"Optimization Scope","text":"<ul> <li> <p><code>is_portfolio_bidding</code> (bool, default: True):</p> <ul> <li><code>True</code>: Optimize at portfolio level</li> <li><code>False</code>: Optimize individual units</li> </ul> </li> <li> <p><code>use_forecast</code> (bool, default: False):</p> <ul> <li><code>True</code>: Use price forecasts (optimization before market)</li> <li><code>False</code>: Use actual prices</li> </ul> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#exclusions","title":"Exclusions","text":"<ul> <li> <p><code>excluded_market_areas</code> (list[str], default: None): Market areas to exclude from optimization</p> <ul> <li>Format: List of strings (e.g., <code>[\"FR\", \"DE\"]</code>)</li> <li>Special values: <code>[\"all\"]</code> or <code>[\"none\"]</code></li> </ul> </li> <li> <p><code>excluded_technologies</code> (list[str], default: None): Equipment types to exclude</p> <ul> <li>Format: List of strings (e.g., <code>[\"thermal\", \"wind\"]</code>)</li> <li>Options: <code>\"thermal\"</code>, <code>\"storage\"</code>, <code>\"wind\"</code>, <code>\"solar\"</code>, <code>\"hydro\"</code>, <code>\"load\"</code>, <code>\"other_non_dispatchable\"</code></li> <li>Special values: <code>[\"all\"]</code> or <code>[\"none\"]</code></li> </ul> </li> <li> <p><code>excluded_thermal_strategy</code> (list[str], default: None): Thermal strategies to manually activate</p> <ul> <li>Format: List of strings (e.g., <code>[\"Peak\", \"Base\"]</code>)</li> <li>Options: <code>[\"Peak\"]</code>, <code>[\"Intermediate\"]</code>, <code>[\"Base\"]</code>, <code>[\"all\"]</code>, <code>[\"none\"]</code>, or <code>None</code></li> </ul> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#penalties-pricing","title":"Penalties &amp; Pricing","text":""},{"location":"modules/portfolio-optimisation/user-guide/input-data/#imbalance-penalties","title":"Imbalance Penalties","text":"<ul> <li> <p><code>imbalance_penalty_offset</code> (float, default: 10 \u20ac/MWh): Offset when forecasting imbalance settlement price</p> </li> <li> <p><code>isp_forecast_lower_bound</code> (float, default: 10 \u20ac/MWh): Lower bound of absolute ISP forecast</p> </li> <li> <p><code>small_imbalance_penalty</code> (float, default: 0.1): Coefficient for small imbalance ISP</p> </li> <li> <p><code>large_imbalance_penalty</code> (float, default: 0.2): Coefficient for large imbalance ISP</p> </li> <li> <p><code>small_imbalance_size</code> (float, default: 0.15): Percentage of portfolio energy considered \"small\"</p> </li> <li> <p><code>maximum_imbalance</code> (float, default: 100000 MW): Maximum allowed imbalance</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#reserve-penalties","title":"Reserve Penalties","text":"<ul> <li> <p><code>automated_unprocured_reserves_penalty</code> (float, default: 30000 \u20ac/MW/h): Penalty for not providing automated reserves</p> </li> <li> <p><code>manual_unprocured_reserves_penalty</code> (float, default: 30000 \u20ac/MW/h): Penalty for not providing manual reserves</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#storage-equipment-parameters","title":"Storage Equipment Parameters","text":""},{"location":"modules/portfolio-optimisation/user-guide/input-data/#battery","title":"Battery","text":"<ul> <li> <p><code>battery_number_of_fragments</code> (int, default: 3): Number of power offer fragments</p> </li> <li> <p><code>battery_smoothing_factor</code> (float, default: 0.2): Smoothing factor for power curve (0-1)</p> </li> <li> <p><code>battery_reserve_duration</code> (Duration, default: 60 minutes): Manual reserve duration</p> </li> <li> <p><code>battery_automated_reserve_duration</code> (Duration, default: 60 minutes): Automated reserve duration</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#pumped-hydraulic-storage","title":"Pumped Hydraulic Storage","text":"<ul> <li> <p><code>pumped_hydraulic_number_of_fragments</code> (int, default: 3): Number of power offer fragments</p> </li> <li> <p><code>pumped_hydraulic_smoothing_factor</code> (float, default: 0.2): Smoothing factor for power curve</p> </li> <li> <p><code>pumped_hydraulic_reserve_duration</code> (Duration, default: 60 minutes): Manual reserve duration</p> </li> <li> <p><code>pumped_hydraulic_automated_reserve_duration</code> (Duration, default: 60 minutes): Automated reserve duration</p> </li> <li> <p><code>hydraulic_minimal_fragment_size</code> (int, default: 100 MW): Minimal power for offers</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#electric-vehicle","title":"Electric Vehicle","text":"<ul> <li> <p><code>electric_vehicle_number_of_fragments</code> (int, default: 3): Number of power offer fragments</p> </li> <li> <p><code>electric_vehicle_smoothing_factor</code> (float, default: 0.2): Smoothing factor for power curve</p> </li> <li> <p><code>electric_vehicle_reserve_duration</code> (Duration, default: 1 minute): Manual reserve duration</p> </li> <li> <p><code>electric_vehicle_automated_reserve_duration</code> (Duration, default: 1 minute): Automated reserve duration</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#performance-parameters","title":"Performance Parameters","text":"<ul> <li> <p><code>use_multiprocessing</code> (bool, default: True): Use parallel processing for portfolios</p> </li> <li> <p><code>max_workers</code> (int, default: None): Max parallel processes (None = CPU count)</p> </li> <li> <p><code>debug</code> (bool, default: False): Enable debug mode</p> </li> <li> <p><code>allowed_round_off_error</code> (float, default: 0.01 MW): Rounding error threshold</p> </li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#example-configuration","title":"Example Configuration","text":"<pre><code>{\n  \"start_date\": \"2024-01-01T00:00:00\",\n  \"end_date\": \"2024-01-02T00:00:00\",\n  \"execution_date\": \"2023-12-31T12:00:00\",\n  \"export_result\": true,\n  \"market\": \"DayAhead\",\n  \"solver_name\": \"XPRESS\",\n  \"solver_timeout\": \"PT60S\",\n  \"timestep\": \"PT1H\",\n  \"is_portfolio_bidding\": true,\n  \"use_forecast\": false,\n  \"use_multiprocessing\": true,\n  \"excluded_market_areas\": [\"FR\", \"DE\"],\n  \"excluded_technologies\": [\"thermal\"],\n  \"excluded_thermal_strategy\": [\"Peak\"]\n}\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/input-data/#next-steps","title":"Next Steps","text":"<ul> <li>Running: How to execute the module</li> <li>Results: Understanding outputs</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/overview/","title":"User Guide Overview","text":""},{"location":"modules/portfolio-optimisation/user-guide/overview/#introduction","title":"Introduction","text":"<p>The Portfolio Optimisation module optimizes energy portfolios to determine optimal dispatch strategies for thermal, hydro, solar, wind, storage, and load assets.</p>"},{"location":"modules/portfolio-optimisation/user-guide/overview/#how-to-use","title":"How to Use","text":"<p>The module follows Atla's standard <code>AbstractModule</code> pattern, run it simply by calling <code>run</code> method:</p> <pre><code>module = PortfolioOptimisationModule()\nmodule.run(input_data: dict[str, list[BusinessModel]], parameters: PortfolioOptimisationParameters)\n</code></pre> <p>Where:</p> <ul> <li><code>raw_data</code>: Dictionary of business model objects (portfolios, equipment, market areas)</li> <li><code>raw_params</code>: Parameter dictionary or path to JSON/YAML file</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/overview/#module-workflow","title":"Module Workflow","text":"<p>The <code>run()</code> method executes:</p> <ol> <li>Import Parameters: Load <code>PortfolioOptimisationParameters</code></li> <li>Import Data: Convert to <code>PortfolioOptimisationInputDataset</code></li> <li>Validate: Check timestep consistency</li> <li>Execute: Run optimization for each portfolio</li> <li>Export: Update equipment <code>power</code> and portfolio <code>imbalance</code> (if <code>export_result=True</code>)</li> </ol> <p>Results are stored directly in the business model objects.</p>"},{"location":"modules/portfolio-optimisation/user-guide/overview/#next-steps","title":"Next Steps","text":"<ul> <li>Parameters: Configuration options</li> <li>Running: Execution details</li> <li>Results: Accessing outputs</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/results/","title":"Results","text":""},{"location":"modules/portfolio-optimisation/user-guide/results/#overview","title":"Overview","text":"<p>Results are stored in business model objects:</p> <ul> <li>Equipment: <code>power</code> attribute (ForecastingMatrix)</li> <li>Portfolio: <code>imbalance</code> attribute (ForecastingMatrix)</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/results/#accessing-results","title":"Accessing Results","text":"<pre><code># Equipment power forecast\nforecast = equipment.power.get_forecast(execution_date, start_date, end_date)\ndf = forecast.dataframe\n\n# Portfolio imbalance\nimbalance = portfolio.imbalance.get_forecast(execution_date, start_date, end_date)\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/results/#power-values-by-equipment-type","title":"Power Values by Equipment Type","text":"<ul> <li>Thermal/Hydro: Generation (MW)</li> <li>Storage: Charge (negative) / Discharge (positive) (MW)</li> <li>Wind/Solar: Curtailed power (MW)</li> <li>Load: Consumption (MW)</li> </ul>"},{"location":"modules/portfolio-optimisation/user-guide/results/#imbalance","title":"Imbalance","text":"<pre><code>imbalance = large_imbalance_down + small_imbalance_down\n          - large_imbalance_up - small_imbalance_up\n</code></pre> <p>Positive = over-generation, Negative = under-generation</p>"},{"location":"modules/portfolio-optimisation/user-guide/results/#troubleshooting","title":"Troubleshooting","text":"<p>No power update: Check <code>export_result=True</code>, verify optimization succeeded, check if equipment was excluded</p> <p>Unexpected values: Verify input data, check solver status, review parameters, enable <code>debug=True</code></p> <p>Zero generation: Check startup/min generation constraints, fuel costs vs prices, min up/down times</p>"},{"location":"modules/portfolio-optimisation/user-guide/running/","title":"Running the Module","text":""},{"location":"modules/portfolio-optimisation/user-guide/running/#basic-usage","title":"Basic Usage","text":"<pre><code>from atlas.modules.portfolio_optimisation import PortfolioOptimisationModule\n\nmodule = PortfolioOptimisationModule()\nmodule.run(raw_data, raw_params)\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/running/#input-data-structure","title":"Input Data Structure","text":"<p>Input data is basically a dictionary of business model objects by type, obtained by a call to <code>InputLoader.from_directory</code>:</p> <pre><code>raw_data = {\n    \"portfolio\": [portfolio1, portfolio2, ...],\n    \"thermal\": [thermal_unit1, ...],\n    \"hydro\": [hydro_unit1, ...],\n    \"storage\": [storage1, ...],\n    \"solar\": [solar1, ...],\n    \"wind\": [wind1, ...],\n    \"load\": [load1, ...],\n    \"other_non_dispatchable\": [...],\n    \"market_area\": [...],\n    \"control_block\": [...]\n}\n</code></pre>"},{"location":"modules/portfolio-optimisation/user-guide/running/#parameters","title":"Parameters","text":"<p>Provide as dictionary or file path:</p> <pre><code># Dictionary\nparams = {\n    \"start_date\": \"2024-01-01T00:00:00\",\n    \"end_date\": \"2024-01-02T00:00:00\",\n    \"execution_date\": \"2023-12-31T12:00:00\",\n    \"export_result\": true,\n    \"solver_name\": \"XPRESS\",\n    \"timestep\": \"PT1H\"\n}\n\n# Or JSON file\nmodule.run(raw_data, \"config/parameters.json\")\n</code></pre> <p>See Parameters for full list.</p>"},{"location":"modules/portfolio-optimisation/user-guide/running/#execution-modes","title":"Execution Modes","text":"<p>Portfolio-level (<code>is_portfolio_bidding=true</code>): Optimizes portfolios with imbalance penalties</p> <p>Individual units (<code>is_portfolio_bidding=false</code>): Optimizes each unit independently</p>"},{"location":"modules/portfolio-optimisation/user-guide/running/#key-options","title":"Key Options","text":"<p>Multiprocessing: Set <code>use_multiprocessing=true</code> and <code>max_workers</code> to parallelize portfolio optimization</p> <p>Manual activation: Use <code>excluded_market_areas</code>, <code>excluded_technologies</code>, or <code>excluded_thermal_strategy</code> to exclude equipment from optimization</p> <p>Solver: Choose solver with <code>solver_name</code> parameter and configure <code>solver_timeout</code>, <code>solver_duality_gap</code>, <code>use_presolve</code></p>"}]}