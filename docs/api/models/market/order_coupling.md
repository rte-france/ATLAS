# OrderCoupling

Links two orders together to model coupled constraints (e.g. block orders, exclusive groups).

::: atlas.OrderCoupling
    options:
        show_if_no_docstring: false
        filters:
            - "!^_"
            - "!serializer"
            - "!^parse_"
            - "!^validate_"
